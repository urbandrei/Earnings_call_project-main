\section{Complete Results}
\label{app:complete}

The tables below are generated byte-identically by \texttt{ecvol report} from the
committed result CSVs (\texttt{data/results/}) and are the source of truth for the
curated tables in \S\ref{sec:results}; the curated tables copy selected cells from
them. They span both corpora (FinCall-Surprise, MAEC), the level-$v$ and \dv{}
targets, both the \Rtwo{} and MSE metrics, and the temporal and \tickerdisjoint{}
splits at all horizons, under the trading-day convention
\pending{calendar-day columns, T9.1}.

% Paths are relative to this file (paper/sections/). The generated .tex files live
% in the repo's data/results/ tree. If your TeX setup restricts \input to the
% project tree, copy them into paper/tables/ instead and update these two lines.
% \clearpage between the generated files flushes each batch of deferred floats;
% without it ~56 consecutive floats deadlock the two-column output routine.
\subsection{Stage-0/1 baselines (Result Table 1)}
\input{../data/results/result_table_1}
\clearpage

\subsection{Stage-2 text heads (Result Table 2)}
\input{../data/results/result_table_2}
\clearpage

\subsection{Stage-3 audio heads (Result Table 3)}
FinCall-Surprise only (MAEC ships no raw audio).
\input{../data/results/result_table_3}
\clearpage

\subsection{Consolidated ablation grid (Result Table 4)}
One pre-registered canonical model per stage; $*$ marks Holm-corrected significance
vs.\ Stage-1 within the \dv{} confirmatory family (two-sided---read direction from
the value).
\input{../data/results/result_table_4}
\clearpage

\subsection{Substrate audit (Result Table 5R)}
\pending{T6R.1: the full per-file audit behind Table~\ref{tab:substrate}---SHA-256
manifests of the fetched VolTAGE/KeFVP/HTML label and split files, boundary dates
and call counts, overlap by cohort, the sentinel-zero cells, the interleaved-column
schema hazard, and the diff of the shipped labels against our recomputed targets
(MAEC join verified at 99.4\%).}

\subsection{Reproductions and code-availability audit (Result Table 6R)}
\pending{T6R.2: the complete reproduction grid behind Table~\ref{tab:repro}, and
the audit of the fifteen non-runnable papers---repository URL, HTTP status and
date, and the reason code (no code; empty or unpushed repository; closed-API
dependency; stated inability to run; paywalled).}
