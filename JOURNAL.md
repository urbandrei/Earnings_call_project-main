# JOURNAL.md — Work Journal

**Append-only, newest entry last.** A chronological record of work performed on the project: what was done, what was found, what broke, what's next. The journal is the project's narrative memory — [TASKS.md](TASKS.md) says *what state* the work is in; this file says *how it got there*.

## What gets an entry

Append an entry at the end of every working session (or completed task, whichever is finer-grained). Include:

- **Date + scope** — what task(s)/topic the session touched (use task IDs where applicable).
- **Done** — work completed, with enough specifics that a future reader can retrace it (commands, files, run IDs).
- **Found** — results, measurements, surprises, dead ends. Negative findings are first-class: an experiment that failed is information.
- **Sources** — papers/links/model cards consulted (anything reusable should also land in DESIGN.md §13).
- **Next** — the intended next step, so any session can pick up where the last one stopped.

Keep entries factual and compact. Decisions that change the design belong in [DECISIONS.md](DECISIONS.md) (link the journal entry to them); the journal records the work and evidence around them.

---

## Log

### 2026-06-12 — Project review, literature research, design doc
- **Done:** Full review of the inherited legacy project (12 notebooks, Paper.pdf, papers/ library); established it is unrunnable (data lost with the `D:` drive, no environment spec). Three parallel web-research sweeps: (1) post-KeFVP SOTA and documented pitfalls, (2) open dataset options, (3) open-source model landscape for consumer GPUs. Wrote DESIGN.md (research questions, data/model/eval design, 9-phase plan, risk register).
- **Found:** Key literature facts that shaped the design — "Same Company, Same Signal" (arXiv 2412.18029): transcript models largely memorize ticker identity and training-free past-vol baselines beat them; financial-NLP reproducibility crisis (~14% of results reproduce, arXiv 2504.07274); FinCall-Surprise (arXiv 2510.03965, Apache-2.0, 2,688 calls 2019–2021 with full audio) is the best truly-open multimodal dataset; legacy +0.73%-vs-KeFVP result is single-seed and not defensible.
- **Sources:** consolidated in DESIGN.md §13 (R1–R27, D1–D8).
- **Next:** docs restructure, then T0.1 scaffolding.

### 2026-06-12 — Docs restructure
- **Done:** Renamed `Earnings_call_project-main/` → `legacy/` (read-only; deleted its stale CLAUDE.md); moved DESIGN.md to project root and removed `new/`; extracted the full task breakdown into TASKS.md (living tracker, 30 tasks T0.1–T8.3); wrote OLDWORK.md (legacy summary, marked outdated); wrote root CLAUDE.md (working principles, automation/testing discipline, exploration policy); moved the decision log out of DESIGN.md into DECISIONS.md; created this journal. Git deliberately not initialized yet — first subtask of T0.1.
- **Found:** Karpathy reference check: he never published a CLAUDE.md template; the viral file is Forrest Chang's distillation of Karpathy's Jan-2026 post on four agent failure modes (silent assumptions, code hypertrophy, collateral changes, no verifiable success criteria) — CLAUDE.md's principles built on those primary sources.
- **Next:** T0.1 — `git init`, .gitignore, initial commit, package skeleton, CI.

### 2026-06-12 — T0.1 package skeleton
- **Done:** Installed uv 0.11.21 (official installer → `~\.local\bin`). `git init -b main`; `.gitignore` (glob-based: `data/` except `data/manifests/`, `artifacts/`, caches, `.env`, legacy bulk binaries — see DECISIONS.md entry) + `.gitattributes` (LF normalization). Eight small commits: governance docs → repo config → legacy reference files → package skeleton → pre-commit → CI workflow → README → doc updates. Package: `pyproject.toml` (uv_build backend, Python pinned 3.12 via `.python-version`, lockfile committed), `src/ecvol/` subpackage layout per DESIGN.md §8.1 (config/data/features{text,audio,llm}/models/eval), Typer CLI with all seven verbs (`prices|targets|splits|featurize|train|evaluate|report`) as stubs exiting code 2, smoke tests (help lists every verb; stubs fail loudly). Ruff (E,F,I,UP,B; line 100; `legacy/` excluded) + pre-commit (ruff-check --fix, ruff-format) installed and green. CI workflow `.github/workflows/ci.yml`: `uv sync --locked` → ruff check → format check → pytest. Fresh-machine install tested once: cloned repo to `%TEMP%`, `uv sync --locked` + tests green from the lockfile alone; steps documented in README.md.
- **Found:** Python 3.14.4 is the system default — pinned 3.12 instead for ML-wheel compatibility (torch et al. lag new CPython releases). Ruff initially linted the legacy notebooks (395 KB of findings) → `extend-exclude = ["legacy"]`, consistent with their read-only status. PowerShell 5.1 `Set-Content -Encoding utf8` writes BOMs — generated files rewritten BOM-free.
- **Sources:** uv install/sync docs (docs.astral.sh/uv), astral-sh/ruff-pre-commit, astral-sh/setup-uv action.
- **Next:** User: create the GitHub repo and push (needs `gh auth login` or manual repo creation) to verify Actions green — the one open T0.1 acceptance item. Then T0.2 config system.

### 2026-06-12 — T0.1 closed
- **Done:** User created the GitHub repo (`urbandrei/Earnings_call_project-main`, private) and pushed; CI run on `main` green on first try (user-verified in the Actions tab — repo is private, so no API access from the agent without `gh`). T0.1 acceptance fully met → `[x]`.
- **Found:** Verifying Actions on a private repo from this machine needs an authenticated `gh`; worth installing (`winget install GitHub.cli` + `gh auth login`) before CI-dependent phases.
- **Next:** T0.2 config system (pydantic schemas, example YAML, config-hash function).

### 2026-06-12 — T0.2 config system
- **Done:** Added pydantic 2.13.4 + PyYAML 6.0.3 (`uv add`, lockfile committed). `src/ecvol/config/`: `schema.py` — `ExperimentConfig` with `DataConfig` (dataset literal), `TargetConfig` (variant level/delta/har_residual; horizons restricted to {3,7,15,30} per DESIGN.md §5.3), `SplitConfig` (temporal/ticker_disjoint/combined + embargo), `FeatureSpec` list (kind/extractor/model_id/revision/options — minimal now, extended when Phases 3–6 land), `ModelConfig` (name + `include_past_vol_covariates` for the §6 invariant + params dict), `EvalConfig` (metrics, bootstrap resamples); `extra="forbid"` on every model so typos fail loudly. `load.py` — `load_config` (ConfigError with file path + dotted field locations), `dump_config` (deterministic sorted YAML), `config_hash` (canonical JSON, sorted keys, compact separators → SHA-256). `configs/example.yaml` (5-seed GBDT on FinCall-Surprise temporal split). 11 tests in `tests/test_config.py` cover both acceptance cases plus round-trip stability and hash canonicalization. Three commits (deps / config system / docs).
- **Found:** Two design-derived validations added beyond the task spec: (1) cross-field check that `embargo_trading_days` ≥ longest horizon on temporal/combined splits — encodes the §5.4 leakage rule at config time; (2) uniqueness of seeds and horizons. Horizons are also sorted on resolve so the config hash is order-insensitive. Pydantic field named `model` does not clash with the `model_` protected namespace (no warning).
- **Next:** T0.3 run tracking & manifests (manifests.py, run-ID scheme + artifact writer, env fingerprint).

### 2026-06-12 — T0.3 run tracking & manifests
- **Done:** Added pyarrow 24.0.0 (`uv add`). `src/ecvol/data/manifests.py`: `ManifestEntry` (path/source_url/retrieved_at/sha256/license, extra="forbid"), chunked `sha256_file`, deterministic `write_manifest` (sorted by path), `verify_manifest` returning per-file problems; new `ecvol data verify` CLI group (defaults to scanning `data/manifests/*.json`, exit 1 on any mismatch/missing file). `src/ecvol/tracking.py`: run-ID scheme `<UTC timestamp>-<config-hash[:8]>`; `write_run` writes `artifacts/runs/<id>/{config.yaml, run.json, metrics.parquet}` with config hash, git SHA+dirty, seeds, env fingerprint (python, platform, uv.lock SHA-256, GPUs via nvidia-smi); metrics validated (non-empty, consistent columns) *before* any writes; existing run dirs never overwritten. 24 tests green; five commits.
- **Found:** (1) T0.1 latent bug: the unanchored `data/` gitignore pattern also matched `src/ecvol/data/`, so the package's data subdir was never committed — and `!data/manifests/` could never re-include below an excluded dir. Fixed with root-anchored `/data/*` + `!/data/manifests/`. (2) Parquet bit-identity holds across separate interpreter processes with pyarrow defaults (compression="none"; footer has no timestamp) — verified manually, asserted in-process in tests. (3) `env_fingerprint()` on this machine: RTX 5060 Ti, driver 591.86. The acceptance's "rerun a CPU-only config" is covered at writer level (deterministic bytes for identical rows); the end-to-end form becomes testable when `ecvol evaluate` lands (T2.2).
- **Sources:** none beyond project docs.
- **Next:** Phase 1 — T1.1 dataset acquisition & mirroring (FinCall-Surprise + MAEC fetchers, manifests now ready for them). Push and confirm CI green (user verifies Actions).

### 2026-06-12 — T0.3 closed
- **Done:** CI green on GitHub for the T0.3 push (user-verified in the Actions tab). T0.3 acceptance fully met; Phase 0 (scaffolding) complete.
- **Next:** T1.1 dataset acquisition & mirroring.

### 2026-06-12 — T1.1 dataset acquisition & mirroring
- **Done:** Sized both datasets before downloading (FinCall-Surprise Drive payload: mp3 18+14+20 GB + ppt 1.7+1.4+2.3 GB ≈ 57 GB zipped; C: had ~49 GB free) → bulk mirror lives on the empty internal D: (954 GB) behind an NTFS junction `data\raw` → `D:\ecvol-data\raw`, so code/manifests keep relative paths (DECISIONS.md). Added `gdown`+`requests`; wrote `src/ecvol/data/fetch.py` (downloads pinned to upstream commit SHAs — FinCall `36bb82d2`, MAEC `65a109f5`; atomic temp+rename downloads/extractions, idempotent re-runs) and CLI verbs `ecvol data fetch fincall|maec|all` (+ `--skip-drive`) and `ecvol data spotcheck` (seeded-random ffmpeg decode). 8 tests in `tests/test_fetch.py`. Installed ffmpeg 8.1.1 (winget, Gyan.FFmpeg). Mirrored MAEC (codeload tarball, 3,443 call folders, manifest verifies clean) and FinCall (transcripts from GitHub; six Drive zips via gdown — hit Drive's "many accesses" quota after 2 zips; a 15-min background retry loop cleared it on attempt 2). Per-file SHA-256 manifests committed (`data/manifests/{fincall,maec}.json`).
- **Found:** (1) **MAEC's 59 GB high-level MFCC archive is link-rotted** (Drive id `1yXLv1r5n35s4Nf7kvP96RSPLKyR-ZeUT` → 404, no mirror findable) — the exact risk T1.1 front-runs; documented in DECISIONS.md; MAEC mirror = transcripts + low-level `features.csv` (which the repo itself carries in full). (2) Counts match published sizes *exactly*: FinCall 2,688 (919+704+1065), MAEC 3,443 (3,443 with text, 3,443 with features). (3) FinCall audio join: every call has a unique `mp3_id`; 2,671/2,688 (99.4%) have the file; 17 reference missing audio; 456 mp3 files are surplus (unreferenced by any transcript). ppt files: 2,671. (4) Spot-decode: 50/50 seeded-random mp3s decode cleanly (ffmpeg, seed 0; plus 20/20 early check on the first two years, seed 1). (5) Google Drive folder listing scraped via `embeddedfolderview` + file sizes via `drive.usercontent.google.com` warning pages — useful pattern for sizing public Drive payloads before committing disk.
- **Sources:** github.com/Tizzzzy/FinCall-Surprise (Apache-2.0); github.com/Earnings-Call-Dataset/MAEC-… (CC-BY-SA-4.0); github.com/wkentaro/gdown (quota FAQ).
- **Next:** T1.2 price ingestion (Stooq fetcher + Tiingo cross-check, ticker normalization, coverage report). Push and confirm CI green (user verifies Actions).

### 2026-06-12 — Exploration: FinCall-Surprise identity reconstruction feasibility
- **Done:** Discovered while scoping T1.2 that FinCall-Surprise records carry **no ticker, company name, or call date** (opaque numeric IDs; Seeking Alpha headers stripped from `input`). Probed recovery routes: SA article-ID URLs (404), EarningsCast — the paper's audio source — is dead entirely (HTTP 410 site-wide), mp3 ID3 tags (empty), slide PDFs (rich: `/Company` metadata, title-page text, dates). Per user decision (DECISIONS.md), ran a 50-call seeded feasibility study (`notebooks/explore_fincall_identity.py`, seed 0): extract name candidates from PDF metadata/page-1 + operator-greeting regex, match against SEC `company_tickers.json` (8,007 names), extract dates from PDF page-1 text + creation stamps.
- **Found:** (1) **Date recovery: 50/50 (100%)** — PDF page-1 date or creation stamp present on every sampled call (PDFs cover 2,671/2,688 calls). (2) **Ticker via quick heuristics: 29/50 (58%)**, but the bottleneck is name→ticker *matching*, not signal: manual read of all misses confirms the company is named plainly in the transcript's first ~700 chars in 10/10 no-candidate cases (Arista, International Paper, DXC, DowDuPont, Vulcan, TransDigm, Stanley Black & Decker…). Failures are brand-vs-legal-name gaps (ADP/Citi/ITW/Otis/PPG vs "Automatic Data Processing"/"Citigroup"/"Illinois Tool Works"/…) — fixable with an alias table and/or LLM-assisted name extraction + deterministic matching. Realistic ceiling ≥95%. (3) **New risk: corpus heterogeneity** — the sample + earlier spot-reads contain non-earnings calls (JPMorgan tech-forum fireside with Qualcomm, DXC/Luxoft M&A announcement, Ford monthly-sales call, BMS/Celgene merger call). The "2,688 conference calls" are not all earnings calls; T1.4 needs a call-type classifier + exclusion reason codes, and the per-call `label` (earnings surprise) may be the cleanest earnings-call indicator to cross-check. (4) SEC `company_tickers.json` (free, official, name+ticker+CIK) works well as the matching backbone.
- **Sources:** arXiv 2510.03965 (FinCall-Surprise paper: transcripts from Seeking Alpha, audio EarningsCast, slides Bloomberg/company sites; identity table kept internal for IBES join); sec.gov/files/company_tickers.json.
- **Next:** Decide T1.4 scope expansion + T1.2 ordering (DECISIONS.md entry pending user review); deferred option: email FinCall authors for their internal call-ID→identity table.

### 2026-06-12 — T1.1 closed
- **Done:** User pushed (8 commits) and verified GitHub Actions green. T1.1 fully closed; T1.4 identity work starts next per DECISIONS.md reorder.

### 2026-06-12 — T1.4 identity reconstruction: pipeline built, 77.5% resolved, 100% sample accuracy
- **Done:** Promoted the exploration into `src/ecvol/data/fincall_identity.py` + `ecvol data identity` (per DECISIONS.md scope expansion; T1.4 pulled ahead of T1.2). Evidence model: full-transcript capitalized-phrase mentions (multi-word SEC names only, scored by CIK so share classes merge) + five weighted greeting/host patterns (welcome-to forms 6, host/IR forms 4) + PDF `/Company` (2, it is sometimes the deck-design agency) ; resolve requires score ≥2 and a ≥2 margin over the runner-up — never guess, flag instead. Greeting-side lookup: exact → possessive-dropped → unique-prefix (largest-cap first) → guarded fuzzy (0.85, same first token). SEC table fetched with the required User-Agent (403 otherwise), cached + brand aliases from distinctive first/last title tokens; generic single-token keys ("INTERNATIONAL CORP"→"international") dropped. PDF page-1/metadata signals cached to JSON (`data/raw/ref/fincall_pdf_signals.json`) — iteration went from ~10 min to seconds. Four matcher iterations driven by residue analysis (58%→63%→66%→75%→77.5%). Identity CSV committed (`data/identity/fincall_identity.csv`).
- **Found:** (1) **Resolution 2,084/2,688 (77.5%); dates 2,629 (97.8%); call types: 2,624 earnings, 14 non-earnings, 50 unknown.** (2) **Accuracy: 40/40 seeded-random resolved calls verified correct by transcript read** — coverage is the open problem, not precision. (3) Failure-class taxonomy from residue sampling: junk single-token SEC matches (companies named "On"/"Bill"/"Charlie" — fixed by multi-word-only body counting); share-class self-competition (fixed by CIK keying); EDGAR title quirks (`/MA/` state tags, "trust" wrongly treated as suffix broke Northern Trust); possessive brands (Kohl's→kohls); single-letter spacing (U.S. Bancorp); name-after-quarter greetings (Phillips 66, Zebra); leading event words (Q2 2021 Kohl's). (4) **Remaining residue (~604 calls) is dominated by companies absent from the *current* SEC table — delisted/renamed/acquired since 2021 (Nordstrom private, Kellogg→Kellanova, Campbell Soup→Campbell's, US Steel acquired) — plus brand acronyms (Citi, ADM, BD)**; 162 unique unmatched greeting names cover 401 of the unresolved calls → a curated per-company override CSV (~100-150 entries) is the efficient next step, possibly seeded from SEC EDGAR submissions' formerNames. (5) PDF `/Company` metadata is unreliable (sometimes the design agency) — correctly weighted low.
- **Sources:** sec.gov/files/company_tickers.json (requires descriptive User-Agent); difflib for guarded fuzzy matching.
- **Next:** Override table for the delisted/renamed/acronym residue → target ≥95%; formalize the accuracy gate (larger audited sample, committed); date validation against trading calendar lands with T1.3. Then T1.2 price ingestion.

### 2026-06-12 — Identity pipeline pushed
- **Done:** User pushed the identity-reconstruction work (8 commits) and verified GitHub Actions green.
- **Next:** Override table for delisted/renamed/acronym residue (→ ≥95% resolution), larger committed accuracy audit, then T1.2.

### 2026-06-12 — T1.4 identity v2: overrides + audit gate, 95.3% on earnings calls
- **Done:** Pushed identity resolution 77.5% → 92.9% overall (**earnings-type 2,381/2,499 = 95.3%**, clearing the ≥95% target) across five committed iterations, each diffed against the previous CSV for flips/losses before landing. (1) Matcher bug fixes: norm_name collapsed whitespace *after* the single-letter join (dotted initials normalized inconsistently — "C. H. Robinson" vs "C.H. Robinson"); fuzzy lookup gave up when the single closest key failed the first-token guard (n=1 → scan n=5). (2) Greeting hygiene: leading-filler stripping ("you to the Adobe", "today's Bank of America"), proper-noun shape filter (the re.I patterns made `[A-Z]` case-blind — "ladies"/"our" reached lookup), months/ordinals/"Reports"/"Business Update"/'21-style event words. (3) **Curated override CSV** (`data/identity/fincall_name_overrides.csv`, 77 rows): brand acronyms (ITW, Citi, UPS, ADP, IBM…) and companies absent from the *2026* SEC table — delisted/renamed since the corpus era (Nordstrom, Kellogg, US Steel, Hess, ANSYS, Walgreens, Discover, Comerica, Synovus…); every CIK verified against the live table by ticker or EDGAR's historical registrant list (`notebooks/verify_identity_overrides.py`, 0 problems). (4) Scoring: capped single-token body evidence (recurrence ≥2, cap 3, whole-name keys only — derived first/last-token aliases matched prose: "Turning"→Turning Point Brands, "Watson"→WTW, "Escalade"→GM junk); greeting scan skips junk captures but stops at the first plausible name (scan-until-table-hit let 'ups' hijack United Continental); **greeting-dominance rule** — unchallenged strong greeting evidence beats body-mention volume (GE's call discusses Baker Hughes 10×; Southern's its Georgia Power sub; Occidental's its "Permian Resources" *segment*; Macy's its "State Street" *store*); weak body-only scores (≤3) need corroboration in the opening 1,500 chars (two prose "Moody's" mentions once resolved a Discover call as MCO); possessive spans retried with 's dropped ("Amgen's" → AMGEN); brand-token collisions go to the larger company (Vertex Pharma vs Vertex-the-tax-co; Prudential Financial vs Prudential plc). (5) Call-type classifier: new `meeting` type; strong conference markers (fireside, "introduce our next company", sell-side analyst intros) outrank earnings keywords, with guards for "investor conference call" and "fireside chat at <other venue>" housekeeping (caught a Macy's earnings call mislabeled in spot-check). Counts: 2,499 earnings / 80 conference / 61 meeting / 33 unknown / 14 ma+sales.
- **Found:** (1) **The committed audit gate passed: 60/60 correct** on a seeded stratified sample (40 random resolved-earnings + 20 newly-resolved; `data/identity/fincall_identity_audit.csv`). The audit itself caught two real errors mid-session (Vertex-Pharma-as-VERX, Discover-as-MCO) that drove fixes — sampling resolved calls is as important as chasing the residue. (2) **~30 v1 identities were wrong and are now corrected or honestly unresolved** — the diff-every-iteration discipline (notebooks/diff_identity.py) was what surfaced them; headline-rate-only tracking would have hidden every one. (3) The 2026 SEC table is missing far more corpus-era issuers than expected (Hess, Discover, Coterra, Sealed Air, Interpublic, Comerica, Hologic, Synovus, ANSYS… — the 2024-26 M&A wave); the override CSV is the durable fix and the EDGAR cik-lookup-data.txt file (all registrants ever) verifies historical CIKs. (4) Remaining unresolved (~190) is dominated by mid-call [Audio Gap] fragments, sell-side conference sessions, and webinars — mostly non-earnings; identity for them is low-value. (5) Classification heuristics still mislabel some conference sessions as earnings (McCormick at a consumer conference) — identity is correct there; refine when exclusion reason codes land with the parser.
- **Sources:** sec.gov/files/company_tickers.json; sec.gov/Archives/edgar/cik-lookup-data.txt (historical registrant names → CIK); EDGAR formerNames concept via DECISIONS.md plan.
- **Next:** T1.4 remaining subtasks: transcript JSON parser onto the common schema, timestamp extraction/validation (after-hours rule), join audit (needs T1.2). Then T1.2 price ingestion against the now-known universe. Push and confirm CI green (user verifies Actions).

### 2026-06-14 — Reviewed prior-team multimodal-vol work; integrated into the charter; open-weight QA feasibility scout
- **Done:** Reviewed this team's prior work (research log + Undermind literature review dropped in `ingest/`), wrote `ingest/ingest.md` (full breakdown + 37-figure index + project-fit/conflict analysis), then **integrated the keepable parts into the governance docs** (per "bring it on board, stay true to the charter"): appended seven DECISIONS.md entries (provenance + integration approach; speaker-turn chunking; §13 refs; TX1/TX2/TX3 promotions; Stage-6 audio-LLM recipe); added a TASKS.md **"Exploration tracks"** section (TX1 open-weight QA features, TX2 short-horizon/IV target, TX3 re-examine the prior KeFVP-beating result through our controls) and a speaker-turn-chunking subtask to T3.1; added DESIGN.md §13 refs **[R28]–[R32]** (Multi-Round Q&A IJCAI'20, Multimodal Multi-Task ACM-MM'20, NAM/ECNum CIKM'21, NumHTML AAAI'22, GNAVol IJCNLP'23 — all URL-verified) plus minimal §5.3/§6 forward-pointers. Ran an open-weight QA-generation feasibility scout (`notebooks/explore_qa_generation.py`) on 3 FinCall earnings calls.
- **Found:** (1) **Open-weight QA generation is viable.** Qwen2.5-3B-Instruct (bf16, no quantization, 6.2 GB) produces usable, generalized, evidence-grounded Q&A pairs with source lines and categories — comparable in spirit to the prior team's GPT-5 output (e.g. BMS/Celgene: "What financial benefits does the transaction offer?" → grounded answer on >40% accretion, $45B FCF). Boilerplate chunks correctly return `[]`. This de-risks TX1's open-weights constraint; the DESIGN-pinned 7B should do at least as well. (2) **Categories drift generic** ("Volatility"/"Stock Volatility") — exactly what TX1's topic-clustering step is meant to fix. (3) `source_lines` format is inconsistent (`[12]`, `[11],[13]`, `[5,6]`, `[18-19]`) → needs normalization in TX1. (4) **FinCall transcripts have weak speaker delineation** — the `input` field is ~20–45 newline paragraphs with occasional inline `Executives:` markers, not clean per-turn structure; true speaker-turn chunking needs a better source (a finding for T3.1). (5) DeFVP (ICME 2024) could not be URL-verified → deferred from §13. (6) **Env friction (Blackwell stack):** the project `.venv` has no GPU torch (torch is a Phase-3+ dep); a scratch `uv pip install` pulled a **CPU** torch 2.12 + downgraded `typer` into `.venv`, so the scout ran on the **global py3.14** (CUDA torch 2.11 + `transformers` 5.12). 7B-in-4-bit/bitsandbytes was skipped (fragile on torch 2.11/cu128 + sm_120) in favor of 3B bf16. **`.venv` restored** with `uv sync --locked` (typer 0.26.7 back, scratch pkgs gone, lockfile never touched). `transformers`+`accelerate` remain in the global py3.14 (harmless; removable on request). transformers 5.x API note: `apply_chat_template(..., return_dict=True)` → `generate(**inputs)`.
- **Sources:** `ingest/ingest.md` and the two `ingest/` resources (prior-team log + Undermind report 2025-11-22); verified ref URLs (arxiv 2201.01770; aclanthology 2023.ijcnlp-short.5; ijcai 2020/631; doi 10.1145/3394171.3413752; doi 10.1145/3459637.3482089); `Qwen/Qwen2.5-3B-Instruct` (Apache-2.0).
- **Next:** continue **T1.2** price ingestion (the actual phase-order work). TX1/TX2/TX3 stay parked as gated exploration tracks; they build later in phase order, none ahead of its dependencies.

### 2026-06-15 — T1.2 price ingestion (source pivot Stooq→yfinance; 95.88% FinCall coverage; Tiingo built)
- **Done:** Built the price pipeline. New modules: `data/calendar.py` (cached XNYS wrapper over `exchange_calendars`), `data/prices.py` (combined FinCall∪MAEC universe assembly, batched yfinance fetch, deterministic OHLCV parquet à la `tracking.py`, coverage report, per-ticker manifest with source provenance, **Tiingo fallback** for Yahoo-purged tickers), `data/tiingo.py` (5% seeded sampler, pure return-correlation gate, `fetch_tiingo_ohlcv`). CLI: `prices` is now a sub-app — `ecvol prices pull` and `ecvol prices crosscheck`. Added deps yfinance/pandas/exchange_calendars (lockfile committed). Pulled the universe: **999/1,309 tickers** cached to `data/prices/*.parquet` (gitignored); committed artifacts = `data/coverage/prices_coverage.csv`, `prices_sources.json`, `data/manifests/prices.json` (999 entries, `ecvol data verify` clean). 78 tests green (added `test_prices`/`test_calendar`/`test_tiingo`, 17 new; network mocked throughout), ruff clean.
- **Found:** (1) **Stooq (DESIGN §5.2 primary) closed free programmatic access** — per-symbol `/q/d/l/` now serves a JS SHA-256 proof-of-work wall *and*, once solved (an `auth` cookie is issued), returns `Access denied`; the bulk `/db/d/?b=d_us_txt` bundle returns `Unauthorized` (account required). Pivoted to **yfinance primary + Tiingo cross-check** (user decision; DECISIONS.md 2026-06-15). (2) **FinCall coverage 372/388 = 95.88%** (gate 98%); covered tickers essentially complete (min completeness 0.979, **zero gappy**). MAEC 906/1,213 = 74.69% (informational pre-T1.5). (3) **The 16 missing FinCall tickers are exactly yfinance's documented blind spot — delisted/acquired symbols Yahoo purges** (verified individually: CMA/WBA/K/ANSS/X/JWN/DFS/HES/CTLT/IPG/SNV/SEE/HOLX… all 404 "Quote not found", including a few that arguably still trade → Yahoo API coverage gaps). This is the precise §5.2 risk that motivated rejecting yfinance; **mitigated by the coverage report (zero silent drops, every miss reason-coded) + the Tiingo fallback** (`fetch_tiingo_ohlcv` auto-recovers Yahoo misses when a key is present), which should clear 98% once the key lands. (4) yfinance batch drops under load were *not* the cause — individual retries also 404; the misses are genuine Yahoo absences. (5) Determinism: OHLCV parquet is byte-identical for identical rows (pyarrow `compression="none"`), matching the T0.3 metrics-parquet convention.
- **Pending (both blocked on a free `TIINGO_API_KEY`, env or `.env`):** the >0.999 cross-check gate run (`ecvol prices crosscheck`), and the Tiingo-fallback recovery of the 16 → ≥98% (`ecvol prices pull`). T1.2 stays `[~]` until then.
- **Sources:** stooq.com/q/d/l (PoW wall observed), stooq.com/db/h (bulk archive, `Unauthorized`); yfinance (Yahoo Finance, delisted-symbol purge behavior); Tiingo daily prices API (api.tiingo.com/tiingo/daily); `exchange_calendars` XNYS.
- **Next:** user adds the Tiingo key → run `prices crosscheck` + `prices pull` to close T1.2; then T1.4 join audit (now unblocked) and T1.3 target computation.

### 2026-06-16 — T1.2 Tiingo key landed; cross-check run (47/50), gate semantics aligned to DESIGN §5.2 + artifact persistence
- **Done:** User supplied a free `TIINGO_API_KEY` (in `.env`, gitignored — verified loadable + HTTP 200 on a live AAPL request). First `ecvol prices crosscheck` run on the seeded 5% sample (50 tickers): **47 pass (>0.999), min corr 0.9756**. Hardened the cross-check to match the design contract: (1) `cross_check` now takes a `documented` exceptions set and the gate passes iff every scored ticker is `pass`/`warn`/documented-`investigate`; undocumented sub-0.99 tickers fail and are surfaced (`CrossCheckResult.undocumented`). (2) New committed reason-code file `data/coverage/crosscheck_exceptions.csv`. (3) New artifact writer `write_crosscheck_report` → `data/coverage/crosscheck_report.csv` + `crosscheck_summary.json` (was stdout-only). CLI prints the documented note + report path, and on failure names the undocumented tickers + where to document them. 80 tests green (3 new in `test_tiingo`), ruff clean. DECISIONS.md 2026-06-16.
- **Found:** (1) **XRX corr 0.9756 — the one genuine sub-0.99 outlier**, root-caused to the **Xerox→Conduent spin-off + 1-for-4 reverse split (eff. Jan 2017)**: a +37.6% single-day move on 2017-01-03 in our parquet (largest |return| in the series by far) that yfinance and Tiingo adjust differently — an adjustment-methodology divergence, *not* corrupted data. Documented in the exceptions file. (2) **HPQ corr 0.9974 = a `warn`** (0.99–0.999 band), which DESIGN §5.2 tolerates as minor drift; one third sub-gate ticker was lost when a verification rerun hit the free-tier **50-req/hr** limit (the crosscheck was stdout-only — the trigger for adding the persisted artifact). (3) The original code's gate (`all corr>0.999`) was **stricter than DESIGN §5.2**, which explicitly grants an "investigated and documented" path for sub-0.99 tickers — so the gate could effectively never pass on a realistic sample. (4) Free-tier limits: 50 req/hr (one call per ticker), 1000/day, 500 unique symbols/month — the 5% sample (~50) fits, but back-to-back runs exhaust the hourly window.
- **Sources:** Tiingo daily prices API (api.tiingo.com/tiingo/daily; free-tier 50/hr·1000/day·500 symbols/mo); Xerox/Conduent 2017 spin-off + reverse split (XRX corporate action); DESIGN §5.2.
- **Next (rate-limit blocked ~1 h):** rerun `ecvol prices crosscheck` once the hourly window resets — deterministic seed=0 sample re-hits XRX/HPQ/+1, now writing the artifact and clearing the gate via the XRX exception (resolve the 3rd ticker then: doc it if <0.99, ignore if warn) — then `ecvol prices pull` to recover the 16 delisted tickers via Tiingo fallback (→ ≥98% FinCall coverage). Both close T1.2; push + user verifies CI.

### 2026-06-17 — T1.2 cross-check gate PASSED (artifact committed); Tiingo fallback scoped to FinCall
- **Done:** (1) Reviewed the `pull` path before running and caught that the Tiingo fallback would attempt **all 310 still-missing tickers** (16 FinCall + 307 MAEC) — over the 50/hr cap and the 500-symbol/month budget, with the fallback's bare `except: continue` silently conflating 404 vs 429. **Scoped the fallback to `in_fincall`** (16 tickers); MAEC Tiingo recovery deferred to T1.5 (DECISIONS 2026-06-17; test `test_tiingo_fallback_scoped_to_fincall_only`). (2) Reran `ecvol prices crosscheck`: all 50 requests completed (no rate-limit abort at exactly-50), **gate PASSED** — 47 pass, 2 warn, 1 documented investigate, min corr 0.975641, `undocumented: []`. Persisted artifacts now committed: `crosscheck_report.csv` (50 rows) + `crosscheck_summary.json`. 81 tests green, ruff clean. Commits f072d03 (gate alignment), 3707cf1 (fallback scoping).
- **Found:** The third sub-0.999 ticker (lost yesterday to the rate-limited rerun) is **APD = 0.9941, a `warn`** — within the 0.99–0.999 tolerance band, no action needed (DESIGN §5.2 only requires investigating <0.99). So the full sub-0.999 set on the seeded sample is {APD 0.9941 warn, HPQ 0.9974 warn, XRX 0.9756 documented} — the cross-check gate is now reproducibly green. Cross-check was run **before** `pull` deliberately: the 999 cached tickers are all yfinance-sourced (the correct population for yfinance-vs-Tiingo drift detection), and running it post-pull would both sample meaningless Tiingo-sourced recoveries and push the 5% sample to 51 > 50/hr.
- **Sources:** Tiingo daily prices API (free-tier 50/hr·1000/day·500 symbols/mo); DESIGN §5.2.
- **Next:** after the hourly window resets, run `ecvol prices pull` (16 FinCall-scoped Tiingo calls) to recover the delisted tickers → ≥98% FinCall coverage, then commit the refreshed coverage/manifest artifacts to close T1.2. Push + user verifies CI. Then T1.4 join audit (unblocked) and T1.3 targets.

### 2026-06-17 — T1.2 CLOSED: Tiingo fallback recovered 12/16 → FinCall 98.97%
- **Done:** Ran `ecvol prices pull` (next rate-limit window). The FinCall-scoped Tiingo fallback (16 calls) recovered **12** Yahoo-purged tickers — ANSS, CMA, CTLT, DFS, HES, HOLX, IPG, JWN, MOR, SEE, SNV, WBA — lifting **FinCall coverage 372→384/388 = 98.97%** (clears the ≥98% gate). Manifest now 1,011 entries (12 tiingo-sourced); `ecvol data verify data/manifests/prices.json` → OK. Committed refreshed `prices_coverage.csv`, `prices_sources.json`, `prices.json`. **T1.2 marked `[x]`** — all three acceptance criteria met (≥98% coverage, cross-check >0.999 with documented exception, zero silent drops). 81 tests green.
- **Found:** (1) **4 FinCall tickers remain absent from *both* sources** — ANDG, K, NPIXY, X — each reason-coded in the coverage report (no silent drops). X = US Steel (acquired by Nippon Steel, delisted 2025); the others are obscure/renamed symbols neither Yahoo nor Tiingo serves over 2014–2022. K (Kellanova) trading today but no data returned for the symbol in-range — a Tiingo coverage gap; 98.97% clears the gate so not chased further (would be scope creep beyond the acceptance test). (2) yfinance also surfaced ~10 MAEC delisted-symbol failures (WWE, XLNX, WRI, ZEUS, WYND, WRK, XL, WTR, X, WPX) — expected, MAEC recovery is T1.5's job. (3) `ecvol data verify` with no args hashes **all** manifests incl. ~57 GB of FinCall mp3s — always pass the specific manifest (`data/manifests/prices.json`) for a price-only check.
- **Sources:** Tiingo daily prices API (free tier); yfinance delisted-symbol behavior; US Steel/Nippon Steel 2025 acquisition (X delisting).
- **Next:** push the four T1.2 commits (f072d03, 3707cf1, f7eaa9d, + this artifact commit) and verify CI green. Then **T1.4 join audit** (now unblocked — prices exist for the resolved FinCall universe) and **T1.3 target computation**.

### 2026-06-17 — T1.3 target computation done (join rate 97.12%)
- **Done:** Built `src/ecvol/data/targets.py` + `ecvol targets build` (replaces the CLI stub with a `targets` sub-app). Pure/deterministic/network-free; reuses `calendar.py` and a new `prices.load_close_series` helper. Computes per (call, τ∈{3,7,15,30}): `v_pre`, `v_post`, `Δv` = log realized vol over the τ-return windows around day 0 (DESIGN §5.3, re-derived not ported), plus HAR-RV inputs (realized variance over last 1/5/22 sessions as of day 0). One row per (call,τ) with a `status`/`reason` — zero silent drops. Artifacts: `data/targets/targets.parquet` (gitignored payload, byte-identical re-run verified via SHA) + committed `data/manifests/targets.json` (`ecvol data verify` OK) + `data/coverage/targets_report.csv`. 11 new tests (analytic RV via zero-mean closed form; after-hours boundaries incl. weekend/holiday + the <09:30 / ≥16:00 timestamp branches; exclusion reason codes; HAR inputs; determinism; **3 hand-verified end-to-end calls**). 92 tests green, ruff clean. DECISIONS 2026-06-17.
- **Found:** (1) **First build: 2,496/2,688 calls resolved, join rate 97.12%** (2,424 calls with ≥1 target), 9,696/10,752 rows ok. Exclusions all reason-coded: 768 `unresolved_ticker` (192 calls), 220 `invalid_date` (55 resolved calls with missing/garbled dates), 60 `no_price_data` (15 calls on the 4 delisted tickers absent from both price sources), 4 `insufficient_post_history`, 4 `zero_variance_post`. Per-horizon ok is identical (2,424) across 3/7/15/30 — the generous 2014–2022 price window covers even the 30-session post-windows for every datable in-universe call. (2) **Call times-of-day are unavailable** (identity CSV has dates only; transcript prose has time markers but extraction is unbuilt T1.4 work) → adopted DESIGN §10 risk #7's **assume-after-hours fallback** (`assumed_after_hours` flag on every row; `anchor_day0` parameterized to accept a real `timestamp` later → rerun + sensitivity check, no rework). (3) **`realized_vol` subtlety:** equal *nonzero* returns produce tiny float-noise variance (not exactly 0), so the zero-variance guard fires only on genuinely flat *price* windows (returns exactly 0.0) — which is the real-data case; the test was corrected to use a flat window. (4) HAR-*residual* target needs a train-split-only HAR fit → deferred to Phase 2; T1.3 emits only the inputs.
- **Sources:** DESIGN §5.3 (target formulas + after-hours rule), §5.4 (information rule), §10 risk #7 (missing-timestamp fallback); legacy notebooks 3/3b cross-checked (same RV formula; not ported, per OLDWORK.md §7); Corsi HAR-RV [R17].
- **Next:** **T1.4 join audit** is now fully unblocked (prices ✓ + targets ✓) — wire the earnings-cohort price+target join rate against the ≥95% gate; plus the T1.4 transcript JSON parser + timestamp-extraction subtasks. Push the T1.3 commits + verify CI.

### 2026-06-18 — T1.4 DONE: FinCall ingestion onto the common schema (join 99.31%)
- **Done:** Built `src/ecvol/data/fincall_ingest.py` + `ecvol data ingest fincall` — the last three T1.4 subtasks (transcript JSON parser, timestamp extraction/validation, join audit). Normalizes all 2,688 calls onto the common schema `(call_id, ticker, utc_timestamp→call_date+flags, transcript_json, audio_path, speaker_metadata, source)` plus accounting columns. Transcript parser splits the raw `input` prose on the corpus's coarse inline role markers (`Executives:`→management, `Analysts:`→analyst, `Operator:`→operator, rare attendee/shareholder; text before the first marker → `unknown`) into role-tagged turns; `transcript_json` + `speaker_metadata` are JSON columns. ffprobe audio durations cached under `data/raw/ref/` (resumable). Output: deterministic `data/fincall/calls.parquet` (gitignored payload, byte-identical re-run verified via SHA) + committed `data/manifests/fincall_calls.json` (`ecvol data verify` OK) + committed `data/coverage/fincall_{ingest_report,audio_durations,join_audit}.csv`. 12 new tests (`tests/test_fincall_ingest.py`: parsing incl. glued-marker `...AmericaAnalysts:` and preamble cases, reason codes, audio-injection, schema, determinism, end-to-end join audit). 104 tests green, ruff clean. DECISIONS 2026-06-18.
- **Found:** (1) **All three acceptance gates met:** 100% parse (2,688/2,688 records, zero `empty_transcript`; exclusions reason-coded `unresolved_ticker`=192, `non_earnings`=115, `no_date`=50 — zero silent drops); audio-duration report (2,671/2,671 present mp3s decoded, median 3,693 s ≈ 61 min, mean 3,733 s, range 1,096–11,939 s, **2,769 h total**; 17 mp3s missing, T1.1-known); **earnings-cohort join rate 2,315/2,331 = 99.31%** vs `targets.parquet` (≥95% gate, comfortably clears). (2) **Timestamp investigation closed (§10 risk #7):** measured time-of-day availability — only **3.4%** of transcripts (91/2,688) and **1.6%** of slide PDFs (43/2,674) mention any clock time in their head, and those are mostly press-release issuance times, not the call start. Per-call extraction isn't worth building; uniform **assume-after-hours** fallback applied + flagged (`time_known=False`/`assumed_after_hours=True`), consistent with T1.3. The common-schema `utc_timestamp` is represented as `call_date` + flags rather than a fabricated clock time. (3) **Cohort = parsed + resolved-ticker + dated earnings calls** (2,331); audio availability is deliberately *not* a cohort gate (text-only models still use the 17 audio-less calls) and is reported separately. (4) **Coarse speaker delineation confirmed** (JOURNAL 2026-06-14): many calls collapse to a few role-blocks (e.g. one `Executives:` turn for all prepared remarks) — fine for T1.4's structural parse; true per-speaker turns / sectioning are T3.1. (5) Console gotcha: `≥` is not cp1252-encodable in the PowerShell console — switched the new CLI echo to ASCII `>=`.
- **Sources:** DESIGN §5.3 (after-hours rule), §10 risk #7 (missing-timestamp fallback); T1.3 targets artifact (join denominator); ffprobe (FFmpeg) for durations; corpus measurement (this session).
- **Next:** **T1.5 MAEC ingestion** onto the same common schema (folder-format parser `YYYYMMDD_TICKER`, audio-feature-vs-raw audit, schema mapping, ≥95% price join — including the deferred MAEC Tiingo price recovery, DECISIONS 2026-06-17), then **T1.6 split builder**. Push the T1.4 commit + user verifies CI green.

### 2026-06-18 — T1.5 DONE: MAEC ingestion onto common schema (price-join 75.4%, documented shortfall)
- **Done:** Built `src/ecvol/data/maec_ingest.py` + `ecvol data ingest maec`. Extracted the common-schema `CallRecord` + parquet writer into shared `src/ecvol/data/calls.py` (now used by both FinCall and MAEC ingesters), and generalized `targets.py` to be call_id-type-agnostic (int() coercion moved into FinCall's `_read_identity`) so MAEC reuses the exact FinCall RV/target math. **FinCall's committed `calls.parquet` + `targets.parquet` re-verified byte-identical after the refactor.** MAEC mapping: folder `YYYYMMDD_TICKER` → date+ticker (no identity reconstruction needed); `text.txt` sentences → `unknown`-role turns (MAEC has no speaker labels); `features.csv` per-sentence acoustics → audio-feature audit + summed `Audio Length` as `audio_duration_sec`; no raw audio (`audio_path=""`); no call times → same assume-after-hours fallback. MAEC targets computed and written to `data/maec/targets.parquet` (string call_id). Artifacts: `data/maec/{calls,targets}.parquet` (gitignored, byte-identical re-run verified) + committed manifests `maec_{calls,targets}.json` + `data/coverage/maec_{ingest_report,audio_features,join_audit,missing_tickers}.csv`. 7 new tests (`tests/test_maec_ingest.py`: folder/sentence/features parsing, schema+status, end-to-end join with a price fixture, determinism, full accounting). 111 tests green, ruff clean. DECISIONS 2026-06-18.
- **Found:** (1) **3,443 folders (= published MAEC count), 3,419 parsed.** The 24 exclusions are genuinely truncated `text.txt` files (just "Thank you."/"Thanks a lot.", 11–14 chars) — a real MAEC data defect, reason-coded `empty_transcript` (not a parser bug; spot-verified). 394,249 sentences total (mean 115/call); features.csv present for all 3,443; **raw audio 0** (MAEC never shipped it). (2) **Price-join 2,578/3,419 = 75.4%** — below the ≥95% gate. 296 of 1,213 tickers have no price data, all reason-coded in `maec_missing_tickers.csv` (zero silent drops); they're predominantly genuine 2015–2018 S&P-1500 M&A/delistings yfinance purges (ABC→Cencora, AGN, ANTM→Elevance, APC, ALXN, ABMD, AKS…). (3) **Gate closed via the documented-shortfall path** (user decision): MAEC is the secondary dataset (DESIGN §5.1); a Tiingo recovery burst would cost ~6 h (free tier 50/hr) + ~296 of the 500/month symbol budget and — since the misses are mostly true delistings — realistically still land ~88–93% (<95%), so it's deferred as optional future work, not run now. (4) The `audio_duration_sec` from summed sentence `Audio Length` gives median ~13.4 min / call (917 h total) — notably shorter than FinCall's ~61 min, consistent with MAEC features covering a subset (Q&A) of each call; reported honestly in the audit with its source noted. (5) Refactor payoff: the common schema now lives in one module, and `targets.py` serves both datasets unchanged — the next dataset (Phase 7 fresh calls) plugs into the same machinery.
- **Sources:** DESIGN §5.1 (MAEC role + ~3,443 count), §11 row 1 (link-rot/delisting risk); DECISIONS 2026-06-12 (MAEC no raw audio / MFCC archive link-rot), 2026-06-17 (MAEC Tiingo recovery deferred to T1.5); yfinance delisted-symbol purge behavior.
- **Next:** **T1.6 split builder** (`ecvol splits build`) — temporal (30-trading-day embargo), ticker-disjoint, combined; leakage-assertion test module that runs in CI forever. Both datasets now share the common schema + targets, so splits can be built per-dataset uniformly. Push the T1.5 commit + user verifies CI green.

### 2026-06-18 — T1.6 DONE: leakage-proof split builder (completes Phase 1)
- **Done:** Built `src/ecvol/data/splits.py` + `ecvol splits build` — three split schemes per dataset over the ≥1-ok-target cohort, written as committed CSVs `data/splits/{dataset}_{scheme}.csv` (`.gitignore` allowlists `data/splits/`). **temporal:** order by `as_of` session index (via `calendar.py`), boundaries at the train/train+val call-count quantiles, with a `max(embargo,horizon)`-session embargo zone on the train side of each boundary (train `idx≤b1−gap`, val `b1<idx≤b2−gap`, test `idx>b2`, else reason-coded `embargo`) → no 30-day target window crosses a boundary, ≥30-session gap between segments. **ticker_disjoint:** seeded (default 0) greedy ticker partition ≈70/10/20 by call count (sector stratification skipped — no sector metadata). **combined:** keep a call only where both schemes agree, else `excluded`. Also did a consistency refactor: **moved FinCall targets `data/targets/targets.parquet` → `data/fincall/targets.parquet`** (+ manifest `fincall_targets.json`, removed old `targets.json`) so the layout is symmetric with MAEC — content byte-identical, `fincall_ingest` join audit reads the new path, splits use `data/{dataset}/targets.parquet` uniformly. 11 new tests (`tests/test_splits.py`), incl. assertions on the **committed real CSVs**. 123 tests green, ruff clean. DECISIONS 2026-06-18.
- **Found:** (1) **Split sizes** — FinCall (cohort 2,424): temporal 1480/228/481 (+235 embargo); ticker_disjoint 1701/239/484; combined 1029/21/92 (+1282 excluded). MAEC (cohort 2,578): temporal 1604/73/507 (+394 embargo); ticker_disjoint 1805/259/514; combined 1096/4/102 (+1376 excluded). (2) **The embargo shrinks val on clustered data** — a 10%-by-count val band narrower than the 30-session embargo collapses; FinCall temporal val=228 is healthy but MAEC val=73 is small (MAEC's calls cluster more tightly in calendar time). Documented, not a bug — DESIGN says "approximate proportions"; FinCall is primary and model selection can use temporal or ticker-disjoint val. The combined splits are small by construction (the double constraint) — the "hardest robustness row" (DESIGN §5.4.3), reported as-is. (3) **The path bug that surfaced the refactor:** first `splits build` silently built MAEC only — FinCall targets were at the legacy `data/targets/` path while my new code looked under `data/fincall/`; rather than special-case it I standardized the location (the asymmetry would have bitten every Phase-2 consumer). (4) **CI-guarding the committed artifacts:** the strongest leakage test reads the committed `data/splits/*.csv` directly (no gitignored parquets needed in CI) and re-derives the embargo gap from the trading calendar — so the actual shipped splits, not just the algorithm, are asserted leakage-proof forever. (5) Two boundary-formulation dead-ends before landing the right one: embargoing both sides of val needs a >2×embargo val band (kills val); the chosen single-train-side-embargo needs only middle-span > embargo.
- **Sources:** DESIGN §5.4 (split design: temporal embargo, ticker-disjoint, combined, committed CSVs + CI leakage assertions); `exchange_calendars` XNYS via `calendar.py`; López de Prado embargo/purging concept (the 30-day-target leakage DESIGN §5.4.1 calls out).
- **Next:** **Phase 1 is complete (T1.1–T1.6 all done).** Phase 2 — eval harness + econometric baselines: **T2.1 metrics & significance** (`eval/metrics.py`: MSE/MAE/R²_OOS/quarterly Spearman; `eval/significance.py`: Diebold-Mariano, cluster bootstrap, Holm), then T2.2 Stage-0/1 baselines → Result Table 1, T2.3 reporting. Push the T1.6 commit + user verifies CI green.

### 2026-06-18 — T2.1 DONE: metrics & significance module (Phase 2 starts)
- **Done:** Built `src/ecvol/eval/metrics.py` (DESIGN §7.1) and `eval/significance.py` (§7.2). Added **scipy** as a dependency (`uv add scipy`, lockfile committed) for the Student-t survival function (DM p-values) and Spearman. `metrics.py`: pure array `mse`/`mae`/`r2_oos`/`spearman` (NaN rows dropped) + the (call,τ) prediction-frame helpers `quarter_of`, `spearman_by_quarter` (equal-weight mean over quarters), `metrics_by_horizon`. `significance.py`: `diebold_mariano` (HLN small-sample correction vs `t_{n-1}`, LRV = γ0+2·Σ_{k=1}^{h-1}γ_k, sign +ve⇒model A worse), `cluster_bootstrap_ci` (resample whole clusters — ticker/quarter — percentile CI, seeded), `holm_correction` (step-down, order-preserving). No CLI verb — these are libraries `report` (T2.3) and the §7.5 confirmatory comparisons consume. 18 new tests; 141 green, ruff clean. DECISIONS 2026-06-18.
- **Found:** (1) **DM validated against a published reference via an exact identity:** the HLN-corrected DM at h=1 *equals* the paired t-test on the loss differential — proved algebraically (DM·√((n-1)/n) with population γ0 collapses to d̄/(s/√n)) and asserted to 1e-9 against `scipy.stats.ttest_rel`. This is a stronger check than a hand-worked example and sidesteps statsmodels (which has no built-in DM). (2) **Cluster bootstrap validated on a known sampling distribution:** i.i.d. data (each obs its own cluster) reproduces the analytic normal-CI half-width (≈1.96·SE within 15%); 20 correlated clusters of 50 widen the CI >3× vs. ignoring clusters — the exact i.i.d.-CI understatement DESIGN §7.2 warns about. (3) **A test-design bug of my own:** first asserted the bootstrap CI contains the *population* mean — but a percentile CI brackets the *sample* mean, and here the sample mean fell ~1 SE low, so the true mean was legitimately outside; corrected to assert `lo<point<hi` + point within a few SE. Good reminder that CI tests must respect that the interval is centered on the estimate, not the truth. (4) **R²_OOS baseline is caller-supplied** (`y_persistence`) so the same function serves level-v (baseline `v_pre`) and Δv (baseline 0) — the headline metric stays target-agnostic. (5) Holm matches R's `p.adjust(method="holm")` worked example exactly (0.01,0.04,0.03,0.005 → 0.03,0.06,0.06,0.02).
- **Sources:** DESIGN §7.1 (metrics: MSE/MAE/R²_OOS/quarterly Spearman), §7.2 (DM, cluster bootstrap, Holm), §7.5 (confirmatory comparisons); [R26] Gu–Kelly–Xiu (R²_OOS convention); [R27] Diebold–Mariano 1995; Harvey–Leybourne–Newbold 1997 (small-sample DM correction); R `stats::p.adjust` (Holm reference).
- **Next:** **T2.2 Stage-0/1 baselines → Result Table 1** — persistence, EWMA, HAR-RV, GARCH(1,1), ticker-FE LightGBM over every (dataset×split×target×τ); sanity gate (HAR-RV beats persistence at τ=30 temporal, else halt+debug targets). Needs `models/baselines.py` + `models/gbdt.py` + the first end-to-end `ecvol evaluate` (which consumes this metrics/significance API). Likely adds `arch`/`lightgbm` deps. Push T2.1 commit + user verifies CI green.

### 2026-06-18 — T2.2 DONE: Stage-0/1 baselines → Result Table 1 (sanity gate passes w/ documented COVID exception)
- **Done:** Built the honest floor. `models/baselines.py` (persistence, EWMA RiskMetrics λ=0.94, Corsi **log-HAR** train-fit, **GARCH(1,1)** via `arch` fit once per call on returns ≤ day0), `models/gbdt.py` (LightGBM with a **ticker fixed effect**, fully deterministic), `eval/evaluate.py` + `ecvol evaluate`. Added deps `arch`/`lightgbm`/`scikit-learn` (`statsmodels` transitively). Computes all three targets (level-v, Δv, **HAR-residual** — the train-only HAR fit deferred from T1.3 lands here); persistence is the per-target trivial forecast and the R²_OOS baseline (so persistence R²_OOS≡0, a built-in check). Per-cell DM p-values via the T2.1 significance API. Output: committed deterministic `data/results/result_table_1.csv` (720 rows; `.gitignore` allowlists `data/results/`). 14 new tests; 155 green, ruff clean.
- **Found:** (1) **GARCH-convergence gate PASSED:** FinCall 99.6%, MAEC 99.9% (>95%). (2) **The sanity gate failed literally and I debugged it — targets are sound; it's the COVID regime shift.** HAR-RV beats persistence at τ=30 temporal on MAEC (R²_OOS +0.206) but LOSES on FinCall (−0.287). Investigation ruled out a target bug: the *same* HAR/targets beat persistence at τ=30 on FinCall **ticker-disjoint** (+0.229), on **MAEC temporal** (+0.206), and at **τ=3/7/15 on every FinCall split** — a target defect would fail those too. Root cause: FinCall's temporal train (2017-11–2021-01) is **18% Feb–May-2020 COVID-window calls** (mean v_post −3.41 vs −3.95 overall); the test segment is the calm late-2021 regime (−4.17, **0.22 lower**). HAR fit on COVID-inflated train over-predicts the level at the long horizon; persistence uses each call's own v_pre (mean −4.16 ≈ test) and tracks the regime. At τ≤15 the post-window is near day-0 so the gap is small and HAR wins; ticker-disjoint mixes dates so no shift. (3) **Resolution:** per the charter (fix-the-gate-via-DECISIONS), the gate now passes on the primary temporal split OR via a **documented COVID-regime exception granted only when the regime-stable cells corroborate sound targets** — not a blanket pass (unit-tested: literal-pass / regime-exception / genuine-failure). The τ=30-temporal under-performance vs persistence is itself a paper finding (long-horizon level forecasting across a regime shift is hard; persistence is a strong floor). (4) On the **Δv** target all models beat the zero baseline (e.g. FinCall temporal test τ=7: HAR R²_OOS +0.41, EWMA +0.36, GBDT +0.25, GARCH +0.19, DM p<0.001) — the level-removing target behaves as designed. (5) The ticker-FE GBDT mis-generalizes hardest across the regime shift (τ=30 temporal R²_OOS −0.84) — expected for the identity+metadata baseline; reported. (6) Engineering: LightGBM's sklearn API needs scikit-learn (added; needed anyway for Phase-2/3 heads + §7.3 probe); a stale `evaluate` in test_cli's STUB_VERBS was invoking the *real* full pipeline during the CLI test (105s, then assert-failed) — removed, suite back to ~7s. ~0.4% non-converged GARCH fits emit benign SLSQP stderr; those fits are dropped via the convergence flag.
- **Sources:** DESIGN §6 (modeling ladder Stages 0-1), §7.1-7.2 (metrics/significance, consumed via T2.1), §5.4.5 (COVID regime caveat); Corsi 2009 HAR-RV [R17]; RiskMetrics EWMA (λ=0.94); `arch` GARCH(1,1); Gu–Kelly–Xiu R²_OOS [R26]; Diebold–Mariano [R27].
- **Next:** **T2.3 reporting** — `eval/report.py` + `ecvol report` renders Result Table 1 to LaTeX + Markdown from `data/results/result_table_1.csv`, with byte-identical regeneration (CI check) and cluster-bootstrap CIs (T2.1 API). Then Phase 2 is complete → Phase 3 text ladder. Push T2.2 commit + user verifies CI green.

### 2026-06-18 — T2.3 DONE: reporting (`ecvol report`) — completes Phase 2
- **Done:** Built `eval/report.py` + `ecvol report` → committed `data/results/result_table_1.{md,tex}` rendered from the committed `result_table_1.csv`. Table specs are **data** (`TABLE_1_SPECS`: a list of frozen `TableSpec(dataset,split,target,metric,segment)`); each pivots model × horizon. Render set = R²_OOS + MSE for {fincall,maec} × {level-v,Δv} × {temporal,ticker_disjoint}, test (16 tables). Cells carry `*` when DM-significant vs persistence; NaN → em dash; LaTeX uses booktabs. Added `notebooks/figures_result_table_1.py` (figure stub). 6 new tests; 161 green, ruff clean. DECISIONS 2026-06-18.
- **Found:** (1) **Byte-identical regeneration enforced two ways:** a render-twice determinism test, and a **committed-artifacts CI guard** (`test_committed_reports_match_fresh_render`) that re-renders from the committed CSV and asserts equality with the committed `.md`/`.tex` — so the shipped tables can't silently drift from the data. (2) The rendered R²_OOS table makes the T2.2 COVID-regime story legible at a glance: on FinCall **temporal**, HAR/EWMA/GBDT are positive (beat persistence) at τ≤15 but go negative at τ=30; on FinCall **ticker-disjoint** they stay positive at τ=30 — the regime-shift signature, now a paper-ready table. (3) **Cluster-bootstrap CIs deferred** — they need per-call predictions the aggregated table doesn't carry; Table 1's significance is already conveyed by the DM `*` markers + GBDT seed-std, and a model-vs-model CI is the actual claim only once content models exist (Phase 3+), so I'll persist per-call predictions then. (4) Stub-verb housekeeping: `report` (and earlier `evaluate`) left `STUB_VERBS` in `test_cli` as they became real commands. (5) `_tex_escape` handles the unicode in titles (Δ→$\Delta$, τ→$\tau$, ²→\textsuperscript{2}, `_`→\_).
- **Sources:** DESIGN §7.1 (R²_OOS headline + MSE comparability), §7.2 (DM markers; bootstrap CIs deferred), §7.5 (DM-significance annotation); booktabs LaTeX convention.
- **Next:** **Phase 2 complete (T2.1–T2.3).** The eval floor — metrics, significance, baselines, Result Table 1, reporting — is in place and CI-guarded. **Phase 3 — text ladder + early identity controls:** T3.1 transcript normalization (sections + speaker-turn chunking, reusing the FinCall/MAEC `transcript_json`), T3.2 frozen text features (BGE/GTE embeddings, FinBERT, surface stats — GPU), T3.3 Stage-2 results → Result Table 2, T3.4 identity-control suite. Push T2.3 commit + user verifies CI green.

### 2026-06-18 — Workflow: near-autonomous task loop (LOOP.md + guardrail hooks)
- **Done:** Set up a near-autonomous backlog loop so the user is no longer in the loop on every task. New files: **`LOOP.md`** (the per-iteration protocol — orient → select unblocked task → design-call check → implement → local gate → local commit + docs → blocker-skip/queue → phase-boundary pause), **`HANDOFF.md`** (append-only human-pending queue, seeded with anticipated blockers), **`.claude/settings.json`** + **`.claude/hooks/guardrail.ps1`** (committed guardrails), and `.gitignore` now excludes `/.claude/settings.local.json`. Updated CLAUDE.md (doc-map rows for LOOP.md/HANDOFF.md + an "Autonomous loop" workflow bullet) and logged the workflow change in DECISIONS 2026-06-18. The four governing decisions came from the user this session (AskUserQuestion): gate = **phase-boundary CI checkpoint** (local `ruff`+`pytest` per task is a faithful CI mirror, so commit locally and keep going; user pushes + confirms CI at phase boundaries); scope = **run until the next human-blocker** (skip `[!]` tasks); design calls = **stop and ask** (never guess a research/design choice); notifications on **blockers + phase-complete + every DECISIONS entry**.
- **Found:** (1) **CI is an exact local mirror** — `.github/workflows/ci.yml` runs only `uv run ruff check .`, `ruff format --check .`, `pytest -q` on locked Py3.12; this is what makes "commit on local-green without waiting" safe rather than reckless, with phase-boundary checkpoints catching any ubuntu-vs-Windows surprise. (2) **Driver = self-paced `/loop` + `ScheduleWakeup`, not a Stop hook** — a Stop-hook continuation loop caps at 8 consecutive blocks and fights `/loop` (claude-code-guide); hooks are reserved for guardrails. (3) **Guardrail scope kept tight on purpose:** `PreToolUse` denies only `git push` (user owns pushes/CI) and `legacy/` writes (read-only) — high-signal, zero false-positive — rather than a brittle "spend/credential" command deny-list; the key/spend/ToS/labeling hard-stops are enforced by the LOOP.md protocol + surfaced in HANDOFF.md (and most aren't runnable non-interactively anyway). `SessionStart` re-injects open `[!]` tasks + a LOOP.md pointer so the task thread survives auto-compaction. (4) Honest expectation set with the user: with "stop on any design call," the near-term payoff is removing per-task push/CI/handoff latency and running the mechanical work + controls plumbing autonomously — not unattended sprints through modeling (T3.1/T3.2 surface design calls early). (5) `.claude/` existed but was empty; `.gitignore` already reserved `/.claude/scheduled_tasks.lock`, so settings.json + hooks/ are net-new committed config.
- **Sources:** Claude Code hooks (PreToolUse deny / SessionStart additionalContext), `/loop` self-paced mode + `ScheduleWakeup`, `PushNotification` (via claude-code-guide agent); CLAUDE.md (user owns push/CI, legacy read-only); `.github/workflows/ci.yml`.
- **Next:** Begin the loop with `/loop follow LOOP.md`. First backlog item is **T3.1 transcript normalization** — but it carries design calls (section-detection heuristics, speaker-turn chunking strategy) the loop will surface for the user before implementing. No human blockers active right now (HANDOFF.md "Active" is empty).

### 2026-06-19 — T3.1 transcript normalization (sectioning + speaker-turn chunking) — impl done, audit pending
- **Done:** First loop task. Surfaced the design call; user picked all three recommended options (deterministic heuristic / best-effort MAEC / sentence-split oversized chunks — AskUserQuestion). Built `features/text/sections.py` + `ecvol featurize sections` (the `featurize` stub is now a sub-app). Output: `data/{dataset}/chunks.parquet` (gitignored deterministic payload + manifest `{dataset}_chunks.json`) + committed `data/coverage/{dataset}_{sections,section_audit}.csv`. Boundary = operator Q&A-cue turn, else first analyst turn (FinCall), with an intro-mention guard (cue must be within 3 turns of first analyst); MAEC = in-text cues only, roles unavailable. Chunk-per-turn, never across turns; oversized turns sentence-split into same-turn sub-chunks (`max_words=320`, under the BGE/GTE 512-token window). 13 new tests; 174 green, ruff clean; byte-identical reruns + `ecvol data verify` OK. DECISIONS 2026-06-19.
- **Found:** **FinCall** Q&A detected 2654/2688 (98.7%); 2408 corroborated by *both* operator-cue and first-analyst (90.7% of detections); methods first_analyst=2151 / operator_cue=443 / text_cue=60 (calls whose coarse role markers didn't parse) / none=34; 196,233 chunks, 0 oversize. **MAEC** (no speaker labels) Q&A detected only 867/3443 (25.2%) — most MAEC sentence text carries no explicit operator cue — 394,280 chunks, 1 oversize; corroboration impossible without roles. Eyeballed the 30-call FinCall audit: boundaries land on genuine analyst questions / operator "first question from..." intros; the only soft cases are out-of-cohort non-earnings calls (a JPMorgan Tech Forum, a Goodyear annual meeting — both no-ticker, excluded from the modeling cohort). The automated proxy (90.7% corroborated, clean eyeball) strongly suggests >90%, but the formal acceptance number is a human check.
- **Sources:** DESIGN §6 (Stage-2 text; section-pooling, no per-sentence alignment), §6 Stage-2 row (BGE/GTE 512-token context → chunk cap); DECISIONS 2026-06-14 (speaker-turn chunking lesson), 2026-06-19 (design-call resolution); LOOP.md protocol.
- **Next:** **Loop paused on a human blocker.** (1) User: verify `data/coverage/fincall_section_audit.csv` (and `maec_...`) — is the prepared/Q&A boundary right on ≥90%? Mark `correct_y_n`; reply to close T3.1 (`[x]`). (2) Then **T3.2 frozen text features** is the next task and a **design call** (embedding model BGE-large vs GTE + revision pin, FinBERT variant, pooling strategy) **+ GPU** — the loop will stop and ask before extracting. Push T3.1 commit + (with the loop-infra commit) confirm CI green at this Phase-3-start checkpoint.

### 2026-06-19 — T3.1 audit artifact refined (cohort sampling + context columns)
- **Done:** On user request, made the section audit easier and fairer. The 30-call sample now draws from the **earnings cohort only** (≥1 ok target, via the same definition as `splits.load_cohort`), dropping the out-of-cohort non-earnings calls that had slipped into the FinCall sample. Added `prev_text`/`boundary_text`/`next_text` snippet columns so each row reads operator-intro → analyst-question → management-answer and is judgable without opening the transcript. Chunks payload + `{dataset}_sections.csv` unchanged (chunks.parquet byte-identical; manifest timestamp churn reverted). 1 new test (cohort restriction + context columns); 175 green, ruff clean. DECISIONS 2026-06-19. User installed the Edit csv + Rainbow CSV VS Code extensions for the grid view.
- **Next:** unchanged — user audits the refreshed CSV (>90% on FinCall) to close T3.1, then the T3.2 design call.

### 2026-06-19 — T3.1 CLOSED: section audit passed 30/30
- **Done:** User completed the hand-audit of the cohort-restricted 30-call FinCall sample → **30/30 correct** (100%, clears the >90% gate). Calibration confirmed: a boundary on the operator's hand-off ("our first question comes from…") counts as correct — it's the start of the Q&A section — as does a boundary on the first analyst question (the two detection methods produce equivalent, valid section splits). T3.1 marked `[x]`; HANDOFF Active cleared. **Phase 3 task 1 of 4 complete.**
- **Found:** The only `n`-leaning rows were operator-handoff boundaries, which are correct by the section-detection definition (not the verbatim-question definition) — no heuristic change needed. Acceptance was section *placement*, not "boundary turn == question text".
- **Next:** **T3.2 frozen text features** — the next task and a design call + first GPU work: embedding model (+ revision pin), FinBERT variant, pooling default, and the torch/transformers/GPU dependency setup. Loop will stop and ask before adding deps/extracting. Push (loop infra + T3.1 commits) at convenience; CI checkpoint formally due at the Phase-3 boundary (after T3.4).

### 2026-06-19 — T3.2 frozen text features: implementation + GPU benchmark (full run pending)
- **Done:** Built the Stage-2 text feature stack. `features/text/_common.py` (chunk loader, content-hash cache `encode_with_cache`, deterministic torch setup, section/scope pooling, deterministic parquet writer), `surface.py` (CPU: len/turns/numeric-density/question-marks per scope), `embeddings.py` (**BAAI/bge-m3** 1024-d, section-pooled, mean or n_words-weighted), `finbert.py` (**ProsusAI/finbert** probs per scope×role + net). CLI `ecvol featurize text` with `--limit` benchmark + ETA. GPU deps in a **`gpu` PEP-735 group** (torch **2.11.0+cu128** for the Blackwell RTX 5060 Ti / sm_120), so `uv sync`/CI stay torch-free; model code imports torch lazily. Design call answered by user (delegated embedding model → BGE-M3; ProsusAI/finbert; fp32+deterministic; benchmark-first). DECISIONS 2026-06-19. 8 new torch-free tests; 182 green, ruff clean.
- **Found:** (1) **GPU works end-to-end** — torch 2.11.0+cu128 drives the 5060 Ti (capability (12,0)=sm_120), CUDA matmul OK; no driver update needed. (2) **Benchmark (50 calls = 3,130 chunks, fp32-deterministic, incl. one-time model load):** BGE-M3 **37 ch/s**, FinBERT **90 ch/s** → conservative full-corpus ETA FinCall (196,233) ~2 h, MAEC (394,280) ~4 h. fp32+deterministic is the throughput cost of bit-identical re-extraction; the content-hash cache makes any re-run free and the full run resumable. (3) A real bug caught by the determinism test: `write_manifest` doesn't create `manifests/` — added the mkdir in all three builds. (4) Windows symlink warning from HF cache is benign (Developer Mode off → more disk, no failure). (5) Kept CI torch-free by lazy-importing torch inside functions — the 8 feature tests exercise only the pure logic (pooling, aggregation, surface stats, cache dedupe/bit-identical), validated without the GPU stack.
- **Sources:** DESIGN §6 (Stage-2: section-pooled embeddings, FinBERT per section/role, surface stats; pooling as config), §3 (GPU throughput benchmarked before full run); mid-2026 MTEB landscape (BGE-M3 MIT workhorse, Qwen3-Embedding tops open MTEB) via web search; model cards BAAI/bge-m3 (MIT), ProsusAI/finbert.
- **Next:** **Loop paused for the benchmark go/no-go** (user chose benchmark-first). On go, run the full extraction (FinCall first — primary, unblocks T3.3; then MAEC) as a resumable background job, then commit the manifests/coverage + close T3.2 `[x]`. Then T3.3 (Stage-2 heads → Result Table 2) and T3.4 (identity controls). Push checkpoint due at the Phase-3 boundary.

### 2026-06-19 — T3.2 CLOSED: full Stage-2 text extraction (both datasets)
- **Done:** User chose "both now (overnight)"; ran the full extraction in one resumable background job (~95 min wall, not the ~6 h conservative estimate). FinCall: `text_embeddings` 7,835 rows / `text_finbert` 28,123 / `text_surface` 7,835 (185,126 chunks encoded, ~11k cache hits from dedup + the 50-call benchmark). MAEC: 7,739 rows each. Manifests verify OK; T3.2 marked `[x]`. Committed the 6 text manifests.
- **Found:** (1) **Acceptance met — re-extraction is bit-identical:** a warm-cache re-run encoded **0 chunks** and reproduced all three FinCall parquets byte-for-byte (content-hash cache + fp32-deterministic). (2) **MAEC ~5× faster per chunk** than FinCall — MAEC chunks are single sentences (short seqs), FinCall chunks are speaker turns up to 320 words; BGE-M3 throughput is sequence-length-bound, so 394k MAEC chunks took less wall time than FinCall's 196k. The 6 h estimate was conservative (linear in chunk count, ignoring MAEC's short sequences + one-time model-load amortization). (3) Dedup by content hash saved ~11k encodes on FinCall. (4) Caches are large (~0.7 GB FinCall, ~1.5 GB MAEC) but gitignored; only the ~61 MB pooled-embedding parquets + manifests are tracked (manifests committed). (5) MAEC finbert has no per-role rows (all turns role `unknown` → only scope-level "all"), as designed.
- **Sources:** DESIGN §6 (Stage-2 frozen features), §3 Stage-2 acceptance (deterministic re-extraction, throughput benchmarked); model cards BAAI/bge-m3, ProsusAI/finbert.
- **Next:** **T3.3 Stage-2 results → Result Table 2** (ridge + shallow-MLP heads on these features, 5 seeds, with/without past-vol covariates; DM vs HAR-RV and vs Stage-1) — a design call (head choices, covariate sets, ablations) the loop will surface. **T3.4 identity controls** (run alongside, per DESIGN "run NOW"). Phase-3 CI checkpoint (push + verify) due after T3.4. Phase-3: T3.1 ✅ T3.2 ✅ → T3.3, T3.4 remain.
