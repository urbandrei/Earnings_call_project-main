# JOURNAL.md — Work Journal

**Append-only, newest entry last.** A chronological record of work performed on the project: what was done, what was found, what broke, what's next. The journal is the project's narrative memory — [TASKS.md](TASKS.md) says *what state* the work is in; this file says *how it got there*.

## What gets an entry

Append an entry at the end of every working session (or completed task, whichever is finer-grained). Include:

- **Date + scope** — what task(s)/topic the session touched (use task IDs where applicable).
- **Done** — work completed, with enough specifics that a future reader can retrace it (commands, files, run IDs).
- **Found** — results, measurements, surprises, dead ends. Negative findings are first-class: an experiment that failed is information.
- **Sources** — papers/links/model cards consulted (anything reusable should also land in DESIGN.md §13).
- **Next** — the intended next step, so any session can pick up where the last one stopped.

Keep entries factual and compact. Decisions that change the design belong in [DECISIONS.md](DECISIONS.md) (link the journal entry to them); the journal records the work and evidence around them.

---

## Log

### 2026-06-12 — Project review, literature research, design doc
- **Done:** Full review of the inherited legacy project (12 notebooks, Paper.pdf, papers/ library); established it is unrunnable (data lost with the `D:` drive, no environment spec). Three parallel web-research sweeps: (1) post-KeFVP SOTA and documented pitfalls, (2) open dataset options, (3) open-source model landscape for consumer GPUs. Wrote DESIGN.md (research questions, data/model/eval design, 9-phase plan, risk register).
- **Found:** Key literature facts that shaped the design — "Same Company, Same Signal" (arXiv 2412.18029): transcript models largely memorize ticker identity and training-free past-vol baselines beat them; financial-NLP reproducibility crisis (~14% of results reproduce, arXiv 2504.07274); FinCall-Surprise (arXiv 2510.03965, Apache-2.0, 2,688 calls 2019–2021 with full audio) is the best truly-open multimodal dataset; legacy +0.73%-vs-KeFVP result is single-seed and not defensible.
- **Sources:** consolidated in DESIGN.md §13 (R1–R27, D1–D8).
- **Next:** docs restructure, then T0.1 scaffolding.

### 2026-06-12 — Docs restructure
- **Done:** Renamed `Earnings_call_project-main/` → `legacy/` (read-only; deleted its stale CLAUDE.md); moved DESIGN.md to project root and removed `new/`; extracted the full task breakdown into TASKS.md (living tracker, 30 tasks T0.1–T8.3); wrote OLDWORK.md (legacy summary, marked outdated); wrote root CLAUDE.md (working principles, automation/testing discipline, exploration policy); moved the decision log out of DESIGN.md into DECISIONS.md; created this journal. Git deliberately not initialized yet — first subtask of T0.1.
- **Found:** Karpathy reference check: he never published a CLAUDE.md template; the viral file is Forrest Chang's distillation of Karpathy's Jan-2026 post on four agent failure modes (silent assumptions, code hypertrophy, collateral changes, no verifiable success criteria) — CLAUDE.md's principles built on those primary sources.
- **Next:** T0.1 — `git init`, .gitignore, initial commit, package skeleton, CI.

### 2026-06-12 — T0.1 package skeleton
- **Done:** Installed uv 0.11.21 (official installer → `~\.local\bin`). `git init -b main`; `.gitignore` (glob-based: `data/` except `data/manifests/`, `artifacts/`, caches, `.env`, legacy bulk binaries — see DECISIONS.md entry) + `.gitattributes` (LF normalization). Eight small commits: governance docs → repo config → legacy reference files → package skeleton → pre-commit → CI workflow → README → doc updates. Package: `pyproject.toml` (uv_build backend, Python pinned 3.12 via `.python-version`, lockfile committed), `src/ecvol/` subpackage layout per DESIGN.md §8.1 (config/data/features{text,audio,llm}/models/eval), Typer CLI with all seven verbs (`prices|targets|splits|featurize|train|evaluate|report`) as stubs exiting code 2, smoke tests (help lists every verb; stubs fail loudly). Ruff (E,F,I,UP,B; line 100; `legacy/` excluded) + pre-commit (ruff-check --fix, ruff-format) installed and green. CI workflow `.github/workflows/ci.yml`: `uv sync --locked` → ruff check → format check → pytest. Fresh-machine install tested once: cloned repo to `%TEMP%`, `uv sync --locked` + tests green from the lockfile alone; steps documented in README.md.
- **Found:** Python 3.14.4 is the system default — pinned 3.12 instead for ML-wheel compatibility (torch et al. lag new CPython releases). Ruff initially linted the legacy notebooks (395 KB of findings) → `extend-exclude = ["legacy"]`, consistent with their read-only status. PowerShell 5.1 `Set-Content -Encoding utf8` writes BOMs — generated files rewritten BOM-free.
- **Sources:** uv install/sync docs (docs.astral.sh/uv), astral-sh/ruff-pre-commit, astral-sh/setup-uv action.
- **Next:** User: create the GitHub repo and push (needs `gh auth login` or manual repo creation) to verify Actions green — the one open T0.1 acceptance item. Then T0.2 config system.

### 2026-06-12 — T0.1 closed
- **Done:** User created the GitHub repo (`urbandrei/Earnings_call_project-main`, private) and pushed; CI run on `main` green on first try (user-verified in the Actions tab — repo is private, so no API access from the agent without `gh`). T0.1 acceptance fully met → `[x]`.
- **Found:** Verifying Actions on a private repo from this machine needs an authenticated `gh`; worth installing (`winget install GitHub.cli` + `gh auth login`) before CI-dependent phases.
- **Next:** T0.2 config system (pydantic schemas, example YAML, config-hash function).

### 2026-06-12 — T0.2 config system
- **Done:** Added pydantic 2.13.4 + PyYAML 6.0.3 (`uv add`, lockfile committed). `src/ecvol/config/`: `schema.py` — `ExperimentConfig` with `DataConfig` (dataset literal), `TargetConfig` (variant level/delta/har_residual; horizons restricted to {3,7,15,30} per DESIGN.md §5.3), `SplitConfig` (temporal/ticker_disjoint/combined + embargo), `FeatureSpec` list (kind/extractor/model_id/revision/options — minimal now, extended when Phases 3–6 land), `ModelConfig` (name + `include_past_vol_covariates` for the §6 invariant + params dict), `EvalConfig` (metrics, bootstrap resamples); `extra="forbid"` on every model so typos fail loudly. `load.py` — `load_config` (ConfigError with file path + dotted field locations), `dump_config` (deterministic sorted YAML), `config_hash` (canonical JSON, sorted keys, compact separators → SHA-256). `configs/example.yaml` (5-seed GBDT on FinCall-Surprise temporal split). 11 tests in `tests/test_config.py` cover both acceptance cases plus round-trip stability and hash canonicalization. Three commits (deps / config system / docs).
- **Found:** Two design-derived validations added beyond the task spec: (1) cross-field check that `embargo_trading_days` ≥ longest horizon on temporal/combined splits — encodes the §5.4 leakage rule at config time; (2) uniqueness of seeds and horizons. Horizons are also sorted on resolve so the config hash is order-insensitive. Pydantic field named `model` does not clash with the `model_` protected namespace (no warning).
- **Next:** T0.3 run tracking & manifests (manifests.py, run-ID scheme + artifact writer, env fingerprint).

### 2026-06-12 — T0.3 run tracking & manifests
- **Done:** Added pyarrow 24.0.0 (`uv add`). `src/ecvol/data/manifests.py`: `ManifestEntry` (path/source_url/retrieved_at/sha256/license, extra="forbid"), chunked `sha256_file`, deterministic `write_manifest` (sorted by path), `verify_manifest` returning per-file problems; new `ecvol data verify` CLI group (defaults to scanning `data/manifests/*.json`, exit 1 on any mismatch/missing file). `src/ecvol/tracking.py`: run-ID scheme `<UTC timestamp>-<config-hash[:8]>`; `write_run` writes `artifacts/runs/<id>/{config.yaml, run.json, metrics.parquet}` with config hash, git SHA+dirty, seeds, env fingerprint (python, platform, uv.lock SHA-256, GPUs via nvidia-smi); metrics validated (non-empty, consistent columns) *before* any writes; existing run dirs never overwritten. 24 tests green; five commits.
- **Found:** (1) T0.1 latent bug: the unanchored `data/` gitignore pattern also matched `src/ecvol/data/`, so the package's data subdir was never committed — and `!data/manifests/` could never re-include below an excluded dir. Fixed with root-anchored `/data/*` + `!/data/manifests/`. (2) Parquet bit-identity holds across separate interpreter processes with pyarrow defaults (compression="none"; footer has no timestamp) — verified manually, asserted in-process in tests. (3) `env_fingerprint()` on this machine: RTX 5060 Ti, driver 591.86. The acceptance's "rerun a CPU-only config" is covered at writer level (deterministic bytes for identical rows); the end-to-end form becomes testable when `ecvol evaluate` lands (T2.2).
- **Sources:** none beyond project docs.
- **Next:** Phase 1 — T1.1 dataset acquisition & mirroring (FinCall-Surprise + MAEC fetchers, manifests now ready for them). Push and confirm CI green (user verifies Actions).

### 2026-06-12 — T0.3 closed
- **Done:** CI green on GitHub for the T0.3 push (user-verified in the Actions tab). T0.3 acceptance fully met; Phase 0 (scaffolding) complete.
- **Next:** T1.1 dataset acquisition & mirroring.

### 2026-06-12 — T1.1 dataset acquisition & mirroring
- **Done:** Sized both datasets before downloading (FinCall-Surprise Drive payload: mp3 18+14+20 GB + ppt 1.7+1.4+2.3 GB ≈ 57 GB zipped; C: had ~49 GB free) → bulk mirror lives on the empty internal D: (954 GB) behind an NTFS junction `data\raw` → `D:\ecvol-data\raw`, so code/manifests keep relative paths (DECISIONS.md). Added `gdown`+`requests`; wrote `src/ecvol/data/fetch.py` (downloads pinned to upstream commit SHAs — FinCall `36bb82d2`, MAEC `65a109f5`; atomic temp+rename downloads/extractions, idempotent re-runs) and CLI verbs `ecvol data fetch fincall|maec|all` (+ `--skip-drive`) and `ecvol data spotcheck` (seeded-random ffmpeg decode). 8 tests in `tests/test_fetch.py`. Installed ffmpeg 8.1.1 (winget, Gyan.FFmpeg). Mirrored MAEC (codeload tarball, 3,443 call folders, manifest verifies clean) and FinCall (transcripts from GitHub; six Drive zips via gdown — hit Drive's "many accesses" quota after 2 zips; a 15-min background retry loop cleared it on attempt 2). Per-file SHA-256 manifests committed (`data/manifests/{fincall,maec}.json`).
- **Found:** (1) **MAEC's 59 GB high-level MFCC archive is link-rotted** (Drive id `1yXLv1r5n35s4Nf7kvP96RSPLKyR-ZeUT` → 404, no mirror findable) — the exact risk T1.1 front-runs; documented in DECISIONS.md; MAEC mirror = transcripts + low-level `features.csv` (which the repo itself carries in full). (2) Counts match published sizes *exactly*: FinCall 2,688 (919+704+1065), MAEC 3,443 (3,443 with text, 3,443 with features). (3) FinCall audio join: every call has a unique `mp3_id`; 2,671/2,688 (99.4%) have the file; 17 reference missing audio; 456 mp3 files are surplus (unreferenced by any transcript). ppt files: 2,671. (4) Spot-decode: 50/50 seeded-random mp3s decode cleanly (ffmpeg, seed 0; plus 20/20 early check on the first two years, seed 1). (5) Google Drive folder listing scraped via `embeddedfolderview` + file sizes via `drive.usercontent.google.com` warning pages — useful pattern for sizing public Drive payloads before committing disk.
- **Sources:** github.com/Tizzzzy/FinCall-Surprise (Apache-2.0); github.com/Earnings-Call-Dataset/MAEC-… (CC-BY-SA-4.0); github.com/wkentaro/gdown (quota FAQ).
- **Next:** T1.2 price ingestion (Stooq fetcher + Tiingo cross-check, ticker normalization, coverage report). Push and confirm CI green (user verifies Actions).

### 2026-06-12 — Exploration: FinCall-Surprise identity reconstruction feasibility
- **Done:** Discovered while scoping T1.2 that FinCall-Surprise records carry **no ticker, company name, or call date** (opaque numeric IDs; Seeking Alpha headers stripped from `input`). Probed recovery routes: SA article-ID URLs (404), EarningsCast — the paper's audio source — is dead entirely (HTTP 410 site-wide), mp3 ID3 tags (empty), slide PDFs (rich: `/Company` metadata, title-page text, dates). Per user decision (DECISIONS.md), ran a 50-call seeded feasibility study (`notebooks/explore_fincall_identity.py`, seed 0): extract name candidates from PDF metadata/page-1 + operator-greeting regex, match against SEC `company_tickers.json` (8,007 names), extract dates from PDF page-1 text + creation stamps.
- **Found:** (1) **Date recovery: 50/50 (100%)** — PDF page-1 date or creation stamp present on every sampled call (PDFs cover 2,671/2,688 calls). (2) **Ticker via quick heuristics: 29/50 (58%)**, but the bottleneck is name→ticker *matching*, not signal: manual read of all misses confirms the company is named plainly in the transcript's first ~700 chars in 10/10 no-candidate cases (Arista, International Paper, DXC, DowDuPont, Vulcan, TransDigm, Stanley Black & Decker…). Failures are brand-vs-legal-name gaps (ADP/Citi/ITW/Otis/PPG vs "Automatic Data Processing"/"Citigroup"/"Illinois Tool Works"/…) — fixable with an alias table and/or LLM-assisted name extraction + deterministic matching. Realistic ceiling ≥95%. (3) **New risk: corpus heterogeneity** — the sample + earlier spot-reads contain non-earnings calls (JPMorgan tech-forum fireside with Qualcomm, DXC/Luxoft M&A announcement, Ford monthly-sales call, BMS/Celgene merger call). The "2,688 conference calls" are not all earnings calls; T1.4 needs a call-type classifier + exclusion reason codes, and the per-call `label` (earnings surprise) may be the cleanest earnings-call indicator to cross-check. (4) SEC `company_tickers.json` (free, official, name+ticker+CIK) works well as the matching backbone.
- **Sources:** arXiv 2510.03965 (FinCall-Surprise paper: transcripts from Seeking Alpha, audio EarningsCast, slides Bloomberg/company sites; identity table kept internal for IBES join); sec.gov/files/company_tickers.json.
- **Next:** Decide T1.4 scope expansion + T1.2 ordering (DECISIONS.md entry pending user review); deferred option: email FinCall authors for their internal call-ID→identity table.

### 2026-06-12 — T1.1 closed
- **Done:** User pushed (8 commits) and verified GitHub Actions green. T1.1 fully closed; T1.4 identity work starts next per DECISIONS.md reorder.

### 2026-06-12 — T1.4 identity reconstruction: pipeline built, 77.5% resolved, 100% sample accuracy
- **Done:** Promoted the exploration into `src/ecvol/data/fincall_identity.py` + `ecvol data identity` (per DECISIONS.md scope expansion; T1.4 pulled ahead of T1.2). Evidence model: full-transcript capitalized-phrase mentions (multi-word SEC names only, scored by CIK so share classes merge) + five weighted greeting/host patterns (welcome-to forms 6, host/IR forms 4) + PDF `/Company` (2, it is sometimes the deck-design agency) ; resolve requires score ≥2 and a ≥2 margin over the runner-up — never guess, flag instead. Greeting-side lookup: exact → possessive-dropped → unique-prefix (largest-cap first) → guarded fuzzy (0.85, same first token). SEC table fetched with the required User-Agent (403 otherwise), cached + brand aliases from distinctive first/last title tokens; generic single-token keys ("INTERNATIONAL CORP"→"international") dropped. PDF page-1/metadata signals cached to JSON (`data/raw/ref/fincall_pdf_signals.json`) — iteration went from ~10 min to seconds. Four matcher iterations driven by residue analysis (58%→63%→66%→75%→77.5%). Identity CSV committed (`data/identity/fincall_identity.csv`).
- **Found:** (1) **Resolution 2,084/2,688 (77.5%); dates 2,629 (97.8%); call types: 2,624 earnings, 14 non-earnings, 50 unknown.** (2) **Accuracy: 40/40 seeded-random resolved calls verified correct by transcript read** — coverage is the open problem, not precision. (3) Failure-class taxonomy from residue sampling: junk single-token SEC matches (companies named "On"/"Bill"/"Charlie" — fixed by multi-word-only body counting); share-class self-competition (fixed by CIK keying); EDGAR title quirks (`/MA/` state tags, "trust" wrongly treated as suffix broke Northern Trust); possessive brands (Kohl's→kohls); single-letter spacing (U.S. Bancorp); name-after-quarter greetings (Phillips 66, Zebra); leading event words (Q2 2021 Kohl's). (4) **Remaining residue (~604 calls) is dominated by companies absent from the *current* SEC table — delisted/renamed/acquired since 2021 (Nordstrom private, Kellogg→Kellanova, Campbell Soup→Campbell's, US Steel acquired) — plus brand acronyms (Citi, ADM, BD)**; 162 unique unmatched greeting names cover 401 of the unresolved calls → a curated per-company override CSV (~100-150 entries) is the efficient next step, possibly seeded from SEC EDGAR submissions' formerNames. (5) PDF `/Company` metadata is unreliable (sometimes the design agency) — correctly weighted low.
- **Sources:** sec.gov/files/company_tickers.json (requires descriptive User-Agent); difflib for guarded fuzzy matching.
- **Next:** Override table for the delisted/renamed/acronym residue → target ≥95%; formalize the accuracy gate (larger audited sample, committed); date validation against trading calendar lands with T1.3. Then T1.2 price ingestion.

### 2026-06-12 — Identity pipeline pushed
- **Done:** User pushed the identity-reconstruction work (8 commits) and verified GitHub Actions green.
- **Next:** Override table for delisted/renamed/acronym residue (→ ≥95% resolution), larger committed accuracy audit, then T1.2.

### 2026-06-12 — T1.4 identity v2: overrides + audit gate, 95.3% on earnings calls
- **Done:** Pushed identity resolution 77.5% → 92.9% overall (**earnings-type 2,381/2,499 = 95.3%**, clearing the ≥95% target) across five committed iterations, each diffed against the previous CSV for flips/losses before landing. (1) Matcher bug fixes: norm_name collapsed whitespace *after* the single-letter join (dotted initials normalized inconsistently — "C. H. Robinson" vs "C.H. Robinson"); fuzzy lookup gave up when the single closest key failed the first-token guard (n=1 → scan n=5). (2) Greeting hygiene: leading-filler stripping ("you to the Adobe", "today's Bank of America"), proper-noun shape filter (the re.I patterns made `[A-Z]` case-blind — "ladies"/"our" reached lookup), months/ordinals/"Reports"/"Business Update"/'21-style event words. (3) **Curated override CSV** (`data/identity/fincall_name_overrides.csv`, 77 rows): brand acronyms (ITW, Citi, UPS, ADP, IBM…) and companies absent from the *2026* SEC table — delisted/renamed since the corpus era (Nordstrom, Kellogg, US Steel, Hess, ANSYS, Walgreens, Discover, Comerica, Synovus…); every CIK verified against the live table by ticker or EDGAR's historical registrant list (`notebooks/verify_identity_overrides.py`, 0 problems). (4) Scoring: capped single-token body evidence (recurrence ≥2, cap 3, whole-name keys only — derived first/last-token aliases matched prose: "Turning"→Turning Point Brands, "Watson"→WTW, "Escalade"→GM junk); greeting scan skips junk captures but stops at the first plausible name (scan-until-table-hit let 'ups' hijack United Continental); **greeting-dominance rule** — unchallenged strong greeting evidence beats body-mention volume (GE's call discusses Baker Hughes 10×; Southern's its Georgia Power sub; Occidental's its "Permian Resources" *segment*; Macy's its "State Street" *store*); weak body-only scores (≤3) need corroboration in the opening 1,500 chars (two prose "Moody's" mentions once resolved a Discover call as MCO); possessive spans retried with 's dropped ("Amgen's" → AMGEN); brand-token collisions go to the larger company (Vertex Pharma vs Vertex-the-tax-co; Prudential Financial vs Prudential plc). (5) Call-type classifier: new `meeting` type; strong conference markers (fireside, "introduce our next company", sell-side analyst intros) outrank earnings keywords, with guards for "investor conference call" and "fireside chat at <other venue>" housekeeping (caught a Macy's earnings call mislabeled in spot-check). Counts: 2,499 earnings / 80 conference / 61 meeting / 33 unknown / 14 ma+sales.
- **Found:** (1) **The committed audit gate passed: 60/60 correct** on a seeded stratified sample (40 random resolved-earnings + 20 newly-resolved; `data/identity/fincall_identity_audit.csv`). The audit itself caught two real errors mid-session (Vertex-Pharma-as-VERX, Discover-as-MCO) that drove fixes — sampling resolved calls is as important as chasing the residue. (2) **~30 v1 identities were wrong and are now corrected or honestly unresolved** — the diff-every-iteration discipline (notebooks/diff_identity.py) was what surfaced them; headline-rate-only tracking would have hidden every one. (3) The 2026 SEC table is missing far more corpus-era issuers than expected (Hess, Discover, Coterra, Sealed Air, Interpublic, Comerica, Hologic, Synovus, ANSYS… — the 2024-26 M&A wave); the override CSV is the durable fix and the EDGAR cik-lookup-data.txt file (all registrants ever) verifies historical CIKs. (4) Remaining unresolved (~190) is dominated by mid-call [Audio Gap] fragments, sell-side conference sessions, and webinars — mostly non-earnings; identity for them is low-value. (5) Classification heuristics still mislabel some conference sessions as earnings (McCormick at a consumer conference) — identity is correct there; refine when exclusion reason codes land with the parser.
- **Sources:** sec.gov/files/company_tickers.json; sec.gov/Archives/edgar/cik-lookup-data.txt (historical registrant names → CIK); EDGAR formerNames concept via DECISIONS.md plan.
- **Next:** T1.4 remaining subtasks: transcript JSON parser onto the common schema, timestamp extraction/validation (after-hours rule), join audit (needs T1.2). Then T1.2 price ingestion against the now-known universe. Push and confirm CI green (user verifies Actions).

### 2026-06-14 — Reviewed prior-team multimodal-vol work; integrated into the charter; open-weight QA feasibility scout
- **Done:** Reviewed this team's prior work (research log + Undermind literature review dropped in `ingest/`), wrote `ingest/ingest.md` (full breakdown + 37-figure index + project-fit/conflict analysis), then **integrated the keepable parts into the governance docs** (per "bring it on board, stay true to the charter"): appended seven DECISIONS.md entries (provenance + integration approach; speaker-turn chunking; §13 refs; TX1/TX2/TX3 promotions; Stage-6 audio-LLM recipe); added a TASKS.md **"Exploration tracks"** section (TX1 open-weight QA features, TX2 short-horizon/IV target, TX3 re-examine the prior KeFVP-beating result through our controls) and a speaker-turn-chunking subtask to T3.1; added DESIGN.md §13 refs **[R28]–[R32]** (Multi-Round Q&A IJCAI'20, Multimodal Multi-Task ACM-MM'20, NAM/ECNum CIKM'21, NumHTML AAAI'22, GNAVol IJCNLP'23 — all URL-verified) plus minimal §5.3/§6 forward-pointers. Ran an open-weight QA-generation feasibility scout (`notebooks/explore_qa_generation.py`) on 3 FinCall earnings calls.
- **Found:** (1) **Open-weight QA generation is viable.** Qwen2.5-3B-Instruct (bf16, no quantization, 6.2 GB) produces usable, generalized, evidence-grounded Q&A pairs with source lines and categories — comparable in spirit to the prior team's GPT-5 output (e.g. BMS/Celgene: "What financial benefits does the transaction offer?" → grounded answer on >40% accretion, $45B FCF). Boilerplate chunks correctly return `[]`. This de-risks TX1's open-weights constraint; the DESIGN-pinned 7B should do at least as well. (2) **Categories drift generic** ("Volatility"/"Stock Volatility") — exactly what TX1's topic-clustering step is meant to fix. (3) `source_lines` format is inconsistent (`[12]`, `[11],[13]`, `[5,6]`, `[18-19]`) → needs normalization in TX1. (4) **FinCall transcripts have weak speaker delineation** — the `input` field is ~20–45 newline paragraphs with occasional inline `Executives:` markers, not clean per-turn structure; true speaker-turn chunking needs a better source (a finding for T3.1). (5) DeFVP (ICME 2024) could not be URL-verified → deferred from §13. (6) **Env friction (Blackwell stack):** the project `.venv` has no GPU torch (torch is a Phase-3+ dep); a scratch `uv pip install` pulled a **CPU** torch 2.12 + downgraded `typer` into `.venv`, so the scout ran on the **global py3.14** (CUDA torch 2.11 + `transformers` 5.12). 7B-in-4-bit/bitsandbytes was skipped (fragile on torch 2.11/cu128 + sm_120) in favor of 3B bf16. **`.venv` restored** with `uv sync --locked` (typer 0.26.7 back, scratch pkgs gone, lockfile never touched). `transformers`+`accelerate` remain in the global py3.14 (harmless; removable on request). transformers 5.x API note: `apply_chat_template(..., return_dict=True)` → `generate(**inputs)`.
- **Sources:** `ingest/ingest.md` and the two `ingest/` resources (prior-team log + Undermind report 2025-11-22); verified ref URLs (arxiv 2201.01770; aclanthology 2023.ijcnlp-short.5; ijcai 2020/631; doi 10.1145/3394171.3413752; doi 10.1145/3459637.3482089); `Qwen/Qwen2.5-3B-Instruct` (Apache-2.0).
- **Next:** continue **T1.2** price ingestion (the actual phase-order work). TX1/TX2/TX3 stay parked as gated exploration tracks; they build later in phase order, none ahead of its dependencies.

### 2026-06-15 — T1.2 price ingestion (source pivot Stooq→yfinance; 95.88% FinCall coverage; Tiingo built)
- **Done:** Built the price pipeline. New modules: `data/calendar.py` (cached XNYS wrapper over `exchange_calendars`), `data/prices.py` (combined FinCall∪MAEC universe assembly, batched yfinance fetch, deterministic OHLCV parquet à la `tracking.py`, coverage report, per-ticker manifest with source provenance, **Tiingo fallback** for Yahoo-purged tickers), `data/tiingo.py` (5% seeded sampler, pure return-correlation gate, `fetch_tiingo_ohlcv`). CLI: `prices` is now a sub-app — `ecvol prices pull` and `ecvol prices crosscheck`. Added deps yfinance/pandas/exchange_calendars (lockfile committed). Pulled the universe: **999/1,309 tickers** cached to `data/prices/*.parquet` (gitignored); committed artifacts = `data/coverage/prices_coverage.csv`, `prices_sources.json`, `data/manifests/prices.json` (999 entries, `ecvol data verify` clean). 78 tests green (added `test_prices`/`test_calendar`/`test_tiingo`, 17 new; network mocked throughout), ruff clean.
- **Found:** (1) **Stooq (DESIGN §5.2 primary) closed free programmatic access** — per-symbol `/q/d/l/` now serves a JS SHA-256 proof-of-work wall *and*, once solved (an `auth` cookie is issued), returns `Access denied`; the bulk `/db/d/?b=d_us_txt` bundle returns `Unauthorized` (account required). Pivoted to **yfinance primary + Tiingo cross-check** (user decision; DECISIONS.md 2026-06-15). (2) **FinCall coverage 372/388 = 95.88%** (gate 98%); covered tickers essentially complete (min completeness 0.979, **zero gappy**). MAEC 906/1,213 = 74.69% (informational pre-T1.5). (3) **The 16 missing FinCall tickers are exactly yfinance's documented blind spot — delisted/acquired symbols Yahoo purges** (verified individually: CMA/WBA/K/ANSS/X/JWN/DFS/HES/CTLT/IPG/SNV/SEE/HOLX… all 404 "Quote not found", including a few that arguably still trade → Yahoo API coverage gaps). This is the precise §5.2 risk that motivated rejecting yfinance; **mitigated by the coverage report (zero silent drops, every miss reason-coded) + the Tiingo fallback** (`fetch_tiingo_ohlcv` auto-recovers Yahoo misses when a key is present), which should clear 98% once the key lands. (4) yfinance batch drops under load were *not* the cause — individual retries also 404; the misses are genuine Yahoo absences. (5) Determinism: OHLCV parquet is byte-identical for identical rows (pyarrow `compression="none"`), matching the T0.3 metrics-parquet convention.
- **Pending (both blocked on a free `TIINGO_API_KEY`, env or `.env`):** the >0.999 cross-check gate run (`ecvol prices crosscheck`), and the Tiingo-fallback recovery of the 16 → ≥98% (`ecvol prices pull`). T1.2 stays `[~]` until then.
- **Sources:** stooq.com/q/d/l (PoW wall observed), stooq.com/db/h (bulk archive, `Unauthorized`); yfinance (Yahoo Finance, delisted-symbol purge behavior); Tiingo daily prices API (api.tiingo.com/tiingo/daily); `exchange_calendars` XNYS.
- **Next:** user adds the Tiingo key → run `prices crosscheck` + `prices pull` to close T1.2; then T1.4 join audit (now unblocked) and T1.3 target computation.

### 2026-06-16 — T1.2 Tiingo key landed; cross-check run (47/50), gate semantics aligned to DESIGN §5.2 + artifact persistence
- **Done:** User supplied a free `TIINGO_API_KEY` (in `.env`, gitignored — verified loadable + HTTP 200 on a live AAPL request). First `ecvol prices crosscheck` run on the seeded 5% sample (50 tickers): **47 pass (>0.999), min corr 0.9756**. Hardened the cross-check to match the design contract: (1) `cross_check` now takes a `documented` exceptions set and the gate passes iff every scored ticker is `pass`/`warn`/documented-`investigate`; undocumented sub-0.99 tickers fail and are surfaced (`CrossCheckResult.undocumented`). (2) New committed reason-code file `data/coverage/crosscheck_exceptions.csv`. (3) New artifact writer `write_crosscheck_report` → `data/coverage/crosscheck_report.csv` + `crosscheck_summary.json` (was stdout-only). CLI prints the documented note + report path, and on failure names the undocumented tickers + where to document them. 80 tests green (3 new in `test_tiingo`), ruff clean. DECISIONS.md 2026-06-16.
- **Found:** (1) **XRX corr 0.9756 — the one genuine sub-0.99 outlier**, root-caused to the **Xerox→Conduent spin-off + 1-for-4 reverse split (eff. Jan 2017)**: a +37.6% single-day move on 2017-01-03 in our parquet (largest |return| in the series by far) that yfinance and Tiingo adjust differently — an adjustment-methodology divergence, *not* corrupted data. Documented in the exceptions file. (2) **HPQ corr 0.9974 = a `warn`** (0.99–0.999 band), which DESIGN §5.2 tolerates as minor drift; one third sub-gate ticker was lost when a verification rerun hit the free-tier **50-req/hr** limit (the crosscheck was stdout-only — the trigger for adding the persisted artifact). (3) The original code's gate (`all corr>0.999`) was **stricter than DESIGN §5.2**, which explicitly grants an "investigated and documented" path for sub-0.99 tickers — so the gate could effectively never pass on a realistic sample. (4) Free-tier limits: 50 req/hr (one call per ticker), 1000/day, 500 unique symbols/month — the 5% sample (~50) fits, but back-to-back runs exhaust the hourly window.
- **Sources:** Tiingo daily prices API (api.tiingo.com/tiingo/daily; free-tier 50/hr·1000/day·500 symbols/mo); Xerox/Conduent 2017 spin-off + reverse split (XRX corporate action); DESIGN §5.2.
- **Next (rate-limit blocked ~1 h):** rerun `ecvol prices crosscheck` once the hourly window resets — deterministic seed=0 sample re-hits XRX/HPQ/+1, now writing the artifact and clearing the gate via the XRX exception (resolve the 3rd ticker then: doc it if <0.99, ignore if warn) — then `ecvol prices pull` to recover the 16 delisted tickers via Tiingo fallback (→ ≥98% FinCall coverage). Both close T1.2; push + user verifies CI.

### 2026-06-17 — T1.2 cross-check gate PASSED (artifact committed); Tiingo fallback scoped to FinCall
- **Done:** (1) Reviewed the `pull` path before running and caught that the Tiingo fallback would attempt **all 310 still-missing tickers** (16 FinCall + 307 MAEC) — over the 50/hr cap and the 500-symbol/month budget, with the fallback's bare `except: continue` silently conflating 404 vs 429. **Scoped the fallback to `in_fincall`** (16 tickers); MAEC Tiingo recovery deferred to T1.5 (DECISIONS 2026-06-17; test `test_tiingo_fallback_scoped_to_fincall_only`). (2) Reran `ecvol prices crosscheck`: all 50 requests completed (no rate-limit abort at exactly-50), **gate PASSED** — 47 pass, 2 warn, 1 documented investigate, min corr 0.975641, `undocumented: []`. Persisted artifacts now committed: `crosscheck_report.csv` (50 rows) + `crosscheck_summary.json`. 81 tests green, ruff clean. Commits f072d03 (gate alignment), 3707cf1 (fallback scoping).
- **Found:** The third sub-0.999 ticker (lost yesterday to the rate-limited rerun) is **APD = 0.9941, a `warn`** — within the 0.99–0.999 tolerance band, no action needed (DESIGN §5.2 only requires investigating <0.99). So the full sub-0.999 set on the seeded sample is {APD 0.9941 warn, HPQ 0.9974 warn, XRX 0.9756 documented} — the cross-check gate is now reproducibly green. Cross-check was run **before** `pull` deliberately: the 999 cached tickers are all yfinance-sourced (the correct population for yfinance-vs-Tiingo drift detection), and running it post-pull would both sample meaningless Tiingo-sourced recoveries and push the 5% sample to 51 > 50/hr.
- **Sources:** Tiingo daily prices API (free-tier 50/hr·1000/day·500 symbols/mo); DESIGN §5.2.
- **Next:** after the hourly window resets, run `ecvol prices pull` (16 FinCall-scoped Tiingo calls) to recover the delisted tickers → ≥98% FinCall coverage, then commit the refreshed coverage/manifest artifacts to close T1.2. Push + user verifies CI. Then T1.4 join audit (unblocked) and T1.3 targets.

### 2026-06-17 — T1.2 CLOSED: Tiingo fallback recovered 12/16 → FinCall 98.97%
- **Done:** Ran `ecvol prices pull` (next rate-limit window). The FinCall-scoped Tiingo fallback (16 calls) recovered **12** Yahoo-purged tickers — ANSS, CMA, CTLT, DFS, HES, HOLX, IPG, JWN, MOR, SEE, SNV, WBA — lifting **FinCall coverage 372→384/388 = 98.97%** (clears the ≥98% gate). Manifest now 1,011 entries (12 tiingo-sourced); `ecvol data verify data/manifests/prices.json` → OK. Committed refreshed `prices_coverage.csv`, `prices_sources.json`, `prices.json`. **T1.2 marked `[x]`** — all three acceptance criteria met (≥98% coverage, cross-check >0.999 with documented exception, zero silent drops). 81 tests green.
- **Found:** (1) **4 FinCall tickers remain absent from *both* sources** — ANDG, K, NPIXY, X — each reason-coded in the coverage report (no silent drops). X = US Steel (acquired by Nippon Steel, delisted 2025); the others are obscure/renamed symbols neither Yahoo nor Tiingo serves over 2014–2022. K (Kellanova) trading today but no data returned for the symbol in-range — a Tiingo coverage gap; 98.97% clears the gate so not chased further (would be scope creep beyond the acceptance test). (2) yfinance also surfaced ~10 MAEC delisted-symbol failures (WWE, XLNX, WRI, ZEUS, WYND, WRK, XL, WTR, X, WPX) — expected, MAEC recovery is T1.5's job. (3) `ecvol data verify` with no args hashes **all** manifests incl. ~57 GB of FinCall mp3s — always pass the specific manifest (`data/manifests/prices.json`) for a price-only check.
- **Sources:** Tiingo daily prices API (free tier); yfinance delisted-symbol behavior; US Steel/Nippon Steel 2025 acquisition (X delisting).
- **Next:** push the four T1.2 commits (f072d03, 3707cf1, f7eaa9d, + this artifact commit) and verify CI green. Then **T1.4 join audit** (now unblocked — prices exist for the resolved FinCall universe) and **T1.3 target computation**.

### 2026-06-17 — T1.3 target computation done (join rate 97.12%)
- **Done:** Built `src/ecvol/data/targets.py` + `ecvol targets build` (replaces the CLI stub with a `targets` sub-app). Pure/deterministic/network-free; reuses `calendar.py` and a new `prices.load_close_series` helper. Computes per (call, τ∈{3,7,15,30}): `v_pre`, `v_post`, `Δv` = log realized vol over the τ-return windows around day 0 (DESIGN §5.3, re-derived not ported), plus HAR-RV inputs (realized variance over last 1/5/22 sessions as of day 0). One row per (call,τ) with a `status`/`reason` — zero silent drops. Artifacts: `data/targets/targets.parquet` (gitignored payload, byte-identical re-run verified via SHA) + committed `data/manifests/targets.json` (`ecvol data verify` OK) + `data/coverage/targets_report.csv`. 11 new tests (analytic RV via zero-mean closed form; after-hours boundaries incl. weekend/holiday + the <09:30 / ≥16:00 timestamp branches; exclusion reason codes; HAR inputs; determinism; **3 hand-verified end-to-end calls**). 92 tests green, ruff clean. DECISIONS 2026-06-17.
- **Found:** (1) **First build: 2,496/2,688 calls resolved, join rate 97.12%** (2,424 calls with ≥1 target), 9,696/10,752 rows ok. Exclusions all reason-coded: 768 `unresolved_ticker` (192 calls), 220 `invalid_date` (55 resolved calls with missing/garbled dates), 60 `no_price_data` (15 calls on the 4 delisted tickers absent from both price sources), 4 `insufficient_post_history`, 4 `zero_variance_post`. Per-horizon ok is identical (2,424) across 3/7/15/30 — the generous 2014–2022 price window covers even the 30-session post-windows for every datable in-universe call. (2) **Call times-of-day are unavailable** (identity CSV has dates only; transcript prose has time markers but extraction is unbuilt T1.4 work) → adopted DESIGN §10 risk #7's **assume-after-hours fallback** (`assumed_after_hours` flag on every row; `anchor_day0` parameterized to accept a real `timestamp` later → rerun + sensitivity check, no rework). (3) **`realized_vol` subtlety:** equal *nonzero* returns produce tiny float-noise variance (not exactly 0), so the zero-variance guard fires only on genuinely flat *price* windows (returns exactly 0.0) — which is the real-data case; the test was corrected to use a flat window. (4) HAR-*residual* target needs a train-split-only HAR fit → deferred to Phase 2; T1.3 emits only the inputs.
- **Sources:** DESIGN §5.3 (target formulas + after-hours rule), §5.4 (information rule), §10 risk #7 (missing-timestamp fallback); legacy notebooks 3/3b cross-checked (same RV formula; not ported, per OLDWORK.md §7); Corsi HAR-RV [R17].
- **Next:** **T1.4 join audit** is now fully unblocked (prices ✓ + targets ✓) — wire the earnings-cohort price+target join rate against the ≥95% gate; plus the T1.4 transcript JSON parser + timestamp-extraction subtasks. Push the T1.3 commits + verify CI.

### 2026-06-18 — T1.4 DONE: FinCall ingestion onto the common schema (join 99.31%)
- **Done:** Built `src/ecvol/data/fincall_ingest.py` + `ecvol data ingest fincall` — the last three T1.4 subtasks (transcript JSON parser, timestamp extraction/validation, join audit). Normalizes all 2,688 calls onto the common schema `(call_id, ticker, utc_timestamp→call_date+flags, transcript_json, audio_path, speaker_metadata, source)` plus accounting columns. Transcript parser splits the raw `input` prose on the corpus's coarse inline role markers (`Executives:`→management, `Analysts:`→analyst, `Operator:`→operator, rare attendee/shareholder; text before the first marker → `unknown`) into role-tagged turns; `transcript_json` + `speaker_metadata` are JSON columns. ffprobe audio durations cached under `data/raw/ref/` (resumable). Output: deterministic `data/fincall/calls.parquet` (gitignored payload, byte-identical re-run verified via SHA) + committed `data/manifests/fincall_calls.json` (`ecvol data verify` OK) + committed `data/coverage/fincall_{ingest_report,audio_durations,join_audit}.csv`. 12 new tests (`tests/test_fincall_ingest.py`: parsing incl. glued-marker `...AmericaAnalysts:` and preamble cases, reason codes, audio-injection, schema, determinism, end-to-end join audit). 104 tests green, ruff clean. DECISIONS 2026-06-18.
- **Found:** (1) **All three acceptance gates met:** 100% parse (2,688/2,688 records, zero `empty_transcript`; exclusions reason-coded `unresolved_ticker`=192, `non_earnings`=115, `no_date`=50 — zero silent drops); audio-duration report (2,671/2,671 present mp3s decoded, median 3,693 s ≈ 61 min, mean 3,733 s, range 1,096–11,939 s, **2,769 h total**; 17 mp3s missing, T1.1-known); **earnings-cohort join rate 2,315/2,331 = 99.31%** vs `targets.parquet` (≥95% gate, comfortably clears). (2) **Timestamp investigation closed (§10 risk #7):** measured time-of-day availability — only **3.4%** of transcripts (91/2,688) and **1.6%** of slide PDFs (43/2,674) mention any clock time in their head, and those are mostly press-release issuance times, not the call start. Per-call extraction isn't worth building; uniform **assume-after-hours** fallback applied + flagged (`time_known=False`/`assumed_after_hours=True`), consistent with T1.3. The common-schema `utc_timestamp` is represented as `call_date` + flags rather than a fabricated clock time. (3) **Cohort = parsed + resolved-ticker + dated earnings calls** (2,331); audio availability is deliberately *not* a cohort gate (text-only models still use the 17 audio-less calls) and is reported separately. (4) **Coarse speaker delineation confirmed** (JOURNAL 2026-06-14): many calls collapse to a few role-blocks (e.g. one `Executives:` turn for all prepared remarks) — fine for T1.4's structural parse; true per-speaker turns / sectioning are T3.1. (5) Console gotcha: `≥` is not cp1252-encodable in the PowerShell console — switched the new CLI echo to ASCII `>=`.
- **Sources:** DESIGN §5.3 (after-hours rule), §10 risk #7 (missing-timestamp fallback); T1.3 targets artifact (join denominator); ffprobe (FFmpeg) for durations; corpus measurement (this session).
- **Next:** **T1.5 MAEC ingestion** onto the same common schema (folder-format parser `YYYYMMDD_TICKER`, audio-feature-vs-raw audit, schema mapping, ≥95% price join — including the deferred MAEC Tiingo price recovery, DECISIONS 2026-06-17), then **T1.6 split builder**. Push the T1.4 commit + user verifies CI green.

### 2026-06-18 — T1.5 DONE: MAEC ingestion onto common schema (price-join 75.4%, documented shortfall)
- **Done:** Built `src/ecvol/data/maec_ingest.py` + `ecvol data ingest maec`. Extracted the common-schema `CallRecord` + parquet writer into shared `src/ecvol/data/calls.py` (now used by both FinCall and MAEC ingesters), and generalized `targets.py` to be call_id-type-agnostic (int() coercion moved into FinCall's `_read_identity`) so MAEC reuses the exact FinCall RV/target math. **FinCall's committed `calls.parquet` + `targets.parquet` re-verified byte-identical after the refactor.** MAEC mapping: folder `YYYYMMDD_TICKER` → date+ticker (no identity reconstruction needed); `text.txt` sentences → `unknown`-role turns (MAEC has no speaker labels); `features.csv` per-sentence acoustics → audio-feature audit + summed `Audio Length` as `audio_duration_sec`; no raw audio (`audio_path=""`); no call times → same assume-after-hours fallback. MAEC targets computed and written to `data/maec/targets.parquet` (string call_id). Artifacts: `data/maec/{calls,targets}.parquet` (gitignored, byte-identical re-run verified) + committed manifests `maec_{calls,targets}.json` + `data/coverage/maec_{ingest_report,audio_features,join_audit,missing_tickers}.csv`. 7 new tests (`tests/test_maec_ingest.py`: folder/sentence/features parsing, schema+status, end-to-end join with a price fixture, determinism, full accounting). 111 tests green, ruff clean. DECISIONS 2026-06-18.
- **Found:** (1) **3,443 folders (= published MAEC count), 3,419 parsed.** The 24 exclusions are genuinely truncated `text.txt` files (just "Thank you."/"Thanks a lot.", 11–14 chars) — a real MAEC data defect, reason-coded `empty_transcript` (not a parser bug; spot-verified). 394,249 sentences total (mean 115/call); features.csv present for all 3,443; **raw audio 0** (MAEC never shipped it). (2) **Price-join 2,578/3,419 = 75.4%** — below the ≥95% gate. 296 of 1,213 tickers have no price data, all reason-coded in `maec_missing_tickers.csv` (zero silent drops); they're predominantly genuine 2015–2018 S&P-1500 M&A/delistings yfinance purges (ABC→Cencora, AGN, ANTM→Elevance, APC, ALXN, ABMD, AKS…). (3) **Gate closed via the documented-shortfall path** (user decision): MAEC is the secondary dataset (DESIGN §5.1); a Tiingo recovery burst would cost ~6 h (free tier 50/hr) + ~296 of the 500/month symbol budget and — since the misses are mostly true delistings — realistically still land ~88–93% (<95%), so it's deferred as optional future work, not run now. (4) The `audio_duration_sec` from summed sentence `Audio Length` gives median ~13.4 min / call (917 h total) — notably shorter than FinCall's ~61 min, consistent with MAEC features covering a subset (Q&A) of each call; reported honestly in the audit with its source noted. (5) Refactor payoff: the common schema now lives in one module, and `targets.py` serves both datasets unchanged — the next dataset (Phase 7 fresh calls) plugs into the same machinery.
- **Sources:** DESIGN §5.1 (MAEC role + ~3,443 count), §11 row 1 (link-rot/delisting risk); DECISIONS 2026-06-12 (MAEC no raw audio / MFCC archive link-rot), 2026-06-17 (MAEC Tiingo recovery deferred to T1.5); yfinance delisted-symbol purge behavior.
- **Next:** **T1.6 split builder** (`ecvol splits build`) — temporal (30-trading-day embargo), ticker-disjoint, combined; leakage-assertion test module that runs in CI forever. Both datasets now share the common schema + targets, so splits can be built per-dataset uniformly. Push the T1.5 commit + user verifies CI green.

### 2026-06-18 — T1.6 DONE: leakage-proof split builder (completes Phase 1)
- **Done:** Built `src/ecvol/data/splits.py` + `ecvol splits build` — three split schemes per dataset over the ≥1-ok-target cohort, written as committed CSVs `data/splits/{dataset}_{scheme}.csv` (`.gitignore` allowlists `data/splits/`). **temporal:** order by `as_of` session index (via `calendar.py`), boundaries at the train/train+val call-count quantiles, with a `max(embargo,horizon)`-session embargo zone on the train side of each boundary (train `idx≤b1−gap`, val `b1<idx≤b2−gap`, test `idx>b2`, else reason-coded `embargo`) → no 30-day target window crosses a boundary, ≥30-session gap between segments. **ticker_disjoint:** seeded (default 0) greedy ticker partition ≈70/10/20 by call count (sector stratification skipped — no sector metadata). **combined:** keep a call only where both schemes agree, else `excluded`. Also did a consistency refactor: **moved FinCall targets `data/targets/targets.parquet` → `data/fincall/targets.parquet`** (+ manifest `fincall_targets.json`, removed old `targets.json`) so the layout is symmetric with MAEC — content byte-identical, `fincall_ingest` join audit reads the new path, splits use `data/{dataset}/targets.parquet` uniformly. 11 new tests (`tests/test_splits.py`), incl. assertions on the **committed real CSVs**. 123 tests green, ruff clean. DECISIONS 2026-06-18.
- **Found:** (1) **Split sizes** — FinCall (cohort 2,424): temporal 1480/228/481 (+235 embargo); ticker_disjoint 1701/239/484; combined 1029/21/92 (+1282 excluded). MAEC (cohort 2,578): temporal 1604/73/507 (+394 embargo); ticker_disjoint 1805/259/514; combined 1096/4/102 (+1376 excluded). (2) **The embargo shrinks val on clustered data** — a 10%-by-count val band narrower than the 30-session embargo collapses; FinCall temporal val=228 is healthy but MAEC val=73 is small (MAEC's calls cluster more tightly in calendar time). Documented, not a bug — DESIGN says "approximate proportions"; FinCall is primary and model selection can use temporal or ticker-disjoint val. The combined splits are small by construction (the double constraint) — the "hardest robustness row" (DESIGN §5.4.3), reported as-is. (3) **The path bug that surfaced the refactor:** first `splits build` silently built MAEC only — FinCall targets were at the legacy `data/targets/` path while my new code looked under `data/fincall/`; rather than special-case it I standardized the location (the asymmetry would have bitten every Phase-2 consumer). (4) **CI-guarding the committed artifacts:** the strongest leakage test reads the committed `data/splits/*.csv` directly (no gitignored parquets needed in CI) and re-derives the embargo gap from the trading calendar — so the actual shipped splits, not just the algorithm, are asserted leakage-proof forever. (5) Two boundary-formulation dead-ends before landing the right one: embargoing both sides of val needs a >2×embargo val band (kills val); the chosen single-train-side-embargo needs only middle-span > embargo.
- **Sources:** DESIGN §5.4 (split design: temporal embargo, ticker-disjoint, combined, committed CSVs + CI leakage assertions); `exchange_calendars` XNYS via `calendar.py`; López de Prado embargo/purging concept (the 30-day-target leakage DESIGN §5.4.1 calls out).
- **Next:** **Phase 1 is complete (T1.1–T1.6 all done).** Phase 2 — eval harness + econometric baselines: **T2.1 metrics & significance** (`eval/metrics.py`: MSE/MAE/R²_OOS/quarterly Spearman; `eval/significance.py`: Diebold-Mariano, cluster bootstrap, Holm), then T2.2 Stage-0/1 baselines → Result Table 1, T2.3 reporting. Push the T1.6 commit + user verifies CI green.

### 2026-06-18 — T2.1 DONE: metrics & significance module (Phase 2 starts)
- **Done:** Built `src/ecvol/eval/metrics.py` (DESIGN §7.1) and `eval/significance.py` (§7.2). Added **scipy** as a dependency (`uv add scipy`, lockfile committed) for the Student-t survival function (DM p-values) and Spearman. `metrics.py`: pure array `mse`/`mae`/`r2_oos`/`spearman` (NaN rows dropped) + the (call,τ) prediction-frame helpers `quarter_of`, `spearman_by_quarter` (equal-weight mean over quarters), `metrics_by_horizon`. `significance.py`: `diebold_mariano` (HLN small-sample correction vs `t_{n-1}`, LRV = γ0+2·Σ_{k=1}^{h-1}γ_k, sign +ve⇒model A worse), `cluster_bootstrap_ci` (resample whole clusters — ticker/quarter — percentile CI, seeded), `holm_correction` (step-down, order-preserving). No CLI verb — these are libraries `report` (T2.3) and the §7.5 confirmatory comparisons consume. 18 new tests; 141 green, ruff clean. DECISIONS 2026-06-18.
- **Found:** (1) **DM validated against a published reference via an exact identity:** the HLN-corrected DM at h=1 *equals* the paired t-test on the loss differential — proved algebraically (DM·√((n-1)/n) with population γ0 collapses to d̄/(s/√n)) and asserted to 1e-9 against `scipy.stats.ttest_rel`. This is a stronger check than a hand-worked example and sidesteps statsmodels (which has no built-in DM). (2) **Cluster bootstrap validated on a known sampling distribution:** i.i.d. data (each obs its own cluster) reproduces the analytic normal-CI half-width (≈1.96·SE within 15%); 20 correlated clusters of 50 widen the CI >3× vs. ignoring clusters — the exact i.i.d.-CI understatement DESIGN §7.2 warns about. (3) **A test-design bug of my own:** first asserted the bootstrap CI contains the *population* mean — but a percentile CI brackets the *sample* mean, and here the sample mean fell ~1 SE low, so the true mean was legitimately outside; corrected to assert `lo<point<hi` + point within a few SE. Good reminder that CI tests must respect that the interval is centered on the estimate, not the truth. (4) **R²_OOS baseline is caller-supplied** (`y_persistence`) so the same function serves level-v (baseline `v_pre`) and Δv (baseline 0) — the headline metric stays target-agnostic. (5) Holm matches R's `p.adjust(method="holm")` worked example exactly (0.01,0.04,0.03,0.005 → 0.03,0.06,0.06,0.02).
- **Sources:** DESIGN §7.1 (metrics: MSE/MAE/R²_OOS/quarterly Spearman), §7.2 (DM, cluster bootstrap, Holm), §7.5 (confirmatory comparisons); [R26] Gu–Kelly–Xiu (R²_OOS convention); [R27] Diebold–Mariano 1995; Harvey–Leybourne–Newbold 1997 (small-sample DM correction); R `stats::p.adjust` (Holm reference).
- **Next:** **T2.2 Stage-0/1 baselines → Result Table 1** — persistence, EWMA, HAR-RV, GARCH(1,1), ticker-FE LightGBM over every (dataset×split×target×τ); sanity gate (HAR-RV beats persistence at τ=30 temporal, else halt+debug targets). Needs `models/baselines.py` + `models/gbdt.py` + the first end-to-end `ecvol evaluate` (which consumes this metrics/significance API). Likely adds `arch`/`lightgbm` deps. Push T2.1 commit + user verifies CI green.

### 2026-06-18 — T2.2 DONE: Stage-0/1 baselines → Result Table 1 (sanity gate passes w/ documented COVID exception)
- **Done:** Built the honest floor. `models/baselines.py` (persistence, EWMA RiskMetrics λ=0.94, Corsi **log-HAR** train-fit, **GARCH(1,1)** via `arch` fit once per call on returns ≤ day0), `models/gbdt.py` (LightGBM with a **ticker fixed effect**, fully deterministic), `eval/evaluate.py` + `ecvol evaluate`. Added deps `arch`/`lightgbm`/`scikit-learn` (`statsmodels` transitively). Computes all three targets (level-v, Δv, **HAR-residual** — the train-only HAR fit deferred from T1.3 lands here); persistence is the per-target trivial forecast and the R²_OOS baseline (so persistence R²_OOS≡0, a built-in check). Per-cell DM p-values via the T2.1 significance API. Output: committed deterministic `data/results/result_table_1.csv` (720 rows; `.gitignore` allowlists `data/results/`). 14 new tests; 155 green, ruff clean.
- **Found:** (1) **GARCH-convergence gate PASSED:** FinCall 99.6%, MAEC 99.9% (>95%). (2) **The sanity gate failed literally and I debugged it — targets are sound; it's the COVID regime shift.** HAR-RV beats persistence at τ=30 temporal on MAEC (R²_OOS +0.206) but LOSES on FinCall (−0.287). Investigation ruled out a target bug: the *same* HAR/targets beat persistence at τ=30 on FinCall **ticker-disjoint** (+0.229), on **MAEC temporal** (+0.206), and at **τ=3/7/15 on every FinCall split** — a target defect would fail those too. Root cause: FinCall's temporal train (2017-11–2021-01) is **18% Feb–May-2020 COVID-window calls** (mean v_post −3.41 vs −3.95 overall); the test segment is the calm late-2021 regime (−4.17, **0.22 lower**). HAR fit on COVID-inflated train over-predicts the level at the long horizon; persistence uses each call's own v_pre (mean −4.16 ≈ test) and tracks the regime. At τ≤15 the post-window is near day-0 so the gap is small and HAR wins; ticker-disjoint mixes dates so no shift. (3) **Resolution:** per the charter (fix-the-gate-via-DECISIONS), the gate now passes on the primary temporal split OR via a **documented COVID-regime exception granted only when the regime-stable cells corroborate sound targets** — not a blanket pass (unit-tested: literal-pass / regime-exception / genuine-failure). The τ=30-temporal under-performance vs persistence is itself a paper finding (long-horizon level forecasting across a regime shift is hard; persistence is a strong floor). (4) On the **Δv** target all models beat the zero baseline (e.g. FinCall temporal test τ=7: HAR R²_OOS +0.41, EWMA +0.36, GBDT +0.25, GARCH +0.19, DM p<0.001) — the level-removing target behaves as designed. (5) The ticker-FE GBDT mis-generalizes hardest across the regime shift (τ=30 temporal R²_OOS −0.84) — expected for the identity+metadata baseline; reported. (6) Engineering: LightGBM's sklearn API needs scikit-learn (added; needed anyway for Phase-2/3 heads + §7.3 probe); a stale `evaluate` in test_cli's STUB_VERBS was invoking the *real* full pipeline during the CLI test (105s, then assert-failed) — removed, suite back to ~7s. ~0.4% non-converged GARCH fits emit benign SLSQP stderr; those fits are dropped via the convergence flag.
- **Sources:** DESIGN §6 (modeling ladder Stages 0-1), §7.1-7.2 (metrics/significance, consumed via T2.1), §5.4.5 (COVID regime caveat); Corsi 2009 HAR-RV [R17]; RiskMetrics EWMA (λ=0.94); `arch` GARCH(1,1); Gu–Kelly–Xiu R²_OOS [R26]; Diebold–Mariano [R27].
- **Next:** **T2.3 reporting** — `eval/report.py` + `ecvol report` renders Result Table 1 to LaTeX + Markdown from `data/results/result_table_1.csv`, with byte-identical regeneration (CI check) and cluster-bootstrap CIs (T2.1 API). Then Phase 2 is complete → Phase 3 text ladder. Push T2.2 commit + user verifies CI green.

### 2026-06-18 — T2.3 DONE: reporting (`ecvol report`) — completes Phase 2
- **Done:** Built `eval/report.py` + `ecvol report` → committed `data/results/result_table_1.{md,tex}` rendered from the committed `result_table_1.csv`. Table specs are **data** (`TABLE_1_SPECS`: a list of frozen `TableSpec(dataset,split,target,metric,segment)`); each pivots model × horizon. Render set = R²_OOS + MSE for {fincall,maec} × {level-v,Δv} × {temporal,ticker_disjoint}, test (16 tables). Cells carry `*` when DM-significant vs persistence; NaN → em dash; LaTeX uses booktabs. Added `notebooks/figures_result_table_1.py` (figure stub). 6 new tests; 161 green, ruff clean. DECISIONS 2026-06-18.
- **Found:** (1) **Byte-identical regeneration enforced two ways:** a render-twice determinism test, and a **committed-artifacts CI guard** (`test_committed_reports_match_fresh_render`) that re-renders from the committed CSV and asserts equality with the committed `.md`/`.tex` — so the shipped tables can't silently drift from the data. (2) The rendered R²_OOS table makes the T2.2 COVID-regime story legible at a glance: on FinCall **temporal**, HAR/EWMA/GBDT are positive (beat persistence) at τ≤15 but go negative at τ=30; on FinCall **ticker-disjoint** they stay positive at τ=30 — the regime-shift signature, now a paper-ready table. (3) **Cluster-bootstrap CIs deferred** — they need per-call predictions the aggregated table doesn't carry; Table 1's significance is already conveyed by the DM `*` markers + GBDT seed-std, and a model-vs-model CI is the actual claim only once content models exist (Phase 3+), so I'll persist per-call predictions then. (4) Stub-verb housekeeping: `report` (and earlier `evaluate`) left `STUB_VERBS` in `test_cli` as they became real commands. (5) `_tex_escape` handles the unicode in titles (Δ→$\Delta$, τ→$\tau$, ²→\textsuperscript{2}, `_`→\_).
- **Sources:** DESIGN §7.1 (R²_OOS headline + MSE comparability), §7.2 (DM markers; bootstrap CIs deferred), §7.5 (DM-significance annotation); booktabs LaTeX convention.
- **Next:** **Phase 2 complete (T2.1–T2.3).** The eval floor — metrics, significance, baselines, Result Table 1, reporting — is in place and CI-guarded. **Phase 3 — text ladder + early identity controls:** T3.1 transcript normalization (sections + speaker-turn chunking, reusing the FinCall/MAEC `transcript_json`), T3.2 frozen text features (BGE/GTE embeddings, FinBERT, surface stats — GPU), T3.3 Stage-2 results → Result Table 2, T3.4 identity-control suite. Push T2.3 commit + user verifies CI green.

### 2026-06-18 — Workflow: near-autonomous task loop (LOOP.md + guardrail hooks)
- **Done:** Set up a near-autonomous backlog loop so the user is no longer in the loop on every task. New files: **`LOOP.md`** (the per-iteration protocol — orient → select unblocked task → design-call check → implement → local gate → local commit + docs → blocker-skip/queue → phase-boundary pause), **`HANDOFF.md`** (append-only human-pending queue, seeded with anticipated blockers), **`.claude/settings.json`** + **`.claude/hooks/guardrail.ps1`** (committed guardrails), and `.gitignore` now excludes `/.claude/settings.local.json`. Updated CLAUDE.md (doc-map rows for LOOP.md/HANDOFF.md + an "Autonomous loop" workflow bullet) and logged the workflow change in DECISIONS 2026-06-18. The four governing decisions came from the user this session (AskUserQuestion): gate = **phase-boundary CI checkpoint** (local `ruff`+`pytest` per task is a faithful CI mirror, so commit locally and keep going; user pushes + confirms CI at phase boundaries); scope = **run until the next human-blocker** (skip `[!]` tasks); design calls = **stop and ask** (never guess a research/design choice); notifications on **blockers + phase-complete + every DECISIONS entry**.
- **Found:** (1) **CI is an exact local mirror** — `.github/workflows/ci.yml` runs only `uv run ruff check .`, `ruff format --check .`, `pytest -q` on locked Py3.12; this is what makes "commit on local-green without waiting" safe rather than reckless, with phase-boundary checkpoints catching any ubuntu-vs-Windows surprise. (2) **Driver = self-paced `/loop` + `ScheduleWakeup`, not a Stop hook** — a Stop-hook continuation loop caps at 8 consecutive blocks and fights `/loop` (claude-code-guide); hooks are reserved for guardrails. (3) **Guardrail scope kept tight on purpose:** `PreToolUse` denies only `git push` (user owns pushes/CI) and `legacy/` writes (read-only) — high-signal, zero false-positive — rather than a brittle "spend/credential" command deny-list; the key/spend/ToS/labeling hard-stops are enforced by the LOOP.md protocol + surfaced in HANDOFF.md (and most aren't runnable non-interactively anyway). `SessionStart` re-injects open `[!]` tasks + a LOOP.md pointer so the task thread survives auto-compaction. (4) Honest expectation set with the user: with "stop on any design call," the near-term payoff is removing per-task push/CI/handoff latency and running the mechanical work + controls plumbing autonomously — not unattended sprints through modeling (T3.1/T3.2 surface design calls early). (5) `.claude/` existed but was empty; `.gitignore` already reserved `/.claude/scheduled_tasks.lock`, so settings.json + hooks/ are net-new committed config.
- **Sources:** Claude Code hooks (PreToolUse deny / SessionStart additionalContext), `/loop` self-paced mode + `ScheduleWakeup`, `PushNotification` (via claude-code-guide agent); CLAUDE.md (user owns push/CI, legacy read-only); `.github/workflows/ci.yml`.
- **Next:** Begin the loop with `/loop follow LOOP.md`. First backlog item is **T3.1 transcript normalization** — but it carries design calls (section-detection heuristics, speaker-turn chunking strategy) the loop will surface for the user before implementing. No human blockers active right now (HANDOFF.md "Active" is empty).

### 2026-06-19 — T3.1 transcript normalization (sectioning + speaker-turn chunking) — impl done, audit pending
- **Done:** First loop task. Surfaced the design call; user picked all three recommended options (deterministic heuristic / best-effort MAEC / sentence-split oversized chunks — AskUserQuestion). Built `features/text/sections.py` + `ecvol featurize sections` (the `featurize` stub is now a sub-app). Output: `data/{dataset}/chunks.parquet` (gitignored deterministic payload + manifest `{dataset}_chunks.json`) + committed `data/coverage/{dataset}_{sections,section_audit}.csv`. Boundary = operator Q&A-cue turn, else first analyst turn (FinCall), with an intro-mention guard (cue must be within 3 turns of first analyst); MAEC = in-text cues only, roles unavailable. Chunk-per-turn, never across turns; oversized turns sentence-split into same-turn sub-chunks (`max_words=320`, under the BGE/GTE 512-token window). 13 new tests; 174 green, ruff clean; byte-identical reruns + `ecvol data verify` OK. DECISIONS 2026-06-19.
- **Found:** **FinCall** Q&A detected 2654/2688 (98.7%); 2408 corroborated by *both* operator-cue and first-analyst (90.7% of detections); methods first_analyst=2151 / operator_cue=443 / text_cue=60 (calls whose coarse role markers didn't parse) / none=34; 196,233 chunks, 0 oversize. **MAEC** (no speaker labels) Q&A detected only 867/3443 (25.2%) — most MAEC sentence text carries no explicit operator cue — 394,280 chunks, 1 oversize; corroboration impossible without roles. Eyeballed the 30-call FinCall audit: boundaries land on genuine analyst questions / operator "first question from..." intros; the only soft cases are out-of-cohort non-earnings calls (a JPMorgan Tech Forum, a Goodyear annual meeting — both no-ticker, excluded from the modeling cohort). The automated proxy (90.7% corroborated, clean eyeball) strongly suggests >90%, but the formal acceptance number is a human check.
- **Sources:** DESIGN §6 (Stage-2 text; section-pooling, no per-sentence alignment), §6 Stage-2 row (BGE/GTE 512-token context → chunk cap); DECISIONS 2026-06-14 (speaker-turn chunking lesson), 2026-06-19 (design-call resolution); LOOP.md protocol.
- **Next:** **Loop paused on a human blocker.** (1) User: verify `data/coverage/fincall_section_audit.csv` (and `maec_...`) — is the prepared/Q&A boundary right on ≥90%? Mark `correct_y_n`; reply to close T3.1 (`[x]`). (2) Then **T3.2 frozen text features** is the next task and a **design call** (embedding model BGE-large vs GTE + revision pin, FinBERT variant, pooling strategy) **+ GPU** — the loop will stop and ask before extracting. Push T3.1 commit + (with the loop-infra commit) confirm CI green at this Phase-3-start checkpoint.

### 2026-06-19 — T3.1 audit artifact refined (cohort sampling + context columns)
- **Done:** On user request, made the section audit easier and fairer. The 30-call sample now draws from the **earnings cohort only** (≥1 ok target, via the same definition as `splits.load_cohort`), dropping the out-of-cohort non-earnings calls that had slipped into the FinCall sample. Added `prev_text`/`boundary_text`/`next_text` snippet columns so each row reads operator-intro → analyst-question → management-answer and is judgable without opening the transcript. Chunks payload + `{dataset}_sections.csv` unchanged (chunks.parquet byte-identical; manifest timestamp churn reverted). 1 new test (cohort restriction + context columns); 175 green, ruff clean. DECISIONS 2026-06-19. User installed the Edit csv + Rainbow CSV VS Code extensions for the grid view.
- **Next:** unchanged — user audits the refreshed CSV (>90% on FinCall) to close T3.1, then the T3.2 design call.

### 2026-06-19 — T3.1 CLOSED: section audit passed 30/30
- **Done:** User completed the hand-audit of the cohort-restricted 30-call FinCall sample → **30/30 correct** (100%, clears the >90% gate). Calibration confirmed: a boundary on the operator's hand-off ("our first question comes from…") counts as correct — it's the start of the Q&A section — as does a boundary on the first analyst question (the two detection methods produce equivalent, valid section splits). T3.1 marked `[x]`; HANDOFF Active cleared. **Phase 3 task 1 of 4 complete.**
- **Found:** The only `n`-leaning rows were operator-handoff boundaries, which are correct by the section-detection definition (not the verbatim-question definition) — no heuristic change needed. Acceptance was section *placement*, not "boundary turn == question text".
- **Next:** **T3.2 frozen text features** — the next task and a design call + first GPU work: embedding model (+ revision pin), FinBERT variant, pooling default, and the torch/transformers/GPU dependency setup. Loop will stop and ask before adding deps/extracting. Push (loop infra + T3.1 commits) at convenience; CI checkpoint formally due at the Phase-3 boundary (after T3.4).

### 2026-06-19 — T3.2 frozen text features: implementation + GPU benchmark (full run pending)
- **Done:** Built the Stage-2 text feature stack. `features/text/_common.py` (chunk loader, content-hash cache `encode_with_cache`, deterministic torch setup, section/scope pooling, deterministic parquet writer), `surface.py` (CPU: len/turns/numeric-density/question-marks per scope), `embeddings.py` (**BAAI/bge-m3** 1024-d, section-pooled, mean or n_words-weighted), `finbert.py` (**ProsusAI/finbert** probs per scope×role + net). CLI `ecvol featurize text` with `--limit` benchmark + ETA. GPU deps in a **`gpu` PEP-735 group** (torch **2.11.0+cu128** for the Blackwell RTX 5060 Ti / sm_120), so `uv sync`/CI stay torch-free; model code imports torch lazily. Design call answered by user (delegated embedding model → BGE-M3; ProsusAI/finbert; fp32+deterministic; benchmark-first). DECISIONS 2026-06-19. 8 new torch-free tests; 182 green, ruff clean.
- **Found:** (1) **GPU works end-to-end** — torch 2.11.0+cu128 drives the 5060 Ti (capability (12,0)=sm_120), CUDA matmul OK; no driver update needed. (2) **Benchmark (50 calls = 3,130 chunks, fp32-deterministic, incl. one-time model load):** BGE-M3 **37 ch/s**, FinBERT **90 ch/s** → conservative full-corpus ETA FinCall (196,233) ~2 h, MAEC (394,280) ~4 h. fp32+deterministic is the throughput cost of bit-identical re-extraction; the content-hash cache makes any re-run free and the full run resumable. (3) A real bug caught by the determinism test: `write_manifest` doesn't create `manifests/` — added the mkdir in all three builds. (4) Windows symlink warning from HF cache is benign (Developer Mode off → more disk, no failure). (5) Kept CI torch-free by lazy-importing torch inside functions — the 8 feature tests exercise only the pure logic (pooling, aggregation, surface stats, cache dedupe/bit-identical), validated without the GPU stack.
- **Sources:** DESIGN §6 (Stage-2: section-pooled embeddings, FinBERT per section/role, surface stats; pooling as config), §3 (GPU throughput benchmarked before full run); mid-2026 MTEB landscape (BGE-M3 MIT workhorse, Qwen3-Embedding tops open MTEB) via web search; model cards BAAI/bge-m3 (MIT), ProsusAI/finbert.
- **Next:** **Loop paused for the benchmark go/no-go** (user chose benchmark-first). On go, run the full extraction (FinCall first — primary, unblocks T3.3; then MAEC) as a resumable background job, then commit the manifests/coverage + close T3.2 `[x]`. Then T3.3 (Stage-2 heads → Result Table 2) and T3.4 (identity controls). Push checkpoint due at the Phase-3 boundary.

### 2026-06-19 — T3.2 CLOSED: full Stage-2 text extraction (both datasets)
- **Done:** User chose "both now (overnight)"; ran the full extraction in one resumable background job (~95 min wall, not the ~6 h conservative estimate). FinCall: `text_embeddings` 7,835 rows / `text_finbert` 28,123 / `text_surface` 7,835 (185,126 chunks encoded, ~11k cache hits from dedup + the 50-call benchmark). MAEC: 7,739 rows each. Manifests verify OK; T3.2 marked `[x]`. Committed the 6 text manifests.
- **Found:** (1) **Acceptance met — re-extraction is bit-identical:** a warm-cache re-run encoded **0 chunks** and reproduced all three FinCall parquets byte-for-byte (content-hash cache + fp32-deterministic). (2) **MAEC ~5× faster per chunk** than FinCall — MAEC chunks are single sentences (short seqs), FinCall chunks are speaker turns up to 320 words; BGE-M3 throughput is sequence-length-bound, so 394k MAEC chunks took less wall time than FinCall's 196k. The 6 h estimate was conservative (linear in chunk count, ignoring MAEC's short sequences + one-time model-load amortization). (3) Dedup by content hash saved ~11k encodes on FinCall. (4) Caches are large (~0.7 GB FinCall, ~1.5 GB MAEC) but gitignored; only the ~61 MB pooled-embedding parquets + manifests are tracked (manifests committed). (5) MAEC finbert has no per-role rows (all turns role `unknown` → only scope-level "all"), as designed.
- **Sources:** DESIGN §6 (Stage-2 frozen features), §3 Stage-2 acceptance (deterministic re-extraction, throughput benchmarked); model cards BAAI/bge-m3, ProsusAI/finbert.
- **Next:** **T3.3 Stage-2 results → Result Table 2** (ridge + shallow-MLP heads on these features, 5 seeds, with/without past-vol covariates; DM vs HAR-RV and vs Stage-1) — a design call (head choices, covariate sets, ablations) the loop will surface. **T3.4 identity controls** (run alongside, per DESIGN "run NOW"). Phase-3 CI checkpoint (push + verify) due after T3.4. Phase-3: T3.1 ✅ T3.2 ✅ → T3.3, T3.4 remain.

### 2026-06-19 — T3.3 DONE: Stage-2 content heads → Result Table 2 (text doesn't cleanly beat the floor)
- **Done:** Built `models/heads.py` (ridge α-on-val + seeded shallow MLP + train-fit PCA + train-median impute), `features/text/assemble.py` (2048-d emb prepared+qa + FinBERT scope×role + surface, zero-fill), `eval/stage2.py` + `ecvol evaluate-text` → committed `data/results/result_table_2.csv` (864 rows), `report.py` Table-2 render + `ecvol report` (md/tex). 3 covariate variants × {ridge, mlp}, 5 seeds, DM vs persistence/HAR/Stage-1. Reuses the Table-1 harness. User design call: section-split+all-blocks / 3 ablations / ridge+MLP. 9 new tests; 189 green, ruff clean. DECISIONS 2026-06-19.
- **Found:** **HEADLINE (honest, reported as-is): frozen text content does NOT cleanly beat the identity/past-vol baselines.** FinCall *temporal* Δv: text+pastvol heads overfit (R²_OOS strongly negative — high-dim embeddings fail to generalize across the COVID regime shift, echoing T2.2); `ridge_text` ≈0. FinCall *ticker-disjoint* Δv: text heads positive (+0.09…+0.28) but `ridge_pastvol` (no text) comparably strong → gain not clearly content. Many `*` (DM vs Stage-1) mark *worse-than-Stage-1*. → the §4 framing-gate evidence; **T3.4 controls must adjudicate before any text-adds-signal claim** (the §3.1 trap). **Two run-surfaced bugs (held the commit, fixed + regression-tested):** (1) 4 MAEC ok-rows have NaN `rv_monthly` (uncomputable 22-session window) → heads now train-median-impute covariates, fit on finite-target rows, select α on finite-val; `har_fit` already NaN-robust so Table 1 unchanged. (2) pandas `.to_numpy()` read-only array → `.copy()`. Synthetic tests missed both (no NaN / writeable arrays); added a NaN-injecting end-to-end test + a real-data smoke on the failing MAEC cell before the full run. Modeling path is sklearn/CPU (no torch) → CI stays torch-free. MLP ConvergenceWarning (max_iter 300) is cosmetic (heads overfit regardless).
- **Sources:** DESIGN §6 (Stage 2: ridge/MLP on section-pooled text, pooling+ablations as config), §7.1-7.2 (R²_OOS, DM), §7.5 (confirmatory: Stage-k vs Stage-1 on Δv), §3 (no α-sweep-then-retrain; small-n discipline), §3.1/§7.3 (identity controls before content claims); Gu-Kelly-Xiu R²_OOS.
- **Next:** **T3.4 identity-control suite** (`models/ticker_only.py`, `eval/controls.py`: ticker-only model, same-ticker transcript shuffle, identity linear probe) — a design call, and the one that triggers the **§4 framing-gate review** (DECISIONS entry required either way) given T3.3's ambiguous content result. Completes Phase 3 → then the Phase-3 push + CI checkpoint. Phase-3: T3.1 ✅ T3.2 ✅ T3.3 ✅ → T3.4 remains.

### 2026-06-19 — T3.4 identity-control suite: ran; identity dominates, content adds little (→ Path B, user decides)
- **Done:** Built `models/ticker_only.py` + `eval/controls.py` + `ecvol controls` → committed `data/results/result_controls.csv` (1,368 rows) + `controls_probe.csv`. Three controls (ticker target-mean, within-ticker + global transcript shuffle, linear identity probe) on all 6 heads, both datasets, both splits. Split heads `fit`/`predict` out of the T3.3 wrappers (additive; Table-2 unchanged). 5 new tests; 194 green, ruff clean. DECISIONS 2026-06-19.
- **Found (decisive):** (1) **Identity probe** — linear logistic on the frozen full-call embedding predicts ticker at **89.5% FinCall (319× chance)** / **53.9% MAEC (487× chance)**: embeddings heavily encode identity. (2) **Transcript shuffle** — within-ticker swap leaves Δv R²_OOS ≈ unchanged everywhere (FinCall temporal `ridge_text` real +0.028/+0.030/+0.015/−0.144 → within +0.017/+0.020/+0.006/−0.142), global shuffle clearly worse ⇒ the heads read **ticker identity, not call content**. A few ticker-disjoint `text+pastvol` cells drop under within-shuffle (honest exceptions, not a ≥2/4-horizon DM-significant pattern). (3) Real bug caught by a test: shuffle was permuting past-vol covariates (not just text) for the *_pastvol variants — fixed to shuffle only true text columns. **→ §4 Path A criterion not met; evidence points to Path B.**
- **Sources:** DESIGN §7.3 (control suite definitions), §3.1 (ticker-identity leakage — the central threat), §4 (pre-registered dual framing, Path A criterion); [R14] (identity-memorization in this task family).
- **Next:** **§4 framing-gate decision is the user's call** (Path A vs B; DECISIONS entry required either way) — surfaced via AskUserQuestion + phone. On decision: append the framing-gate DECISIONS entry, mark T3.4 `[x]` → **Phase 3 complete**, then the **Phase-3 push + CI checkpoint** (12+ commits). Phase-3: T3.1 ✅ T3.2 ✅ T3.3 ✅ T3.4 (controls ✅, framing pending).

### 2026-06-19 — §4 framing-gate DECIDED + Phase 3 COMPLETE
- **Done:** User chose **provisional Path B** ("rigorous re-examination") at the post-Phase-3 gate — adopt B now (drives Phase-4 framing/writing), explicitly revisit/flip to A if the Phase-4 audio ladder yields DM-significant gains surviving the controls (DECISIONS 2026-06-19). T3.4 marked `[x]`; **Phase 3 (T3.1–T3.4) complete.** Corrected a doc count error (T3.4 = 5 new tests / 194 green, not 9/203).
- **Found:** the project now has a coherent, honest spine: an open benchmark (ecvol-bench) + strong baselines + an identity-control suite showing that frozen-text "signal" on this task is largely ticker identity. RQ1-audio (Phase 4) is the open question that could still move the framing.
- **Next:** **Phase-3 boundary checkpoint (gate policy): user pushes the local commits (now ~13: loop infra → T3.4) and confirms CI green before Phase 4 begins.** Then **Phase 4 — audio ladder** (T4.1 audio QC, T4.2 eGeMAPS, T4.3 WavLM/emotion2vec+ — GPU; T4.3 carries the HF gated-model accept for pyannote if diarization is enabled, and the 50-call ETA gate). Loop pauses here per the phase-boundary gate.

### 2026-06-19 — T4.1 DONE: audio QC + 16 kHz FLAC store (FinCall-only)
- **Done:** `features/audio/qc.py` + `ecvol audio qc`/`audio qc-ref`. One ffmpeg pass per call → QC (astats peak/RMS + silencedetect) on the source AND a 16 kHz mono FLAC to `data/raw/audio_16k/fincall/` (2,671 files / 119 GB, gitignored regenerable cache). Committed `data/coverage/fincall_audio_qc.csv` + `_summary.csv` + `earnings21_qc_validation.csv`. User design call: ffmpeg-filters / persist FLAC / both validations. 8 new tests; 202 green, ruff clean. DECISIONS 2026-06-19.
- **Found:** **2,671/2,671 decoded (100%); 1 `mostly_silent` flagged (reason-coded, not dropped), 0 decode errors.** Median ~61 min, silence 0.16, peak −1.07 dBFS. **Heterogeneous source sample rates** — 22050 (1361)/16000 (930)/44100 (256)/11025 (77)/**8000 (14)**/32000/24000/48000; sub-16k sources upsampled (no info gain), flagged for audio-model interpretation. Earnings-21 reference (3 wavs) all decode_ok, sane (peak ≈0, silence 15–21%) → QC pipeline validated against external ground truth. MAEC has no audio → audio ladder is FinCall-only. ffmpeg-only (no Python audio deps); CI tests use synthetic signals (skip if no ffmpeg) + pure parse_qc (always).
- **Sources:** DESIGN §4/§6 Stage 3 (audio QC before GPU-weeks), §8.3 (16 kHz, eGeMAPS CPU), [D5] Earnings-21 (Rev.com CC-BY-SA); ffmpeg astats/silencedetect.
- **Next:** **T4.2 eGeMAPS extraction (CPU)** — openSMILE eGeMAPS functionals per call over the 16 kHz store; a design call (openSMILE Python pkg vs binary, per-call vs per-turn pooling, feature-set config). Then T4.3 (WavLM/emotion2vec+, GPU, 50-call ETA gate + optional pyannote). Loop continues within Phase 4 (mid-phase; CI checkpoint due at the Phase-4 boundary).

### 2026-06-19 — T4.2 DONE: eGeMAPS functionals per call (CPU) + resumability fix
- **Done:** `features/audio/egemaps.py` + `ecvol audio egemaps`; opensmile eGeMAPSv02 Functionals (88-d) per call over the 16 kHz store. `opensmile` in a new `audio` dep group (CI-light, lazy). **2,671/2,671 calls × 88 features, 0 failures.** Output `data/fincall/audio_egemaps.parquet` (gitignored) + manifest + `fincall_egemaps_summary.csv`. 4 new tests; ruff clean. DECISIONS 2026-06-19.
- **Found:** (1) **Distribution sane vs published eGeMAPS:** F0 median 28.6 semitones (human-voice ~20–45), loudness 0.46 (>0), no all-NaN columns. (2) **Runtime ~1.6 h on 8 cores** — eGeMAPS over ~hour-long calls is ~30–60 s CPU each, far over my 15–30 min estimate (the user flagged it past 30 min; I checked worker CPU-time to confirm ~85-min elapsed, fully saturated, ~⅓–½ done, and we chose to let it finish). **Lesson: apply the 50-call ETA-measure-first gate to CPU extraction too.** (3) **Resumability mid-course fix:** the initial batch-write-at-end had no crash recovery/progress — refactored to checkpoint every 200 calls + skip cached call_ids on re-run; validated by a skip-all re-run that reproduced the parquet **byte-identical** (manifest OK). Per-turn eGeMAPS deferred to T4.3 (diarization).
- **Sources:** DESIGN §6 Stage 3 (eGeMAPS-first, CPU), §8.3 (CPU/parallel), §11 risk #3 (audio throughput); openSMILE eGeMAPSv02 (audeering `opensmile`).
- **Next:** **T4.3 neural audio (WavLM-Large + emotion2vec+, GPU)** — the heaviest audio task: chunk ≤30 s for VRAM, **50-call ETA gate before the full run** (DESIGN), optional pyannote diarization (gated HF model + HF_TOKEN — HANDOFF if enabled). The resumable/checkpointed pattern from T4.2 carries over. Then T4.4 (Result Table 3 + gender analysis). Phase-4 CI checkpoint at the phase boundary.

### 2026-06-19 — T4.3 (in progress): WavLM-Large neural audio + ETA gate
- **Done:** `features/audio/wavlm.py` + `ecvol audio wavlm` — `microsoft/wavlm-large` (1024-d), 30 s non-overlapping windows, time-mean last-hidden-state per window → mean over windows → per-call vector. Resumable/checkpointed (every 100, skips cached), fp16 option, window-batching. User design call: WavLM-first / per-call (no diarization) / 30 s no overlap. 2 new tests (pure parquet + guarded embed); 208 green, ruff clean. DECISIONS 2026-06-19.
- **Found:** **ETA gate (mandatory) caught a slow naive impl:** 50 calls/1478 s → ~22 h. Root cause = batch-1-per-window + per-window normalize/GPU-sync overhead (~122 windows/call). Optimized (batch full windows per forward + fp16): 30 calls/361 s → **~12 s/call → ~9 h fp16** (~18–22 h fp32). **Plan = local overnight fp16, resumable** (user decision); cloud rejected (119 GB upload friction for a ~9 h job). Full run launched (background). Lesson reinforced from T4.2: always ETA-gate the heavy extraction *and* batch GPU work, not per-item.
- **Sources:** DESIGN §6 Stage 3 (WavLM/emotion2vec, ≤30 s chunks, no per-sentence alignment), §8.3 (WavLM ~4–6 GB fp16 on 16 GB), §11 risk #3 (audio throughput → ETA gate + cloud option); `microsoft/wavlm-large`.
- **Next:** await the full WavLM run → verify 2,671×1024 parquet + manifest, commit data artifacts. Then **emotion2vec+** (second pass, funasr) to complete T4.3, then **T4.4** (Stage-3 heads → Result Table 3 + §3.5 gender-confound analysis). Committing WavLM code now (code-only; data with the completion commit).

### 2026-06-23 — T4.3 WavLM full run DONE (data artifact)
- **Done:** Overnight fp16 WavLM run finished cleanly: **2,671×1024 embeddings, all finite, 0 all-NaN rows**, manifest verifies OK, and a re-run is a clean no-op (0 new — resume skips all). Window counts median 124 (≈62 min/call), range 37–398; mean L2 norm 1.52. Committed the data artifact (manifest; parquet gitignored). Ran ~overnight as planned (~9 h ballpark; resumable checkpoints held).
- **Found:** the fp16 + window-batching optimization held up over the full corpus — no OOM, no NaNs, consistent ~12 s/call. The resumable design meant the mid-run progress checks (330/2671 etc.) were read straight off the growing checkpoint parquet.
- **Next:** **emotion2vec+** (the remaining T4.3 sub-task; funasr dep — finicky on Windows, will treat install failure as a documented HANDOFF and proceed to T4.4 WavLM-only if needed). Then **T4.4** Stage-3 heads → Result Table 3 + §3.5 gender-confound analysis (the audio analogue of T3.3+controls; provisional Path B is revisited here if audio beats the floor surviving controls).

### 2026-06-23 — T4.3 emotion2vec+ (second pass): funasr risk cleared, full run launched
- **Done:** `features/audio/emotion2vec.py` + `ecvol audio emotion2vec` (`emotion2vec_plus_large` via funasr; 1024-d; 30 s-window mean-pool per call; resumable, mirrors WavLM). **funasr Windows risk CLEARED:** installs (needed an undeclared `torchaudio` dep — added to the gpu group on the cu128 index) and *runs* — the guarded synthetic embed test passed (1024-d finite). ETA gate: 20 calls/330 s → **~16.5 s/call → ~12.3 h** full corpus. User chose local overnight (fp32) → full run launched (background, resumes from 20 cached). 210 green, ruff clean. Committing code now (data on completion).
- **Found:** the `Warning, miss key in ckpt: ...decoder.blocks...` messages are **benign** — emotion2vec_plus_large ships decoder/classifier weights the embedding-extraction path doesn't load; feats come out 1024-d, finite, mean L2 ~10.8. funasr's AutoModel doesn't expose a clean fp16 toggle (unlike WavLM), so the run is fp32 — acceptable for a mean-pooled per-call vector. Local pytest now ~10 min because the audio/gpu stack is in the venv so guarded model tests actually run; **CI stays fast** (installs only the `dev` group → guarded tests skip).
- **Sources:** DESIGN §6 Stage 3 (emotion2vec+), §8.3 (~2–3 GB); emotion2vec/emotion2vec_plus_large model card; funasr AutoModel.
- **Next:** await the emotion2vec+ full run → verify 2,671×1024 + manifest, commit data artifact → **T4.3 complete**. Then **T4.4** (Stage-3 audio heads → Result Table 3 + §3.5 gender-confound analysis) closes Phase 4 → Phase-4 push + CI checkpoint.

### 2026-06-23 — Paper scaffold started (LaTeX living draft, framing-neutral)
- **Done:** Created `paper/` — a self-contained two-column (ACL/EMNLP-style) LaTeX manuscript scaffold that compiles with a stock TeX distribution (no external style files; documented one-line switch to the official `acl` class). Full section structure (`sections/00`–`10` + `A1` appendix): abstract, intro (RQ1–RQ4 + contributions), related work, data, methods (stage ladder 0–5), experiments (metrics, DM/bootstrap/Holm, identity-control suite), results, discussion, conclusion, ACL-required Limitations + Ethics. Finished phases written with **real numbers**; pending phases marked with `\TODO{}`/`\pending{}` macros (greppable). Curated headline tables (`tables/baselines.tex`, `tables/text.tex`, `tables/controls.tex`) built from Result Tables 1–2 + controls CSVs; appendix `\input`s the `ecvol report`-generated `data/results/result_table_{1,2}.tex` verbatim. `refs.bib` built from DESIGN.md §13 (all cited keys). TikZ stage-ladder figure stub. `paper/README.md` with build + ACL-switch instructions. DECISIONS 2026-06-23.
- **Found:** No TeX toolchain installed in this environment (no `latexmk`/`pdflatex`/`xelatex`/`tlmgr`), so the PDF was **not built here** — static review only. Two compile risks pre-empted: (1) the fragile `\twocolumn[\begin{@twocolumnfalse}…]` abstract trick replaced with the plain `\maketitle`+`abstract` form; (2) the appendix `\input`s ~32 generated table floats with little intervening text → "Too many unprocessed floats" risk, mitigated with `\extrafloats{120}` in the preamble. User decisions captured during planning: living-scaffold scope, framing-neutral, ACL/EMNLP two-column.
- **Sources:** DESIGN.md (RQs §1, targets §5.3, splits §5.4, ladder §6, eval §7, refs §13), TASKS.md (phase status), DECISIONS.md (§4 gate, COVID exception), `data/results/result_table_{1,2}.tex`, `result_controls.csv`, `controls_probe.csv`.
- **Next:** User installs a TeX distribution (TeX Live / MiKTeX) or uploads `paper/` + the two generated tables to Overleaf, then `cd paper && latexmk -pdf main.tex` to render and eyeball. As Phase-4 audio lands, fill Stage-3 results (Result Table 3), re-evaluate the §4 framing gate, and resolve the `\TODO` headline markers in abstract/intro/conclusion. Resume the audio loop (T4.3 WavLM/emotion2vec+ → T4.4).

### 2026-06-24 — T4.3 COMPLETE: emotion2vec+ full run done (both audio reps in hand)
- **Done:** emotion2vec+ overnight run finished cleanly: **2,671×1024, all finite, 0 all-NaN**, manifest verifies OK, resume = clean no-op (mean L2 ~10.86). ~12 h as benchmarked. Committed the data artifact. **T4.3 complete** → FinCall now has three audio feature sets: WavLM-Large (2,671×1024), emotion2vec+ (2,671×1024), eGeMAPS (2,671×88). All per-call (no diarization, v1).
- **Found:** funasr/emotion2vec ran the full corpus without incident (benign ckpt-miss warnings throughout, as expected). The resumable checkpoint design proved its worth across two long overnight GPU runs (WavLM ~9 h, emotion2vec ~12 h) — progress readable live off the growing parquet, neither needed a restart. A `paper/` LaTeX scaffold was added in a parallel session (2026-06-23), orthogonal to the audio loop; T4.4's Result Table 3 will feed it.
- **Next:** **T4.4 Stage-3 audio heads → Result Table 3 + §3.5 gender-confound analysis** — the audio analogue of T3.3 (ridge/MLP on audio features, covariate ablations, DM vs Stage-1/Stage-2/HAR) **plus the identity controls** (where the provisional Path-B framing is revisited: if audio beats the floor on Δv surviving controls on ≥2/4 horizons, framing could flip toward Path A). Design call — loop will stop and ask. Then Phase-4 push + CI checkpoint.

### 2026-06-24 — T4.4 DONE: Result Table 3 + audio controls + gender → Phase 4 COMPLETE
- **Done:** `eval/stage3.py` + `features/audio/assemble.py` + `eval/audio_eval.py` + `ecvol evaluate-audio` + `report.py` Table-3 render. Result Table 3 = 1,296 rows (5 feature sets × {audio, audio+pastvol} × ridge/mlp × 3 targets × 4 horizons × 3 splits × 2 segments; DM vs persistence/HAR/Stage-1/Stage-2). Plus identity probe, §3.5 gender analysis, and a same-ticker audio shuffle. Committed Table 3 (csv/md/tex) + `audio_{probe,gender,shuffle}.csv`. 6 new tests; ruff clean. Reuses the T3.3 harness; FinCall-only. DECISIONS 2026-06-24.
- **Found (decisive for the framing gate):** (1) **Identity probe** — WavLM→ticker **76.3%** (271× chance), emotion2vec 29.4% (105×). (2) **Δv vs Stage-1** — only the **+past-vol** variants win, only on **ticker-disjoint** (ridge_wavlm_audio_pastvol τ3 +0.337 / τ7 +0.171 / τ30 +0.212); nothing on temporal. (3) **Audio shuffle (clincher)** — those ticker-disjoint gains are ~unchanged even under **global** shuffle (τ3 0.337→0.318, τ30 0.212→0.100) → WavLM features are **inert**, past-vol carries the signal (stronger than text, where global-shuffle hurt). (4) **Gender** — F0 proxy 100% coverage; low/high-pitch MSE 0.241 vs 0.242, F0↔err² corr 0.03 → no large disparity. **§4 Path A criterion not met.** A real bug caught by the end-to-end test: the Stage-2 DM reference was returned test-only but indexed with the full mask → fixed to per-segment.
- **Sources:** DESIGN §6 Stage 3, §7.3 (controls), §7.5 (confirmatory DM set), §3.5 (gender), §4 (framing gate); reuses T2.1 DM, T3.3 heads, T3.4 controls helpers.
- **Next:** **Framing gate = Path B kept PROVISIONAL** (user 2026-06-24), revisited after Phase-5 fusion + Phase-6 LLM. **Phase 4 (T4.1–T4.4) complete → Phase-4 boundary checkpoint: user pushes (~20 commits since the last push) + confirms CI green before Phase 5.** Then **Phase 5 — T5.1 fusion models + T5.2 full ablation grid (Result Table 4, the main table)**. The `paper/` scaffold stays framing-neutral; Result Table 3 now available to feed its Stage-3 section.

### 2026-06-24 — T5.1 DONE: Stage-4 fusion (RQ2: multimodal not > best unimodal)
- **Done:** `models/fusion.py` (gated fusion: per-modality PCA(64)+L2-norm gate→MLP; late-fusion stacking: meta-ridge over [text, audio, Stage-1] base preds) + `eval/stage4.py` + `ecvol evaluate-fusion` → `result_table_4_fusion.csv` (288 rows, 0 NaN). All modalities + past-vol, covariates {in,out}, DM vs persistence/Stage-1/2/3. sklearn/CPU, <5M/<2GB. Cross-attention omitted (degenerate at one-vector-per-modality). User design call: gated+stack / sklearn / all modalities. 5 new tests; ruff clean. DECISIONS 2026-06-24.
- **Found:** **RQ2 — multimodal is NOT > best unimodal.** Only 1/48 Δv-test cells beats all of Stage-1/2/3 DM-significantly (gated τ7 r2 +0.019, negligible); best fusion (stack ticker-disjoint τ3 r2 +0.436) not DM-sig vs Stage-1 (p=0.13). The stack recovers the strongest base (Stage-1/past-vol); gated fusion overfits like the unimodal embeddings. Consistent with provisional Path B. Two parallel-session artifacts committed clean: `paper/` scaffold (the user built `paper/main.pdf` — TeX toolchain now present) and `notebooks/validate_results.py` (independent target/leakage validation harness, lint-fixed).
- **Sources:** DESIGN §6 Stage 4 (gated fusion + late-fusion stacking), §7.5 (Stage-4 vs best unimodal on Δv), §4 (framing gate); reuses T3.3/T3.4 harness.
- **Next:** **T5.2 — full §7.6 ablation grid → Result Table 4 (the main paper table)**: unify Stages 0–4 (modality × covariates × split × target × horizon) with **Holm-corrected** confirmatory comparisons + `ecvol report` regeneration. Design call (table structure + Holm families) — loop will stop and ask. Closes Phase 5 → Phase-5 push + CI checkpoint.

### 2026-06-24 — Validation/gut-check session: results audited, no integrity issues; two hygiene caveats recorded
- **Done:** Extended `notebooks/validate_results.py` (validation-only, not pipeline; matplotlib installed into the venv transiently — **not** added to the committed lockfile, per the "figures at Phase 8" note in `figures_result_table_1.py`). Produces `data/results/figures/*.png` (gitignored) + `target_handcheck.csv`, and a stdout reconciliation/leakage report. Five checks: (1) **independent target reconciliation** — reimplemented the §5.3 realized-vol math from scratch in numpy directly over the raw `data/prices/*.parquet`, defining sessions from the price index itself (no `src/ecvol` calls), and compared to the stored `fincall/targets.parquet`; (2) leakage (temporal ordering + ticker-disjointness from the split CSVs); (3) target distributions; (4) Result-Table R²_OOS heatmaps + the temporal-vs-disjoint identity gap; (5) frozen-feature sanity (norms/NaNs/constant-dims) + FinBERT sentiment.
- **Found:** **Targets are correct and fully reproducible** — all **9,696 ok rows** match stored to floating-point epsilon (max|Δ|=8.9e-16, 0 unmatched); `ecvol`'s own `compute_call_targets` also re-derives JNJ/BSX/BMY exactly from current prices (so prices haven't drifted from build time). **Leakage checks pass** (FinCall+MAEC: train all before test on temporal; **0** train/test ticker overlap on ticker-disjoint). **Features clean**: BGE-M3 (2688×1024), WavLM & emotion2vec+ (2671×1024) all 0 NaN / 0 constant-dim / unimodal norm hists; FinBERT skews positive-neutral (p_pos .21 / p_neu .71), expected. Target dists are sane bells centered ~−4.1 log-RV (≈1.6% daily, ≈26% annualized), Δv centered ~0. **Process note:** my first harness pass reported huge false mismatches — a pandas index-misalignment bug in the *check itself* (`ok.apply` keeps the original non-contiguous index; `concat(axis=1)` against a reset `ok` aligned each row to a *different* row's recompute; only row 0 lined up). Fixed by resetting both indices. The check can detect real mismatch — it isn't trivially self-agreeing. **Gut-check verdict: behavior is as expected and, importantly, NOT too good** (no leakage-inflated cells). Two hygiene caveats (not data-integrity), now also in TASKS notes: **(A) the MLP heads are unreliable** — `seed_std` reaches **1.07 mean / 3.36 max R² units** (`mlp_egemaps_audio_pastvol`, Table 3); the catastrophic R²s (−26 Table 2, −77 Table 3) are seed-driven divergence, not a feature property (features are StandardScaler'd in `heads.py`, checked). Ridge/structural are deterministic and stable (GBDT MSE-std ~0.005). **(B) "Ridge/MLP (past-vol)" in Tables 2–3 ≠ HAR in Table 1**: past-vol = `[v_pre, rv_daily, rv_weekly, rv_monthly]` standardized with val-selected α (`stage2.py:26`), vs HAR's untuned OLS on the 3 RV terms — which is why Ridge(past-vol) tanks to −2.0 on temporal τ=15 where HAR sits at +0.21; they agree on the stationary ticker-disjoint split. Legit (a regime-robustness difference, arguably a finding) but the two "past-vol baselines" across tables are not comparable. Minor caveat: short-horizon R²_OOS gains are partly inflated by a *noisy* persistence baseline (persistence MSE τ=3=1.17 vs τ=7=0.42). Reassuring non-bug: Table-1 `Δv` rows are byte-identical to `level-v` for the feature-free baselines (EWMA/HAR/GARCH) but differ for the refit GBDT — the correct mathematical signature (same residual for `v` and `Δv` when the prediction doesn't depend on the target label), confirming the metric plumbing is coherent.
- **Sources:** `src/ecvol/data/targets.py` (§5.3 RV math), `src/ecvol/models/heads.py` (ridge/MLP heads, StandardScaler, val-selected α), `src/ecvol/eval/stage2.py:26` (`PASTVOL` block), committed `data/results/result_table_{1,2,3}.csv` + `data/splits/*.csv`; DESIGN §5.3, §7.3, §7.5.
- **Next:** No code change required — targets/splits/features validated. Future-work notes added to **T5.2** (MLP-head robustness: report median or retune; or demote MLP to a robustness check) and **T8.2** (paper limitations: MLP instability, the past-vol-baseline-≠-HAR distinction, noisy-short-horizon-baseline caveat). Figures available to feed the Phase-8 figures notebook. Resume T5.2 (the next backlog task).

### 2026-06-24 — T5.2 DONE: Result Table 4 (main grid) + Holm + per-year → Phase 5 COMPLETE
- **Done:** `eval/grid.py` + `ecvol grid` + `report.py write_reports4` → committed `result_table_4.csv` (720 rows) + `result_table_4.{md,tex}` + `result_table_4_peryear.csv`. Consolidates the **pre-registered canonical model per stage** (persistence/HAR/GBDT/ridge-text/ridge-audio/fusion-stack — all ridge/structural, **deliberately no MLP** given the parallel-session seed-instability finding above). Holm correction on the confirmatory Stage-k-vs-Stage-1 Δv DM tests, across 4 horizons per (stage, split). Per-year breakdown (exploratory). 3 new tests (incl. committed-artifact CI guard); 13+ green in the targeted subset; ruff clean. DECISIONS 2026-06-24.
- **Found:** 15 Δv-test cells Holm-sig vs Stage-1 — but the **7 favorable ones are all ticker-disjoint** (the identity/past-vol-confounded split per T3.4/T4.4 shuffle controls); on the regime-honest **temporal** split, text/fusion are Holm-sig **worse** than Stage-1. **Per-year is single-year** (FinCall temporal test = all 2021): there every content model underperforms persistence (level-v R²_OOS HAR −0.29 / GBDT −0.82 / text −1.09 / audio −1.66 / fusion −1.02) — the COVID train(2020)→test(2021) regime shift quantified. Net across Phase 5: neither fusion nor any unimodal content beats the floor once identity/regime are honestly controlled → reinforces provisional Path B. The main table reads off deterministic ridge canonicals, so it's not exposed to the MLP seed divergence.
- **Sources:** DESIGN §7.6 (ablation grid), §7.2 (Holm across horizons), §7.5 (confirmatory Stage-k vs Stage-1 on Δv); reuses T2.1 `holm_correction`, the T3.3/T3.4/T5.1 harness.
- **Next:** **Phase 5 (T5.1–T5.2) complete → Phase-5 boundary checkpoint: push + confirm CI before Phase 6.** Then **Phase 6 — LLM structured features** (T6.1 schema design + T6.2 constrained extraction with the **κ>0.6 human-audit gate** — the first hard human-labor blocker; T6.3 Result Table 5). The §4 framing gate is revisited a final time after Phase 6.

## 2026-06-24 — T6.1 LLM feature schema + reading/labeling tooling (Phase 6 entry; scaffolding, then paused for human)
- **Scope:** Phase-5 boundary cleared (user pushed + confirmed CI green). Entered Phase 6, built the T6.1 engineering scaffolding, then hit the first hard human-labor blocker and paused.
- **Done:**
  - `features/llm/schema.py` — `SectionFeatures` pydantic schema (per `(call, section)`): guidance_direction {raise/maintain/lower/none}, hedging_intensity 0–4, qa_evasiveness 0–4 (Q&A-only), surprise_mentions 0–20 count, analyst_tone 0–4 (Q&A-only), free-text evidence. `extra="forbid"`; `SECTION_FIELDS`/`applicable_fields` encode the prepared-vs-Q&A applicability; `CATEGORICAL/ORDINAL/COUNT_FIELDS` groupings drive the κ-audit.
  - `docs/llm_feature_rubric.md` (v1) — per-field anchors, leakage rule, the T6.1 two-pass + T6.2 κ>0.6 acceptance protocol, two-rater workflow.
  - `features/llm/prompts.py` — section-aware system+user prompt drafts, `PROMPT_VERSION="v1"` (part of the T6.2 cache key); prepared-remarks prompt omits Q&A-only anchors and floors them to 0.
  - `features/llm/reading.py` + `ecvol featurize llm-reading-pack` — samples N **train-split-only** calls (leakage assertion), renders readable transcripts to `data/fincall/llm_reading/*.md` (gitignored), emits a blank labeling sheet `data/coverage/fincall_llm_label_sheet.csv` (one row per call×section; Q&A-only cells pre-marked "NA" in prepared remarks).
  - `tests/test_llm_schema.py` (7 tests) — schema bounds/extra-forbid/bad-category, per-section applicability, prompt versioning + prepared-omits-Q&A-anchors, and the **train-only leakage guard** + determinism + size guard on the sampler.
  - Generated the real pack: 20 FinCall train calls (seed 0), 39 sheet rows (20 qa + 19 prepared — one call had no detected prepared section). Transcripts render clean (role-labeled turns, section split).
- **Found:** pandas coerces the literal "NA" sheet marker to NaN on read → readers must use `keep_default_na=False` (documented in `reading.py`, enforced in the test). Field set was already pinned by TASKS/DESIGN, so building the schema is operationalization, not a fresh design guess; the genuine design validation is exactly the human two-pass the acceptance test demands.
- **Gate:** `ruff check .` clean · `ruff format --check .` clean (96 files) · `pytest -q` **231 passed** (full suite). Local commit made; not pushed (phase-internal — user pushes at the Phase-6 boundary).
- **Sources:** TASKS T6.1 / TX1 (leakage scope), DESIGN §6 (Stage 5 field set), existing T3.1 sectioning (chunks.parquet) + T0.2 split CSVs.
- **Next:** **Paused — T6.1 is `[!]` (human blocker).** User reads the 20 calls, fills `data/coverage/fincall_llm_label_sheet.csv` (second rater on the 10-call subset for two-pass agreement), and either signs off the v1 schema or requests rubric edits. On sign-off the loop builds **T6.2** (Qwen2.5-7B-Instruct 4-bit + Outlines constrained extraction, resumable, ETA-gated) against the frozen schema+`PROMPT_VERSION`, then the **κ>0.6 50-call audit** gates corpus scale. All of Phase 6 + Phase 7 T7.1 are human-gated, so the loop stops here rather than skipping ahead.

## 2026-06-24 — T6.2 LLM extraction pipeline + κ-audit tooling + OSC cloud package; local probe → cloud route (Phase 6)
- **Scope:** built the full T6.2 engineering (extraction, audit, cloud burst), ran the local ETA probe to decide local-vs-OSC, hit a feasibility wall locally → cloud.
- **Done:**
  - `features/llm/extract.py` — `iter_section_inputs` (per call×section text from chunks), engine abstraction: `TransformersOutlinesEngine` (Windows; bitsandbytes nf4 4-bit) + `VLLMEngine` (OSC Linux), Outlines 1.3 (`from_transformers`/`Generator`) JSON-schema-constrained **greedy** decoding. `build_llm` resumable (per-model `llm_features__{slug}.parquet` is the resume store; skip done call×section), deterministic, manifest-tracked.
  - `features/llm/audit.py` + `ecvol llm-kappa` — per-field κ (Cohen / linear-weighted / binarized-count), `passes_gate(0.6)`, `kappa_matrix` (model-vs-model cross-validation). `reading.sample_train_calls` factored out (train-only, leakage-asserted) and shared by reading pack / audit sample / probe.
  - CLI: `featurize llm` (+ `--max-model-len` for vLLM), `featurize llm-eta` (probe), `featurize llm-audit-sample` (50-call train sheet). Regenerated the FinCall label sheet at n=50.
  - deps: `outlines` (→1.3), `bitsandbytes`, `accelerate` in gpu group; Linux-marked `cloud` group (`vllm`) for the OSC container; CI stays dev-only/torch-free.
  - `cloud/osc/`: Apptainer def (vLLM base + Outlines + ecvol), parameterized resumable `extract.sbatch` (one model/submission), `stage.sh` (offline weight pre-stage), README (workflow, config block, model panel, cost/ETA).
  - tests: `test_llm_extract` (mock engine: assembly, resume, determinism, sampler), `test_llm_audit` (synthetic κ + gate). Full suite **242 passed**, ruff clean.
- **Found (the decisive result):** local Qwen2.5-7B-4bit **CUDA-OOMs** on a single long section's prefill on the 16 GB card (O(seq²) SDPA, no Windows flash-attn); 0 sections completed. Section lengths: FinCall median 5,448 / **max 61,141** tokens; MAEC median 1,840 / max 11,137; ~38M tokens over 9,443 sections (`data/coverage/llm_probe_report.md`). → **Local is infeasible at full context; corpus runs on OSC** (vLLM, ~couple GPU-hrs/7B, well under $1000). The 61k max exceeds Qwen2.5's 32k native context → a truncate-vs-YaRN design call deferred to cloud-run time (HANDOFF).
- **Decisions:** DECISIONS 2026-06-24 — multi-model exploration panel (7B→32B, cross-validated), $1000 OSC envelope, >20h routing rule, audit-matches-corpus reproducibility rule, and the routing-outcome addendum (cloud by feasibility).
- **Sources:** outlines 1.3 API (`from_transformers`, `Generator`); Qwen2.5 context (32k native / 128k YaRN); existing T3.1 chunks + T0.2 splits; resumable audio extractor pattern (`features/audio/wavlm.py`).
- **Gate:** ruff clean · 242 pytest pass · 3 local commits (pipeline 23320ae, cloud+spend 5014209, + this governance commit).
- **Next (loop paused — human + Phase-6 boundary):** (1) **OSC access** — confirm allocation/account code + cluster (Ascend A100 / Cardinal H100), build the sif, `stage.sh` the panel weights; (2) **T6.1 schema sign-off + 50-call human labels** (the κ gate; extraction for the audit-50 runs on OSC alongside the corpus); (3) resolve the >32k context policy. Then per-model corpus extraction → `ecvol llm-kappa` per model → T6.3 (Stage-5 eval per model, masking ablation, Result Table 5). Phase-6 push/CI checkpoint pending too.


## 2026-06-29 — T6.1 v2 schema sign-off + rater-1 κ-audit ingest (T6.1/T6.2)

- **Scope:** processed the first human rating set (`ingest/Ratings_1.xlsx`), revised the LLM feature schema to v2 from rater feedback + literature, and signed off the frozen schema for the OSC corpus run. User on an OSC time crunch; one rating set in hand, second still pending.
- **Done:**
  - **Rater-1 ingest (reproducible):** new `ecvol featurize llm-ingest-ratings` (`features/llm/ratings.py`, stdlib `zipfile`+`xml` xlsx parse — no new dep) → `data/coverage/fincall_llm_labels_rater1.csv`. Validates against the frozen 50-call audit sample (exact `(call_id, section)` match) + schema ranges. Ran clean: 97 rows / 50 calls, all in range. 6 tests.
  - **Single-rater go/no-go (decision):** the κ>0.6 OSC gate runs on this one rater; second-rater IAA deferred to pre-publication. Borderline κ (≈0.45–0.6) re-blocks on rater 2. DECISIONS 2026-06-29.
  - **Schema → v2 (superset):** added two **exploratory** fields — `management_optimism` (over-selling axis the rater flagged) + `quantitative_specificity` (numeral intensity) — extracted in the same OSC pass, no κ-gate until labeled. κ-gate narrowed to a **confirmatory core** (`guidance_direction`/`hedging_intensity`/`surprise_mentions`), fixed pre-extraction on label variance (not model κ). Weak labeled fields (`qa_evasiveness`, `analyst_tone`) reported-not-gated. `PROMPT_VERSION` v1→v2. Threaded through schema/prompts/extract/audit/cli + rubric.
  - **v2 signed off** (user); T6.1 → `[x]`, schema frozen.
- **Found:**
  - Rater left the workbook's **Schema Feedback sheet blank**, but gave verbal feedback (via user): management rarely evasive — over-answers to look good; analysts mostly agreeable except on big bad news. Matches the label distributions: `qa_evasiveness` ∈ {0–2} (near-degenerate), `analyst_tone` compressed to 2–3. → the two v1 Q&A-only fields are the weak ones; the interesting variance is a promotional/optimism axis.
  - **Literature (answer to "why not domain-specific features?"):** the open-data principle excludes guidance-vs-consensus surprise (needs licensed IBES); but the numeral-aware line — NAM/ECNum [R30], NumHTML [R31], GNAVol [R32] — shows quantitative content carries volatility signal and is text-only + auditable → motivates `quantitative_specificity`. Counterweight: "Same Company, Same Signal" [R14] + this project's own inert-beyond-past-vol results temper expectations.
- **Sources:** DESIGN §3 lineage + §13 refs (R8 ECC Analyzer, R30/R31/R32 numeral-aware, R14 identity); rater-1 workbook; existing audit/extract scaffolding.
- **Gate:** ruff clean · ruff format clean · **253 pytest pass** (+11 this session). Not yet pushed (local main).
- **Next:** T6.2 corpus extraction on OSC against frozen v2 (remaining blockers operational: OSC allocation + >32k-token context policy, HANDOFF). Then `ecvol llm-kappa` (confirmatory core) per model → T6.3. Second rater → pre-publication IAA.


## 2026-07-04 — T6.2 compute rerouted OSC → Colab Pro+ (T6.2)

- **Scope:** the booked OSC allocation lapsed (deadline passed) and its funds are gone, so the corpus-extraction compute was rerouted. Did a pro/con of paid inference API vs Colab; user chose **Colab Pro+** (flat subscription, no paid-API budget, max monthly compute units, no deadline). Prepared the pipeline for testing on that route.
- **Done:**
  - **Route decision + log:** DECISIONS 2026-07-04 — reroute to Colab Pro+; the 2026-06-24 $1000 OSC spend approval is **superseded/void (never used, $0 spent)**. Same `--engine vllm` + Outlines + YaRN-to-65536 engine (Colab, like OSC, is Linux+CUDA where vLLM runs), so the pipeline barely changes.
  - **Only new code:** `featurize llm --audit-sample` (+ `--audit-n`/`--audit-seed`) restricts a run to the 50 train-only κ-gate calls via `sample_train_calls`, mutually exclusive with `--limit`. This makes the audit-matches-corpus rule (DECISIONS 2026-06-24) hold on *any* engine — the κ-gate is now extracted through the same vLLM engine as the corpus, not just because the audit calls happen to be a FinCall subset. 2 tests (`test_build_llm_audit_sample_restricts_to_sample`, CLI mutual-exclusivity guard).
  - **`cloud/colab/` package** (mirrors `cloud/osc/`): `README.md`, `RUNBOOK.md` (from-scratch + Colab gotcha index), idempotent `setup.sh` (per-session vLLM+Outlines+ecvol install, no container/offline-staging since the Colab VM has internet), `run.ipynb` (mount Drive → setup → smoke → κ-gate → corpus). `cloud/osc/` retained as reference.
  - **Docs:** TASKS T6.2 note; HANDOFF (OSC Active entry → Resolved/superseded, new Colab Active entry with the operational steps, rater-1 entry de-OSC'd, cloud-burst FYI annotated); this journal entry.
- **Found:**
  - Corpus scale reconfirmed for costing: **9,443 (call,section) units**, ~38M input tokens (`llm_probe_report.md`) — trivially small, which is why a paid open-weights API would have been ~$30–80 for the full panel; rejected only because it reintroduces a paid budget the user declined (design was already open-weights-clean since the served weights are open).
  - **Colab realities handled:** (1) VM disk is ephemeral → repo + `data/` outputs live on Drive; the per-model parquet resume store makes session timeouts a non-issue. (2) GPU class not guaranteed — A100-**40GB** (vs OSC 80GB) fits 7B/8B-fp16 @65k; **32B must be AWQ 4-bit**; **72B dropped** on Colab. Never silently lower `--max-model-len` (would desync audit from corpus).
- **Sources:** measured corpus stats from `data/{fincall,maec}/chunks.parquet`; `cloud/osc/` package (mirrored); Colab Pro+ GPU tiers (A100-40/L4-24/T4-16); vLLM/Outlines/YaRN paths already built (DECISIONS 2026-06-24/29).
- **Gate:** ruff check · ruff format · pytest — see the commit; `cloud/` is docs+scripts (not import-tested), the CLI change is covered by the 2 new tests.
- **Next (human, on Colab):** put the repo + the two `chunks.parquet` on Drive; attach an A100/L4 runtime; run `cloud/colab/run.ipynb` — smoke (3 calls) → `--audit-sample` + `ecvol llm-kappa` (must clear κ>0.6) → full FinCall+MAEC corpus (panel: 7B → 32B-AWQ → Llama-3.1-8B). Then per-model κ → T6.3 (Result Table 5). Phase-6 push/CI checkpoint still pending.

## 2026-07-19 — Advisor-meeting slide deck rebuilt (briefing/, untracked)

- **Scope:** restructured the meeting deck (`briefing/slides_base.pptx`, from the 2026-07 briefing doc) for an academic-advisor review, per user direction: no criticism of prior work (neutral concept overview instead), no internal-process content (phases/commits/CI), fixed section order (previous work → outside work → claims+headline → done → results+meaning → next steps+benefit → timeline → asks), tighter humanized prose, ASCII typography, diagrams/equations over text, niche terms defined (HAR-RV, R²_OOS, constrained decoding, Cohen's kappa, LLM-knowledge leakage).
- **Done:** `briefing/build_slides.py` (python-pptx, run via `uv run --with python-pptx` — not added to the lockfile) generates `briefing/slides_meeting.pptx`, 14 slides, native editable shapes: pipeline + LLM-stage flow diagrams, transcript-swap control diagram, RV/target + HAR-RV + R²_OOS equation cards with true sub/superscripts, 4-week Gantt (Jul 20 – Aug 16, user-directed fast-track assuming cloud burst + dedicated hours). `slides_base.pptx` kept as backup.
- **Verified:** automated re-extraction check (14 slides; zero em/en-dashes, middots, arrows, curly quotes in prose; zero internal-jargon terms) + full visual render via PowerPoint COM export → PNG, all slides inspected for overflow/overlap.
- **Not a pipeline change:** no `src/` or docs-contract edits; no gate run needed (briefing/ is untracked presentation material).
- **Next:** user presents; timeline in deck implies immediate start of the Colab extraction run (see 2026-07-04 entry).

## 2026-07-19 (later) — Deck v2: stretch-goal corpus slide, asks slide dropped

- **Scope:** user feedback round on `briefing/slides_meeting.pptx`. Added the one stretch goal they want presented — building our own modern earnings-call corpus — and removed the asks slide (read as redundant; nothing is really being asked).
- **Done:** new slide 13 "In parallel: the modern corpus this field is missing" (full-benchmark ambition: thousands of 2024-2026 calls, transcripts + audio, clean identity + exact call timestamps from day one; acquisition-flow diagram; framed as flagship corpus of the released benchmark + evergreen post-cutoff lookahead data). Timeline slide gained a green "Parallel" Gantt track spanning all four weeks (corpus build; feeds week-3 lookahead calls) with row spacing compressed to fit. Deck still 14 slides; still generated by `build_slides.py`; typography/jargon check + PowerPoint COM render re-verified.
- **Note:** the stretch goal is deck-only for now — not yet a DESIGN.md/TASKS.md change; if pursued it needs a DECISIONS.md entry and task IDs (natural extension of Phase 7's D4 acquisition route).
- **Update (same session):** the stretch goal is no longer deck-only — user directed capturing it now; DECISIONS 2026-07-19 entry added (amends the 2026-06-12 "existing datasets first" decision; task IDs + DESIGN amendment still owed once scoped).

## 2026-07-19 (later still) — Reference existence audit (paper + slides)

- **Scope:** user asked to verify every referenced paper in the slides and the paper exists, with saved evidence.
- **Done:** audited all 40 citations (DESIGN §13 R1–R32 + refs.bib extras) via five parallel web-verification agents (fetch cited URL, confirm title/authors/venue; bot-blocked publishers cross-confirmed via dblp/RePEc/institutional mirrors). Local check: all 33 refs.bib keys cited, no dangling \cite keys. Evidence: `paper/reference_audit.md`.
- **Found:** **all 40 references exist — none fabricated.** 5 citation errata (papers real, metadata wrong): R15 wrong title ("Detecting Lookahead Bias in LLM Forecasts") + year 2025; R16 DatedGPT year 2026; R12 Sound of Risk year 2025 + truncated title; B1 FinCall-Surprise bib title wrong ("A Large Scale Multi-modal Benchmark for Earning Surprise Prediction", 2025); R4 VolTAGE title truncated. Notes: R18's 14% figure not in abstract (page-reference when cited); refs.bib BGE entry cites bge-large-en-v1.5 while pipeline uses bge-m3. Slides contradict nothing.
- **Next:** apply the 5 errata to DESIGN §13 + refs.bib (not yet done — DESIGN edits await user OK per contract discipline).

## 2026-07-19 (later still 2) — Novelty & frontier scan (deep-research workflow)

- **Scope:** user asked whether the work is still unique, whether anything has beaten us to the punch, and what new work we must compare against. Ran the deep-research workflow (5 angles, 101 agents, 19 sources fetched, 25 claims 3-vote adversarially verified: 24 confirmed / 1 refuted). Report: `paper/novelty_scan_2026-07.md`.
- **Found (verdicts):** Q1 identity-controlled re-examination CLEAR (2412.18029 still the sole prior, no follow-ups); Q2 LLM structured features MUST-CITE OVERLAP not scooped (ECC Analyzer verified: no schema/no kappa/no identity controls; new: KPI-extraction arXiv 2605.03147, EvasionBench 2601.09142); Q3 lookahead study MUST-CITE OVERLAP not scooped but the line is heating up (LAP 2512.23847 v2, DatedGPT 2603.11838, Look-Ahead-Bench 2601.13770, Memorization Problem 2504.14765, MemGuard-Alpha 2603.26797; nuance: ECB-presser paper 2508.13635 did a post-cutoff check on *rates* vol → scope our claim to earnings-call volatility); Q4 benchmark/corpus CLEAR at moderate certainty (MiMIC has no audio; nothing leakage-proof multimodal).
- **Key comparison target:** The Sound of Risk (R12) claims 43.8% OOS RV variance from text+acoustics with no identity controls — the strongest recent positive result our controls challenge; engage directly in the discussion.
- **Refuted claim worth remembering:** EvasionBench's κ=0.835 is inter-LLM-annotator agreement, NOT a human audit — never cite it as kappa-gate prior art.
- **Next:** fold the 6 must-cites into DESIGN §13/refs.bib during Phase-8 writing; re-scan before submission; keep the 4-week timeline (stage-(b) novelty is time-sensitive).

## 2026-07-19 (later still 3) — Novelty scan folded into contract + paper

- **Scope:** user directed folding the scan into the project. All new-reference titles/authors re-verified against live arXiv pages first (audit discipline).
- **Done:** DESIGN §13 + [R33]–[R40]; §3.1 masking-doesn't-help corroboration (R33); §3.2 frontier update + Phase-7 claim scoped to earnings-call-volatility literature + optional point-in-time-LLM arm (R39); §3.0 R12 marked primary comparison target. refs.bib +7 entries (memorization2025, lookaheadbench2026, evasionbench2026, kpi2026, memguard2026, ecbvol2025, chronollm2025); related-work pitfalls paragraph rewritten around the now-active lookahead line; structured-extraction neighbors (EvasionBench, KPI paper) added to the lineage paragraph; discussion engages Sound of Risk's 43.8% claim directly. TASKS T7.2/T8.2 notes. DECISIONS 2026-07-19 entry.
- **Next:** unchanged — Colab extraction launch; pre-submission re-scan reminder now lives in T8.2.

## 2026-08-09 — T6.2 local extraction route built; κ-gate FAILED (corpus not started)

- **Scope:** user freed the machine for ~19 h (hard stop 19:30 EDT) and asked to start the next stage locally tonight, with progress migratable to Colab later. Four design calls answered up front (engine, migration semantics, gate-failure behaviour, scope order); all four taken as recommended.
- **Done — the local wall came down.** The 2026-06-24 "local is infeasible" finding turned out to be **engine-specific, not hardware-specific**: `transformers`+bitsandbytes OOMed on long-section prefill, but llama.cpp chunks the prefill and ships a CUDA flash-attention kernel that builds for Blackwell sm_120. New `--engine llamacpp` (`LlamaCppServerEngine`: `llama-server` subprocess driven over HTTP with `response_format: json_schema`, chosen over Python bindings because there is no CUDA toolchain here to compile them), plus `build_llm(deadline=)` for a clean wall-clock stop and a `cloud/local/run_corpus.sh` supervisor. Same frozen v2 schema/prompt, same YaRN-65536 policy, greedy. Qwen2.5-7B-Q4_K_M runs whole 65k-token sections in **~9 GB VRAM at 4.63 s/section** → FinCall ~6.6 h, FinCall+MAEC ~12 h. Q8_0 also fits (13.5/16.3 GB, 6.0 s/section). Model pinned to HF rev `8911e8a4…`; quant is keyed into the parquet name so Colab can resume the identical rows.
- **Found — three real defects, two of them silent and two of them NOT mine.** (1) pydantic omits defaulted fields from `required`, so the constrained grammar could close the object early: the model skipped `evidence` on **every single section**, and a skipped `management_optimism`/`quantitative_specificity` would have been backfilled by its default `0` — a non-rating stored as, and indistinguishable from, a real `0` rating. (2) With evidence actually being generated and nothing bounding it, the model quoted until it hit the decode cap and emitted JSON truncated mid-string (unparseable); llama.cpp rejects `maxLength`, so the bound is now an anchored regex repeat, and `MAX_NEW_TOKENS` 640→1024. (3) The CLI died with `UnicodeEncodeError` the instant its output was redirected to a log file — it prints "κ", stdout falls back to cp1252 when not a terminal. **(1) and (2) are schema-level and therefore latent in the Outlines/vLLM path too**; not patched there because there is no vLLM on this machine to verify against — documented instead, with the exact fix.
- **Found — the κ-gate FAILED, decisively.** On the 97 audit rows: `guidance_direction` **0.165**, `hedging_intensity` **−0.050**, `surprise_mentions` **0.222** (confirmatory core; gate is >0.6). Verified it is not a plumbing artefact — 97/97 label join, section text spot-checked as correct prose in the correct section, 100% schema-valid rows — and not a scale offset (shifting the model's ordinal by ±1/±2 does not rescue κ; raw agreement peaks at 40%). Failure modes: model near-constant on `analyst_tone` (47/50 Q&A sections rated "2"), independent of the human on hedging (for human=0 it says 1–2, for human=1 it also says 1–2), under-detects surprise (misses 26 of 36 positives), and defaults `guidance_direction` to `none` (catches 12 of 41 human `raise`). Per the user's standing instruction the corpus run was **not** started; parquet holds 108 rows (11 smoke + 97 audit) and MAEC is untouched.
- **Found — two negative controls, run on the otherwise-idle GPU to make the diagnosis actionable.** *Prompt anchoring is not the cause*: a variant adding explicit decision rules for exactly the three failure modes moved κ by +0.010 / +0.037 / 0.000 (and reproduced the frozen-v2 κs to 3 dp, confirming determinism). *Quantization is not the cause*: Q8_0 scored 0.187 / −0.003 / 0.308 — uniformly better, as expected, but ~10% of the gap, not the gap. What remains is capability-vs-validity, and **a second rater distinguishes them cheaply**: humans agreeing with each other ⇒ model problem (scale up); humans disagreeing comparably ⇒ schema problem (no model fixes it). Report: `data/coverage/llm_kappa_gate_report.md`.
- **Note on the labels, for the user's eye:** rater 1 marked 85 of 97 sections as hedging 0–1, i.e. "essentially no hedging" on most earnings calls, while the model saw moderate hedging nearly everywhere. The rubric says "density of hedging/uncertainty language" without specifying density *relative to what*. That ambiguity may be doing real work in this result.
- **Sources:** llama.cpp release b10331 win-cuda-13.3 (CUDA ≥12.8 is what adds sm_120 Blackwell targets); GGUF `bartowski/Qwen2.5-7B-Instruct-GGUF` @ `8911e8a47f92bac19d6f5c64a2e2095bd2f7d031` (Q4_K_M + Q8_0).
- **Next:** blocked on the user's call (HANDOFF 2026-08-09) — recommended order: second rater → then bigger model or rubric revisit depending on what it says. Nothing is compute-blocked: an audit+gate is ~10 min per model locally, the corpus ~12 h once something passes.

## 2026-08-09 (later) — T6.2 diagnosis: four null controls, and the arithmetic re-done with CIs

- **Scope:** user asked (a) whether the κ failure might be mere rater disagreement, (b) how it sits against the literature and what the paper actually needs, (c) how to course-correct, and (d) whether the paper trail is adequate; then set me on everything not needing them while they collect a second rating set.
- **Done — unblocked the human side.** We could *read* a rater workbook but never *write* one, which is why rater 1's had to be hand-made; and the obvious shortcut (save the blank CSV as .xlsx) fails on the first row, because the ingester maps `prepared remarks`/`qa` and not the canonical `prepared_remarks`. New `ecvol featurize llm-rating-workbook` emits a ready-to-fill `.xlsx` derived from the frozen label sheet (so rated rows cannot drift from the audit sample), shuffling **call order** while keeping a call's sections adjacent — a second pass must not walk the original sequence or the number measures recall, not reliability. `ingest_ratings(allow_subset=)` now accepts a strict subset so a blinded partial re-rate is ingestable; **extra** rows stay fatal. Also fixed the CLI accepting `--sheet-name` and silently dropping it. Delivered `ingest/Ratings_2.xlsx` (50 calls) + `Ratings_2_selfrerate20.xlsx` (20).
- **Done — closed three paper-trail gaps** found by auditing rather than assuming. (1) Feature rows recorded model/revision/prompt but **not** the decoding settings that determine their content (context, YaRN, decode budget, evidence bound, quant file) — and this session changed exactly those mid-development, so "all 108 rows share a config" rested on my say-so. `featurize llm` now appends a `<features>.run.json` sidecar per invocation (config + digest + git SHA + env) and warns loudly if a parquet ends up mixing digests. (2) The controls lived in scratch files that get deleted; promoted to `notebooks/` (`llm_kappa_diagnostics.py` regenerates every non-headline number in the report). (3) The analysis itself lived only in chat; written into the gate report.
- **Found — four independent controls, all null.** Prompt anchoring: +0.010/+0.037/0.000. Quantization (Q8_0): CIs span zero. Few-shot worked examples (10 exemplar calls, scored on the held-out 40): **the one non-null result** — `hedging_intensity` +0.092 [+0.029,+0.181], CI excluding zero, every other field noise. **Model scale (14B vs 7B, same family/quant/prompt, both re-run under one identical config because 14B cannot fit the frozen 65k on 16 GB): every field's difference CI spans zero** — scale reshuffles which fields score better rather than improving them, and 14B fails the gate on its own terms (upper bounds 0.263/0.030/0.456). Four levers, one measurable effect, against a ~0.4 κ gap — and that effect lands on exactly the field whose written anchor fails to specify a baseline, which is what the instrument hypothesis predicts.
- **Found — I had been over-reading point estimates, and corrected it.** κ here comes from 97 rows (50 for Q&A-only fields), SE ≈ 0.1. `notebooks/llm_kappa_uncertainty.py` bootstraps by resampling **calls** (a call's two sections are correlated), paired across arms. Two opposite consequences: the **gate result is robust** — every confirmatory field's 95% upper bound is far below 0.6 (guidance [0.064,0.271], hedging [−0.124,0.033], surprise [0.054,0.378]), so κ>0.6 is *ruled out at 95%* rather than merely missed, which is the claim for the paper; but the **control deltas are not** — "Q8_0 is uniformly a little better" was noise, and the anchoring deltas get the same caveat (few-shot's hedging effect survived bootstrapping; its other fields did not). Corollary now in the report: a 50-call audit cannot distinguish interventions of the size these levers produce.
- **Found — incidental and untested:** the 7B scored higher at 20 480/no-YaRN than at 65 536/YaRN on guidance (0.244 vs 0.165) and evasiveness (0.249 vs 0.108) while dropping on surprise. Every audit section fits whole in both, so the only functional difference is YaRN rope scaling. Same magnitude as the noise floor → **suggestive only**, but if it survives a proper test it would mean the frozen "extend, don't truncate" policy (DECISIONS 2026-06-29) buys long-tail coverage with quality on the ~99% of short sections. Flagged for test before any corpus run; **not** acted on.
- **Found — the literature framing matters more than the number.** ECC Analyzer (R8, closest prior, verified in the July scan) has *no human validation of extraction quality at all*, and EvasionBench's κ=0.835 is inter-LLM, not human (DESIGN §13 already flags this). So this is not a bar the field clears and we missed — we are the only ones measuring it. That makes the failure the natural third leg of the project's thesis (identity → audio-inert → extraction-invalid). But it also forecloses a shortcut: RQ3 sells "explicit, **auditable** semantics", so a column that fails its audit may be reported with its validity κ attached and may not be called *hedging*.
- **Next:** blocked on the second rating set (user, in progress). Evidence now favours **rubric redesign** over "go bigger" — behaviourally anchored scales, coarser levels, retire/re-scope `analyst_tone`. A fifth path exists and is a decision not an experiment: accept the failure as the result, run the corpus, and report extraction invalidity as a headline finding (needs a DECISIONS entry; should wait for rater 2).

## 2026-08-13 — Exploration: FinCall-Surprise collection methodology + modern-successor design

- **Scope:** user paused the T6.2 queue to revisit building our own modern dataset: analyze how FinCall-Surprise was collected, find improvements. Four constraints fixed up front via Q&A: new benchmark contribution (train-scale, doubles as Phase-7 holdout), audio+transcript, fully publicly redistributable, zero-cost (scraping + volunteer collectors). Analysis only — no DESIGN/TASKS changes; promotion needs a DECISIONS entry.
- **Done:** four parallel research passes (paper, data repo, 2026 source landscape, our own Phases-1–6 defect record) synthesized into `docs/fincall_methodology_and_successor.md` — reconstructed methodology, defect inventory with local numbers, preserve-list, successor design ("ecvol-live"), defect→fix table, volunteer protocol, pilot proposal, open design calls.
- **Found — FinCall's methodology is unreplicable as published.** Audio came from EarningsCast (dead: HTTP 410 site-wide, re-verified today — a survey subagent claimed it alive; direct curl refuted that), transcripts primarily from Seeking Alpha (ToS-blocked), no collection code released despite the paper claiming it, no attrition accounting (~4,000-firm pool → 2,688 calls unexplained), IBES label unreproducible (licensed source + withheld identity join). arXiv v1 and the ACL camera-ready state *different* sources with no erratum. Apache-2.0 over scraped third-party content is license-washing.
- **Found — new corpus-level defects measured on our mirror:** every MP3 is a uniform **40 kbps transcode** (never previously called out; all acoustic features sit on compression-damaged audio); 32.4% of reconstructed dates come from PDF creation stamps; 17 audio-less calls ship the literal placeholder `"more ppt"`; 456 surplus unreferenced MP3s.
- **Found — a legally grounded fully-public path exists:** *Swatch Group v. Bloomberg* (2d Cir. 2014) held redistribution of an earnings-call recording fair use (factual work, no licensing market); Earnings-21/25 (Rev.ai) provide the release template (CC-BY transcripts/metadata, audio redistributed with provider-terms disclaimer + takedown policy). Whisper self-transcription removes the transcript-copyright problem entirely.
- **Found — the binding constraint is time, not money.** Free-audio aggregators are gone (EarningsCast dead; earningscall.biz audio = $129/mo, free tier = 2 tickers); IR webcast replays decay in 30–90 days; free calendars cover ~1 month. ⇒ backfill of 2023–2025 audio is essentially impossible at zero cost; the corpus must be **forward-collected, rolling, per season** — and the Q2 2026 replay window is open now.
- **Found — Earnings25 (Zenodo, CC-BY, ~500 Q4-2025 S&P 500 calls, 498 h) is a usable seed**: instant post-cutoff Phase-7 material for the Qwen2.5 stack, WER reference for our ASR, QC cross-check. They disclose neither audio source nor scripts — releasing ours is the differentiator.
- **Sources:** arXiv 2510.03965 + ACL 2026.acl-long.610; github.com/Tizzzzy/FinCall-Surprise; Swatch v. Bloomberg 12-2412 (2d Cir. 2014); arXiv 2607.23813 (Earnings25) + Zenodo 10.5281/zenodo.18762168; revdotcom/speech-datasets; earningscall.biz/api-pricing; full URL list in the doc §7.
- **Next:** user decides the doc's §6 open calls — foremost the time-sensitive 50-call current-season pilot go/no-go, universe breadth, and whether to email the FinCall authors (deferred-not-rejected, DECISIONS 2026-06-12). T6.2 remains blocked on rater 2 in parallel.

## 2026-08-13 (later) — TX4 green-lit; discovery built + run; 50-call pilot sample drawn

- **Scope:** user answered the successor doc's §6 open calls: pilot **go** (capture-first), pilot universe **mixed ~35 S&P 500 + ~15 S&P 400** (so the season-scale breadth call is made from measured per-stratum capture rates), author email **still deferred**; volunteer/naming calls deferred to season scale. DECISIONS 2026-08-13 + new exploration task TX4 (commit cccfdbd).
- **Done:** `src/ecvol/collect/discovery.py` + `ecvol collect discover|sample` (+6 tests, no-network). Universe snapshot: Wikipedia S&P 500/400 constituent lists (raw HTML cached + manifested, CC-BY-SA) → 903 tickers (503+400, tickers disjoint), CIK joined from the SEC table T1.4 already caches — 1 miss (ECHO/EchoStar, the known 2024–26-M&A-wave gap from FinCall identity work). Calendar sweep: Nasdaq daily earnings calendar 2026-07-01..2026-08-13, one cached JSON per day (offline re-runs) → **806 candidate calls** (444 sp500 / 362 sp400) — squarely inside the doc's 700–900/season estimate. Seeded stratified sample (seed 20260813): **50 calls, 35/15, 2026-07-16..2026-08-12, 11 GICS sectors** → `data/live/pilot_sample.csv`.
- **Found:** api.nasdaq.com silently hangs (read-timeout, no error) without full browser-like headers — descriptive-UA etiquette works for Wikipedia/SEC but not there; headers documented in the module. Wikipedia header cells contain line breaks (`GICS<br>Sector`) — parse joins with spaces. Nasdaq's `time` field is `time-not-supplied` for 802/806 past-date rows → BMO/AMC and exact call datetimes must come from the 8-K/press release at capture time, as the design already assumed. **Replay-decay pressure is measured, not hypothetical:** the oldest sampled call (PLD 2026-07-16) reaches 30-day replay age ~2026-08-15 — capture must start oldest-first, now.
- **Sources:** en.wikipedia.org/wiki/List_of_S%26P_500_companies + _400_ (snapshot 2026-08-13); api.nasdaq.com/api/calendar/earnings; SEC company_tickers.json (cached).
- **Next:** TX4 capture stage — per-call replay-URL + datetime location (EDGAR 8-K press releases as source of record), then direct-MP3/HLS capture oldest-first with reason codes for the walled remainder.

## 2026-08-19 — T8.2: paper draft updated with all post-scaffold results (Phases 4–6)

- **Scope:** user asked to fold all new work into the LaTeX draft. The scaffold (2026-06-24, dcfbfbf) was written through Phase 3 with `\pending{}` stubs for audio/fusion/LLM/lookahead; since then Phase 4 (Result Table 3), Phase 5 (Result Table 4 + grid), T6.1 v2, the T6.2 κ-gate failure, and TX4 have all landed.
- **Done — three new curated tables**, cells copied from committed artifacts: `tables/audio.tex` (Stage-3 ridge heads, FinCall Δv test, both splits, **plus the audio-shuffle block** — real 0.337/0.171/0.111/0.212 vs global-shuffle 0.318/0.115/0.093/0.100 on ticker-disjoint, the "audio is inert" clincher); `tables/grid.tex` (canonical S0–S4 ladder from `result_table_4.csv` with **signed** Holm markers — * better / † worse than Stage-1, since the two-sided p<0.05 cells split 2 favorable / 4 unfavorable on FinCall Δv test); `tables/llm.tex` (κ-gate: 0.165/−0.050/0.222 with call-level-bootstrap 95% CIs, upper bounds ≪ 0.6).
- **Done — sections rewritten:** results gains three real subsections (audio incl. WavLM 76.3%-identity-probe + F0 gender analysis; fusion/grid incl. RQ2-negative and the per-year-2021 collapse; LLM incl. the four null capability controls and the form-vs-content-validity point), lookahead stays the only `\pending{}`. Methods Stage 3–5 describe what was actually built (30 s windows; gated fusion + stacking, cross-attention omitted; llama.cpp grammar decoding + YaRN-65k + audit-matches-corpus rule) and note the ridge-canonical/MLP-instability policy. Discussion: five-statement evidence summary, "engaging the strongest positive claims" (Sound of Risk prosody now tested and inert; ECC Analyzer unvalidated-extraction contrast), framing gate stated as **provisionally Path B**, new "instrument, not model" paragraph (anchor ambiguity, κ-ceiling problem), future-work pointer to the ecvol-live corpus. Abstract/intro/conclusion carry the negative headline framing-neutrally; limitations rewritten (adds MLP seed-instability, pastvol-ridge≠HAR, short-τ R²-inflation, single-rater κ + unmeasured ceiling, "no Result Table 5 exists ⇒ RQ3's predictive half unanswered-not-negative").
- **Done — appendix** now inputs Result Tables 1–4; explicit paragraph on the absence of Table 5 (blocked at the validity gate, by design).
- **Found — build bug:** with 56 consecutive generated floats the two-column output routine hits `Output loop—100 consecutive dead cycles` (the old 32-float appendix built fine; `\extrafloats{120}` alone doesn't save it). Fix: `\clearpage` between the four generated-table `\input`s. Also: `\dag` must stay text-mode (was `$\dag$` in captions).
- **Gate:** ruff clean, 281 tests green; `paper/build.sh` → 20 pp, 0 undefined refs/citations, bibliography resolves. TASKS T8.2 → `[~]` with a dated note.
- **Next:** the draft's only stub is Phase-7 lookahead; TX4 capture (oldest-first, replay decay) and the T6.2 rater-2 decision remain the open fronts.

## 2026-08-19 (later) — T8.2: peer-review render of the draft

- **Scope:** user asked for a shareable version of the PDF for peers. Choices confirmed via Q&A: keep the author anonymous; keep the `\pending{}`/`\TODO{}` markers visible; add line numbers for feedback.
- **Done:** `lineno` (gray, tiny) on both columns; subtitle → "anonymized working draft for comments — please do not cite or circulate"; removed the placeholder author line. Applied two content fixes from this session's project review before sharing: (1) data-section erratum — 92.9% is the *overall* identity-resolution rate, 95.3% is the earnings-cohort rate (was mislabeled); (2) the **40 kbps source-audio caveat** (from `docs/fincall_methodology_and_successor.md` §2.3) added to Limitations and as a clause on the Sound-of-Risk rebuttal — the audio-inert finding is established on compression-damaged audio. Also fixed 4 author-less refs.bib entries that rendered as key fragments ("scs (2024)", "(fin, 2025)"): first authors taken from the verified `paper/reference_audit.md` (Yu/SCSS, Tatarinov/survey, Liu/AT-FinGPT, Lin/emo-bias) + "and others".
- **Gate:** paper-only change; build clean (20 pp, 0 undefined refs), pages 1 and 5 visually verified.
- **Next:** unchanged from the review — TX4 capture (decaying), rater-2 decision, Phase 7 (Earnings-25 option), push + CI.

## 2026-08-23 — Advisor redirection: assessment + literature code audit + substrate audit (analysis only)

- **Scope:** user relayed an advisor-meeting proposal to redirect — (a) clean/augment the *existing* corpus and build the system that fixes it for future research instead of collecting our own, and (b) re-run cutting-edge papers under our controls as a "literature review with our tooling." Asked for evaluation, then for a paper/code hunt, then for this record. **No DESIGN/TASKS/DECISIONS changes made** — every promotion needs its own DECISIONS entry first. Full write-up: `docs/advisor_redirection_2026-08.md`.
- **Done — assessment.** Half A is largely a *reweighting*, not new work: ecvol-bench is already DESIGN §4 contribution #1 and the assets exist (10,270 LOC / 51 modules / 281 tests / 34 CLI commands; leakage-proof committed splits; 9,696 targets reproduced to 1e-15; five feature families corpus-wide). Crown jewel is the identity reconstruction (92.9% / 95.3% earnings cohort) — nobody else has it, and it is the precondition for the very controls that expose the field's confound. Half B is N reproduction projects wearing a literature review's clothing → recommend capping at 2–3 code-released models and leading with the substrate audit instead. Argued the pivot's strongest (unstated) benefit: it converts the "your audio is inert because it's 40 kbps garbage" reviewer attack from a wound into the thesis. Recommended hedge: run Phase 7 on Earnings25 *first* so a complete paper is always in hand before betting on the pivot.
- **Found — a defect in our own contract.** DESIGN §5.3 claims τ ∈ {3,7,15,30} ***trading*** days "(Qin & Yang convention, for comparability)". The source PDF says **calendar** days ("We choose different τ values, including 3, 7, 15, 30 calendar days"). Every downstream paper inherits Qin & Yang's released labels, so the published leaderboard is calendar-day while `targets.py` is trading-day (~30 vs ~21 sessions at τ=30). Our targets remain internally valid; the *comparability claim* does not. Needs a DECISIONS entry — options: recompute on calendar days, keep trading days and drop the attribution, or ship both (recommended).
- **Found — the literature's code is mostly not there.** ~20 papers checked; **5 runnable repos**: Same-Company-Same-Signal (MIT, pushed 2026-08-13, ships DEC + augmented EC/MAEC earnings histories), HTML (~50 KB PyTorch, no requirements.txt, last code commit 2021), KeFVP (full code; `requirements.txt` won't install — `spacy==.5.3`, `pickle`; torch 1.12 predates Blackwell; MAEC KePt embeddings explicitly unavailable), DialogueGAT (MIT, τ≤15, corpus must be rebuilt), Sawhney ACM MM 2020 (TF 2.1/Keras 2.3.1). **Not runnable:** ECC Analyzer (no code; GPT-4 Turbo + OpenAI embeddings — the frontier claim Phase 6 was built to test), RiskLabs (no code; GPT-4/3.5), Sound of Risk (repo is figures-only, zero source; proprietary 1,795-call corpus; DeepSeek-R1 671B) — this was our *primary empirical comparison target* per DESIGN §3.0, DeFVP (repo **size 0 KB**, never pushed), ECHO-GL (README: "can not run at present"; target is direction not volatility), NumHTML (promises code, gives no URL), GNA-Vol (artifact URL 404), AMA-LSTM (10-byte stub, unattributed), AT-FinGPT (paywalled). Two agents independently: **no 2025–26 multimodal ECC volatility model with public runnable code exists; no leaderboard exists for this task.**
- **Found — the substrate audit, which needs no reproduction at all.** VolTAGE's and KeFVP's `test_split_SeriesSingleDayVol3.csv` are **byte-identical** — the lineage shares one physical label set inherited from Qin & Yang, never recomputed. Split integrity across all three canonical benchmarks: EC 80.4% / MAEC-15 44.2% / MAEC-16 55.7% of test calls have their ticker in train, with **0-day embargo** everywhere (EC train ends *and* val starts 2017-08-03: 4 train calls, 14 val calls on that date; same at the val/test boundary) against targets running to 30 days — DESIGN §5.4 mandates a 30-day embargo for exactly this. Our own splits: temporal 91.3% (fincall) / 78.9% (maec), ticker-disjoint **0.0%** by construction. Honest framing recorded: temporal splits *always* permit reuse, so the claim is not "they have overlap" but "**no paper ever reports the disjoint condition**" plus the embargo defect. Label defects: **137 exact zeros / 16,800 cells (0.82%)** in the single-day log-vol series (headline Avg_Series is clean); KeFVP's MAEC files **interleave four binary labels at exactly τ ∈ {3,7,15,30} among 26 price columns** under identical `future_label_N` naming (154/154 rows, zero exceptions). And the bridge works: KeFVP's MAEC labels key on `20151029_ALXN` = our `call_id`, joining our targets at **99.4% (153/154)** — TX3 is immediately actionable.
- **Found — new tools/resources not in DESIGN §13.** **FinTrust** (Yang et al., ACL 2023 short — *by the HTML author*): consistency/perturbation harness, repo live, 4 perturbation generators + 3/7/15/30 data; venue precedent for the "we built a diagnostic and the field fails it" shape, and a complementary control axis to our shuffle. **SPGISpeech 2.0** now **CC BY-SA 4.0** (+3,780 h, speaker tags) — DESIGN §5.1 skipped v1 on licensing, so that call is stale. **Same-Company-Same-Signal's DEC** uses `beforeAfterMarket` — a candidate fix for our assume-after-hours fallback. Corroborating negatives: "Acoustic Camouflage" (audio *degrades* recall 66.25→47.08 on MAEC) and Netspar 2025-15 (TRILLsson no predictive power).
- **Found — repo-state gaps relevant to a benchmark framing.** `artifacts/` holds only 8 diagnostic files, **no run payload**, against DESIGN §8.2/§12 promising every paper number regenerates from `artifacts/runs/<run_id>/` (not yet checked whether `report` sources `data/results/` instead); `configs/` holds only `example.yaml` against "one committed config per experiment". Under a resource-paper framing these are load-bearing, not footnotes. Also: `t2019/t2020/t2021.json` = **137 MB of raw FinCall transcripts at repo root, untracked *and* un-gitignored** — a stray `git add -A` would commit scraped third-party content into the repo whose licensing posture is becoming a headline contribution.
- **Found — TX4 is further from done than "capture not started" implies:** `src/ecvol/collect/` contains only `discovery.py`; no capture, no downloader, no transcription. 8 of the 50 pilot calls are past the 30-day replay floor (oldest 38 days). Recommend formal deferral; Earnings25 covers the Phase-7 need it was going to serve.
- **Sources:** 13 repos fetched directly (URLs in the doc §8); Qin & Yang PDF in `legacy/papers/`; local `data/splits/*.csv`, `data/maec/targets.parquet`. Unresolved: IEEE ICDMW 2025 "Volatility Prediction with Audio and Structure-Aware Text Embeddings in Earnings Conference Calls" — exact title match for our task, paywalled, authors/DOI unretrieved; needs a manual Xplore lookup before claiming the space is clear.
- **Next:** user answers the 10 open decisions in `docs/advisor_redirection_2026-08.md` §7 (pivot scope, horizon convention, reproduction slate, Phase-7 source, TX4, T6.2, EDGAR retrofit, repro debt, venue, housekeeping). Nothing implemented until then.

## 2026-08-26 — Session recovery: the 2026-08-23 redirection committed, DESIGN amended, paper restructured benchmark-first, T9.3 opened

- **Scope:** the 2026-08-23 session disconnected after the user had answered the redirection's open decisions but before anything was committed — DECISIONS (12 points), TASKS restructure, HANDOFF rater-2 cancellation, `.gitignore`, and the analysis doc were all sitting uncommitted, the only 08-23 JOURNAL entry still said "analysis only", and the DESIGN amendments promised under §3/§11 had not been made. This session reconstructed the state, asked the user four remaining questions, and closed the loose ends.
- **User answers (2026-08-26):** κ-gate → **excised from the paper entirely** (not a reported negative); work order → **T9.3 first**; `briefing/` → **gitignore, keep local**; paper → **restructure the skeleton now**. Recorded as DECISIONS 2026-08-26 (five points, incl. the RQ3 re-fill with the reproduction question).
- **Done — records (commits 4a3affd, 38879c1).** 08-23 work committed as-is; then DESIGN §1 (reframe note; RQ3 → "do the published models, re-run on their own data, retain their advantage under embargoed, ticker-disjoint evaluation?"), §4 (benchmark-first contribution #1 incl. identity reconstruction + dual-convention targets + substrate audit + reproduction study; Path-A/B gate superseded as organising axis; venue ACL/ARR with the anonymity-vs-preprint conflict flagged), §5.3 (both horizon conventions; calendar-day misattribution corrected), §6 (Stage 5 dropped), §9 (phase table: 6 dropped, 6R + 9 added, 7 → Earnings25; task IDs T0.1–T9.4). HANDOFF wording corrected to "excised". `/briefing/` ignored (the pre-existing pattern never matched).
- **Done — paper (commits 7e043b9, 3883c2c).** Title → *ecvol-bench: An Open, Identity-Controlled Benchmark for Multimodal Earnings-Call Volatility Prediction*. New spine: Intro → Related (adds "The shared substrate") → **The ecvol Benchmark** (§3.1 substrate audit → Table 5R stub; corpora + identity reconstruction; targets under both conventions; call timing; splits with the 0.0% / 91.3% / 78.9% overlap contrast; release) → Models (Stages 0–4 + *Reproduced prior models*) → Protocol → Results (all Stage 0–4 numbers verbatim; new pending subsections: convention delta, Table 6R, Earnings25 clean audio, lookahead) → Discussion ("why this is a benchmark paper"; the pre-registered gate reported as an auditable fact) → Conclusion → Limitations → Ethics → Appendix (5R/6R stubs). Every Phase-6/κ-gate/Stage-5 reference removed; `tables/llm.tex` deleted. Builds clean with MiKTeX: 21 pp, 0 undefined refs. The 2026-08-23 substrate figures are quoted in §3.1 explicitly *pending regeneration* by `ecvol audit substrate` (T6R.1) — they must not be cited as final until then.
- **Found — T9.3 opening check (recorded in TASKS).** `ecvol report` renders from `data/results/*.csv`, which are git-tracked and written directly by `evaluate` / `evaluate-text` / `controls` / `evaluate-audio` / `evaluate-fusion` (option-driven: `--root`, `--seeds`; no config files); `tracking.write_run` (T0.3) has never been called by any command and `artifacts/runs/` does not exist; `configs/` holds only `example.yaml`; `tests/test_report.py::test_committed_reports_match_fresh_render` guards CSV → md/tex only. So the reproducibility chain that is actually enforced today is *result CSV → rendered table*, not *run → result CSV*. The paper's Reproducibility paragraph was reworded to exactly that, with the rest marked pending T9.3. Also: the T9.3 acceptance test's "clean-machine regeneration of Result Table 1 in CI" cannot run in CI as written — Table 1 needs the price/target payload on `D:\ecvol-data`, which CI does not have; CI can assert committed artifacts, a clean-machine run belongs to T8.1. Flagged for the design call.
- **Gotchas:** `uv run python` printing "κ" dies under cp1252 when stdout is redirected — set `PYTHONIOENCODING=utf-8` (same class of bug fixed in the CLI on 2026-08-09). Bash heredocs containing long quoted prose failed to parse in this shell; write scripts to the scratchpad with the Write tool instead.
- **Sources:** working tree + `git status` reconstruction; `docs/advisor_redirection_2026-08.md`; `src/ecvol/cli.py`, `src/ecvol/tracking.py`, `src/ecvol/eval/{evaluate,report,grid}.py`, `tests/test_report.py`.
- **Next:** T9.3 design call (how run artifacts attach to option-driven multi-model commands; where the byte-identical assertion can honestly live) → implement; then T7.1's one-day Earnings25 metadata verification; then T9.1 calendar-day targets. Human items unchanged (HANDOFF 2026-08-23): relay the rater-2 cancellation; ACL/ARR dates + anonymity check; IEEE Xplore lookup.

## 2026-08-26 (later) — T9.3 DONE: per-command run manifests; Result Table 1 regenerates byte-identically

- **Scope:** the T9.3 design call was put to the user (two questions) and answered: provenance attaches per result-producing **command** (not one `ExperimentConfig` per model), and CI asserts **manifest integrity** (the clean-machine regeneration moves to T8.1). DECISIONS 2026-08-26 (later).
- **Done (commits e223815, f49129f, 3e3d074 + this).** `CommandConfig` schema + `load_command_config`; `configs/<command>.yaml` for the six commands; `tracking.{COMMAND_OUTPUTS, write_command_run, verify_outputs, find_manifest, resolve_command_config, ProvenanceError}`; the six commands take `--config`/`--seeds` and write `artifacts/runs/<run_id>/run.json` after their outputs; `ecvol report` refuses unmanifested CSVs; new `ecvol runs verify|backfill`; `.gitignore` negation so manifests are committed; `tests/test_runs.py` (8 tests incl. the CI guard). 289 tests green. DESIGN §8.1/§8.2/§12 amended; paper Reproducibility paragraph now states exactly what holds.
- **Found — the regeneration claim holds for Table 1.** After backfilling manifests over the eleven existing CSVs, `ecvol evaluate` was re-run live (GARCH convergence fincall 99.6% / maec 99.9%; sanity gate passed with the documented COVID exception; HAR τ=30 level-v: fincall temporal −0.2871, ticker-disjoint +0.2287, maec temporal +0.2064): `result_table_1.csv` came back **byte-identical** to the committed file, so the new `provenance: run` manifest (`20260826T190559Z-evaluate-cbe08bbd`) and the backfill share the same output SHA and config hash. That is the first end-to-end demonstration that a paper table regenerates from code + config + data alone.
- **Found — ruff B008 interaction:** the formatter wraps long `typer.Option(...)` defaults and strands the `# noqa: B008` marker; a module-level `_CONFIG_OPT` singleton is the clean fix (ruff's own suggestion).
- **Open:** the other ten CSVs are still `backfill` — re-run `evaluate-text`, `controls`, `evaluate-audio`, `evaluate-fusion`, `grid` to upgrade them (features are cached; heads are minutes each); any non-identical regeneration there (MLP seed instability) is a finding to record, not to hide. Curated `paper/tables/*.tex` are still hand-copied cells (T8.1).
- **Next:** T7.1's one-day Earnings25 metadata verification (gates T9.4, the highest-value experiment), then T9.1 calendar-day targets. Human items unchanged (HANDOFF 2026-08-23).

## 2026-09-01/02 — T9.1 DONE: calendar-day targets shipped beside the trading-day set; Result Table 1 under both conventions. T7.1 Earnings25 verification started

- **Scope:** user chose to keep moving and batch the push/CI checkpoint for later (28 commits ahead of origin at session start). Started T7.1's Earnings25 verification (Zenodo 10.5281/zenodo.18762168: open access, CC-BY-4.0, one 12.04 GB zip, no key — downloading to `D:\ecvol-data
aw\earnings25\`), then implemented T9.1 while it pulled.
- **Done — T9.1 (DECISIONS 2026-09-01).** `targets.py`: `convention=` kwarg; calendar post window = sessions dated in `(day0, day0+τ]`, pre = `[day0−τ+1, day0]`; population variance over the *n* sessions present; `<2` sessions → `short_window_{pre,post}`; `n_pre`/`n_post`/`convention` columns; `TARGET_FILES` = `targets.parquet` (trading) + `targets_calendar.parquet`; `convention_delta()`. `ecvol targets build` writes both FinCall files (+ `targets_calendar_report.csv`, one manifest with both entries); `ecvol data ingest maec` likewise; new `ecvol targets compare` → `data/coverage/targets_convention_delta.csv`. `ecvol evaluate` loads every present convention into one frame (EWMA/GARCH fit once per call; GARCH averages per-step variance over the row's `n_post`; HAR refit per convention), rows carry `convention`, calendar rows sort after trading, gate reads trading only. `ecvol report` renders the 16 calendar views after the 16 trading ones. Tests: hand-counted windows around the 2021 MLK weekend (τ=3 post window empty), closed-form calendar τ=7 (5 pre / 4 post sessions), short vs missing window precedence, both-file determinism, manifest/report presence, delta report, `build_table` convention filter, and `test_calendar_convention_rows_leave_trading_rows_unchanged`. DESIGN §5.3 amended (exact rule, denominator, short-window edge rule, file layout). Paper: §3 targets paragraph rewritten; §6 convention subsection filled; new curated `tables/convention.tex`.
- **Verified — nothing that was validated moved.** Trading `targets.parquet` values are identical on the original 15 columns for FinCall and MAEC (pandas `equals` against the pre-change files); `ecvol splits build` left every committed split CSV unchanged; the regenerated Result Table 1's 720 trading rows equal the committed table exactly (new run `20260902T040159Z-evaluate-cbe08bbd`, provenance `run`, 2m29s; GARCH 99.6%/99.9%; gate passes via the documented COVID exception as before). `ecvol runs verify` 11/11.
- **Found — the convention is not a footnote.** (1) Calendar windows hold 2.4/4.9/10.7/21.0 sessions at τ=3/7/15/30 (FinCall; MAEC 2.5/4.9/10.7/20.9). At τ=3, **1,183/2,424 FinCall calls (48.8%) have ≤1 post session** — every Thursday/Friday call — and are excluded; the released label sets carry a value for every call, so their τ=3 targets are one- to three-return "volatilities". Hypothesis for T6R.1: the 137 sentinel zeros in the released single-day series are these one-return windows. (2) corr(v_post trading, calendar) = 0.748/0.925/0.957/0.936; corr(Δv) 0.761/0.895/0.909/0.887. (3) Persistence MSE (the leaderboard metric) ≈ doubles at τ=3 under calendar (FinCall temporal 1.174→2.307; MAEC 1.383→2.391) and is 20–40% higher at τ=7–30 — published MSEs are not comparable across conventions. (4) The COVID gate cell (HAR τ=30 FinCall temporal) goes −0.287 → +0.017 under calendar: a 21-session window straddles less of the regime shift. MAEC τ=30 calendar has 2 *more* ok rows than trading (shorter window clears the delisting edge).
- **Found — Earnings25 metadata from the paper (arXiv 2607.23813), pending the zip:** testset-full = ~500 S&P 500 calls, 2025 Q4, 498 h, 12 countries (US 93.8%), **MP3 at 11–44.1 kHz source rates**; the zip works out to ≈53 kbps averaged over 498 h, i.e. close to FinCall's 40 kbps transcodes → T9.4's "clean audio" premise must be measured per file before anything is planned around it. Metadata: speaker names/roles, industry, company identifiers; per-call dates/times not stated in the paper.
- **Open (design call, put to the user):** Stages 2–4 under the calendar convention — not done (doubles head compute; MLP heads seed-unreliable). Tables 2–4 gain the `convention` column (= trading) when their commands are next re-run (T9.3 backfill→run upgrade).
- **Sources:** Qin & Yang 2019 (calendar-day convention, verified 2026-08-23); DECISIONS 2026-08-23 §3; `exchange_calendars` XNYS; Zenodo record 18762168 API (`files[0].checksum md5:5aa434b3…`); arXiv 2607.23813 HTML.
- **Next:** finish T7.1's verification once the zip lands (md5, unpack, per-call metadata inventory, price-join dry run, per-file bitrate/sample-rate distribution, licence/ToS note); then T6R.1 substrate audit or T9.2 timestamp check per the user. Push + CI batched at the user's request.

## 2026-09-02 — T7.1: Earnings25 verification DONE (GO), with two findings that change the plan

- **Done:** zip landed (12.04 GB, md5 verified), unpacked to `D:\ecvol-data
aw\earnings25\`; inventoried `testset-full` (514 calls, 497.9 h) field by field against the T1.3 join needs; company→ticker dry run via the T1.4 matcher (+8 overrides, S&P 500 snapshot filter); ffprobe over all 514 mp3s; yfinance dry check for the 2025-09→2026-02 window; transcript cross-check of the `ReleaseDate` timezone. Written up in `docs/earnings25_verification_2026-09.md`; committed `data/coverage/earnings25_{inventory,audio_probe}.csv`.
- **Found — it carries what we need, plus something we did not have:** `ReleaseDate` is a full datetime for every call, consistent with UTC (three Pacific-time transcript mentions sit exactly 8 h earlier; hour histogram peaks at 8–11 am / 4–6 pm ET). That is the first measured call time in the project → T9.2's fallback can be replaced on this corpus and the after-hours rule tested for real. Name→ticker resolves 499/514; 472 are current S&P 500 members; ~20 more are 2025-Q4 members since dropped. Prices need a fresh pull (archive ends 2022-06-30; yfinance serves the window).
- **Found — Earnings25 is contaminated by name collisions (~5%):** Grainger PLC, two foreign Domino's, Paramount Group (REIT), Vertex Inc. (tax software), PTC India ×2, Cummins India, GE Vernova T&D India, Everest Kanto/Medicines, Prologis Mexico, CVS Group plc, Cleanaway, Capital Ltd, plus US non-members that matched on a famous first token (Goldman Sachs BDC, Blackstone Secured Lending/Mortgage Trust, BlackRock TCP, Morgan Stanley Direct Lending, Apple Hospitality REIT, The Bancorp, Snap). Reason-coded exclusions at ingestion; a sentence for the paper.
- **Found — T9.4's premise is false:** the audio is webcast-replay MP3 at **64 kbps (281 calls) / 24 kbps (216) / 16 kbps (16)**, mono; 45% is *below* FinCall's 40 kbps. Earnings25 cannot test "clean audio". It can test the audio-quality hypothesis *within* one corpus (64 vs ≤24 kbps, same quarter, same extractors) — proposed re-scope, user's call.
- **Design calls put to the user:** (1) re-scope T9.4 as the bitrate-stratified re-run; (2) universe rule = S&P 500 membership on `ReleaseDate` (recommended) vs any US-listed ticker.
- **Sources:** Zenodo record 18762168 (`files[0].checksum`), arXiv 2607.23813 §3 table (11–44.1 kHz source rates), SEC `company_tickers.json` (cached), `data/live/universe.csv` (2026-08-13 snapshot), ffprobe 8.1.1, yfinance.
- **Next:** on the user's answers → T7.1 ingestion (`ecvol data ingest earnings25` onto the common schema: ticker resolution + membership filter + reason codes, price pull for the window, targets under both conventions, timestamps carried through) → T9.4 (re-scoped) and T9.2 (UTC confirmation on an 8-K sample). Push + CI still batched.

## 2026-09-02 (later) — Design calls answered; T7.1 DONE: Earnings25 ingested onto the common schema

- **Scope:** the user answered the four open calls (all on the recommendation — DECISIONS 2026-09-02): T9.4 → bitrate-stratified re-run; universe = S&P 500 on `ReleaseDate`; Stages 2–4 stay trading-day; order T7.1 → T9.4 → T9.2 → T6R.1 → T6R.2. Push/CI batched until further notice. Loop runs autonomously from here.
- **Done — `src/ecvol/data/earnings25_ingest.py` + `ecvol data ingest earnings25` + `ecvol prices pull --dataset earnings25`** (`prices.pull_tickers`: own store + manifest + Tiingo fallback; the 2014–2022 archive is untouched). Membership on the call date is reconstructed **by CIK from month-end Wikipedia revisions of the S&P 500 list** (MediaWiki API `rvstart`, one cached HTML per month, manifested) — the live page dropped its "selected changes" table, so revisions are the only reproducible source; 6 months needed (2025-03/07/08/10/11/12). Name→ticker via the T1.4 matcher + 8 curated overrides + a 4-name look-alike block list. Speaker-attributed segments → turns (`operator`/`unknown` roles; T3.1's cue fallback handles the rest, as for MAEC). `ReleaseDate` UTC → America/New_York → passed as a real timestamp to `compute_call_targets` (first corpus with `time_known=True`). 8 tests (`tests/test_earnings25_ingest.py`): UTC→ET incl. DST, turn merging, strata, overrides/look-alikes, end-to-end universe rule + after-hours branches (16:30 ET → day0 = call date; 08:30 ET → prior session), determinism, string ids.
- **Found — gate passed:** 476/514 admitted (`lookalike` 4, `not_sp500` 23, `unresolved` 11 — the 11 are all foreign look-alikes); prices 475/476 (**99.79%**, ≥95% gate). AVB and EA recovered from Tiingo (Yahoo purged both — taken private/merged in 2026); **EQR is gone from both** → the one reason-coded miss. Bitrate strata: 64k = 281, ≤24k = 232, other = 1 (the lone 32 kbps file).
- **Found — call time ≠ release time.** Of the 476 admitted calls, 215 start before 09:30 ET, 137 intraday, 124 after 16:00 ET. DESIGN §5.3 treats intraday as after-hours (day0 = call date), so for those 137 the reaction session lands in the *pre* window. Worse, companies like Lennar release after the prior close and hold the call at 11:00 ET — information is public a day before the call. `ReleaseDate` is a call-start time, so T9.2 must settle day 0 from the 8-K Item 2.02 timestamp, not the call time; the ingestion records the measured call time (`earnings25_call_times.csv`, tier `earnings25_metadata`) and applies the DESIGN rule as written.
- **Bug caught by the first real run:** `audio_file_path` is `audio/<id>.mp3`, not `<id>.mp3` (the dataset card's example misleads) → audio paths were doubled and every probe silently returned None. Fixed; `audio_exists` is now checked on disk rather than assumed.
- **Sources:** MediaWiki API (`action=query&prop=revisions`), Wikipedia "List of S&P 500 companies" revisions 1283349047/1302049498/1308482549/1319706093/1324692884/1329075976, SEC `company_tickers.json`, yfinance, Tiingo daily prices.
- **Next:** T9.4 — the frozen audio ladder (T4.1 QC → eGeMAPS → WavLM/emotion2vec+ → Stage-3 heads + controls) on Earnings25, per stratum. The audio commands are FinCall-hardwired (`qc.py` reads `fincall/calls.parquet`); they need a `--dataset` switch first.

## 2026-09-02 (later still) — T9.4 wiring landed; T9.2 preliminaries while the audio store builds

- **Done — T9.4 code (commits cb51aa4, 857e74e):** audio ladder parametrized by dataset (`audio qc|egemaps|wavlm|emotion2vec --dataset`), `evaluate_stage3`/`audio_shuffle_control` take a scheme list + cohort filter, new `ecvol evaluate-audio-earnings25` (all / 64k / le24k cohorts, ticker-disjoint only) with its command config + run-manifest outputs; `splits build` and `featurize sections` cover earnings25. Splits: ticker-disjoint 333/47/95; **temporal is degenerate (train 5 / embargo 377 / test 93)** — a single-quarter corpus spans ~105 sessions, less than 4× the embargo — recorded in `earnings25_splits_report.csv` and the reason T9.4 reports ticker-disjoint only. Sections: 512/514 Q&A boundaries found (504 operator cue, 8 text cue). Running now: `audio qc --dataset earnings25` (ffmpeg → 16 kHz FLAC store, ~90 min), `featurize text --dataset earnings25` (GPU).
- **Caught — `featurize sections` overwrote the hand-filled FinCall audit sheet.** `fincall_section_audit.csv` carries the user's 30 `correct_y_n` answers (T3.1 gate); a re-run regenerated the blank sheet. Restored from git; audit sheets are now write-once (`test_audit_sheet_is_write_once`). Chunk parquets themselves were byte-identical (manifest SHAs unchanged; only `retrieved_at` moved).
- **Found — T9.2 preliminary, SCSS DEC (`piqueyd/Same-Company-Same-Signal`, `dataset/DEC.csv`, CC-BY-4.0 for the authors' fields):** 90 tickers × 20 quarters (2019–2023) with `beforeAfterMarket`; overall 69.7% BeforeMarket. **323 FinCall calls match exactly on (ticker, date)** (411 within ±1 day; 13–17% of the resolved corpus) — too thin to replace the fallback, but a free sensitivity sample: **80.5% of the matched FinCall calls are BeforeMarket**, i.e. the assume-after-hours rule anchors day 0 one session late for four calls in five, leaving the reaction session in the *pre* window. `data/coverage/fincall_dec_timing_overlap.csv`.
- **Found — T9.2 route settled by a 50-call pilot (`data/coverage/edgar_timing_pilot.csv`):** the SEC submissions API (`data.sec.gov/submissions/CIK##########.json`, + the paginated `files`) lists every filing with `acceptanceDateTime` and `items`; the earliest 8-K carrying Item 2.02 in [call date − 3, call date + 1] is the earnings release. **48/50 found**; the acceptance time agrees with the DEC flag in 43/48, and the 5 "disagreements" are prior-evening releases before a next-morning call (e.g. CCI 2021-07-21 16:17 ET for a 07-22 call), where the *release* time is the correct information boundary and DEC's flag refers to the release day — so EDGAR is the better anchor, not merely a substitute. Free, deterministic, ~0.15 s/request, cached under `D:\ecvol-data
aw
ef\edgar_submissions\`. Consistent with Earnings25's measured call times (45% before-open, 29% intraday, 26% after-close).
- **Implication (for T9.2, not acted on yet):** day 0 should be anchored on the *release* timestamp (8-K acceptance), with the call time as the tier below it and the fallback last; then rebuild targets and re-run Table 1 as the sensitivity check DESIGN §10 risk #7 has been waiting for. Provenance tiers: `edgar_8k` › `earnings25_metadata` › `dec_flag` › `assumed_after_hours`.
- **Sources:** SCSS repo + LICENSE-DATA; SEC EDGAR submissions API (fair-use UA header, ≤10 req/s); DEC `beforeAfterMarket`.
- **Next:** finish T9.4 (eGeMAPS → WavLM → emotion2vec+ → `evaluate-audio-earnings25`), then T9.2 bulk retrofit.

## 2026-09-03 — T9.2 DONE: EDGAR-anchored call timing; the after-hours fallback is wrong for most calls

- **Done:** `timing.py` (`fetch_submissions` cached forever, `release_filing`, `session_rule`, tiers, `build_call_times`, `build_measured_targets`, `sensitivity`), `ecvol timing build|targets|sensitivity` (sensitivity manifested; `configs/timing-sensitivity.yaml`), 6 tests. Bulk EDGAR pull: ~2,000 submission JSONs cached under `raw/ref/edgar_submissions/`. A first bulk run crashed on FinCall's 2 out-of-range identity dates (year 0001-style stamps) → tolerated with `session_rule=invalid_date`. The harness killed the long background jobs mid-run (twice); they now run as detached `cmd /c` processes with a persistent monitor — resumable by design, nothing lost.
- **Found — coverage:** FinCall 89.3% / MAEC 68.6% / Earnings25 99.8% of resolved calls get an 8-K Item 2.02 acceptance time; DEC adds 5 FinCall calls; the rest fall back. FinCall session rule: before_open 1295, after_close 554, intraday 46, **after_call_date 292** — an 8-K accepted the day *after* the identity date is almost certainly a date error on our side (32% of FinCall dates are PDF-stamp derived), which T9.2's rule treats as after-hours on the call date; flagged for the identity work, not fixed here. Earnings25: 341 before-open *releases* vs 215 before-open *call starts* — the release-vs-call distinction is real for 126 calls.
- **Found — the headline:** as_of moves for **53.3% of FinCall, 58.6% of MAEC, 27.6% of Earnings25** calls (one session earlier). Persistence MSE (the leaderboard metric) rises ×1.01–1.08 on FinCall and ×1.06–1.26 on MAEC (τ=3 largest) once the reaction session sits in the post window; HAR R²_OOS moves within ±0.08 (up on the disjoint splits, down on FinCall temporal). Earnings25 v_post correlates 0.967 across anchors, Δv only 0.799. So DESIGN §10 risk #7 was not a tail risk: the uniform after-hours rule mis-anchors the majority of calls, and every released label set in the literature (call-date anchored) shares the error — a substrate finding for Table 5R.
- **Decision recorded (DECISIONS 2026-09-03):** release time is the anchor, tiers are recorded, measured targets ship as a variant, and re-baselining is put to the user (HANDOFF 2026-09-03; recommendation B after 6R). Paper §3.4 rewritten with the numbers; DESIGN §5.3 amended.
- **Sources:** SEC submissions API; SCSS DEC; DESIGN §5.3/§10 #7.
- **Next:** T9.4 evaluation as soon as the detached eGeMAPS / WavLM / emotion2vec+ chains finish; then T6R.1.

## 2026-09-03 (later) — T6R.1 DONE: `ecvol audit substrate` regenerates Result Table 5R from a manifested lineage mirror

- **Done:** the canonical files were not kept after the 08-23 audit, so they were re-fetched via the GitHub API pinned to commit SHAs (KeFVP 95993892, VolTAGE 597d69d8, HTML 9d717972; 44 files; `raw/ref/lineage/lineage_manifest.json`). `src/ecvol/eval/substrate.py` + `ecvol audit substrate` (manifested; `configs/audit-substrate.yaml`) compute per benchmark: calls per split, test-ticker-in-train share, embargo days per boundary, duplicate (ticker, date) across boundaries, EC single-day sentinel zeros, KeFVP MAEC binary-column detection, the KeFVP↔VolTAGE byte-identity check, our own splits as contrast rows, and the MAEC label-vs-targets diff. 2 tests. Paper: `tables/substrate.tex`, §3.1 rewritten around the regenerated table (+ a new "labels versus prices" point), A1 pointer.
- **Found — everything from 08-23 reproduces exactly**, and one claim was too generous: the "99.4% join" of KeFVP's MAEC labels to our targets was a key match on the price-label ids; against *priced* calendar-day targets the join is 74–77% at τ≥7 (our 75.4% MAEC price coverage) and 20–38% at τ=3 (short-window exclusions). On the joined calls the shipped labels correlate 0.67–0.88 (MAEC-15) / 0.51–0.82 (MAEC-16) with our calendar-day v_post, 0.71 / 0.25 at τ=3, mean |diff| 0.15–0.73 log-vol — the released labels are not recoverable from prices under any single documented rule (anchor, denominator, window). That is the Table-5R sentence that matters for T6R.2's "reproduce the published number first" step.
- **Sources:** GitHub trees/raw for the three repos; `docs/advisor_redirection_2026-08.md` §5.
- **Next:** T9.4 evaluation (detached chains still running), then T6R.2 (HTML first — `legacy/4-Reproduce_HTML.ipynb` is a working reimplementation).

## 2026-09-03 (evening) — T9.4 DONE: the audio result does not depend on bitrate

- **Done:** the detached chains finished (eGeMAPS 514, WavLM 514 in ~4 h, emotion2vec+ 514 in 2.6 h); `ecvol evaluate-audio-earnings25` ran in ~1 h on CPU (5 seeds × MLP heads × 3 cohorts) and wrote the three manifested CSVs. Paper §6 subsection filled, curated `tables/earnings25_audio.tex`; TASKS T9.4 → [x].
- **Found:** on the ticker-disjoint test (n=95; strata 39 / 55) every audio+past-vol ridge head beats persistence on Δv, but a head fed a *random other call's* audio keeps most of it: audio increments (real − global shuffle) 0.09 / 0.05 / 0.04 / 0.01 pooled, and per stratum 64 kbps +0.03 / 0.00 / +0.08 / −0.02 vs ≤24 kbps −0.04 / +0.02 / +0.07 / +0.04. Nothing separates the strata and the better-encoded one is not better. Audio-only heads post positive Δv R² (0.09–0.19 at τ≤7) that also survives shuffling — it is the intercept (Δv mean reversion in Q4-2025), not audio. **The FinCall "audio is inert beyond past volatility" finding survives on 64 kbps, 2025, post-cutoff audio.** Within-ticker shuffle = identity on this corpus (one call per company), as designed; the identity probe is undefined and not reported.
- **Caveats to carry into the paper:** strata are small (39/55 test calls); ridge only for the headline (MLP seed std is in the table); 64 kbps webcast MP3 is still not studio audio — the "clean audio" question remains open, just not answerable with any public corpus.
- **Next:** T6R.2 HTML run as soon as `featurize text --dataset ec` (GPU, 89,722 chunks) finishes.

## 2026-09-03 (night) — T6R.2 first entry: HTML on EC — published long-horizon numbers not reproduced; ticker-disjoint costs 9–46%

- **Done:** EC text features (BGE-M3/FinBERT/surface over 89,722 chunks, 8 min GPU); `ecvol reproduce html` (128 fits × 30 epochs, ~1.5 h GPU; α tuned on val with seed 0, 3 seeds on test) → `results/result_table_6r.csv` (manifested). Paper §6 Stage-R subsection + curated `tables/repro.tex`; TASKS subtask ticked.
- **Found — the reproduction:** with the shipped labels on the published split the head lands at 0.769 / 0.436 / 0.285 / 0.223 against the paper's 1.175 / 0.372 / 0.153 / 0.133. τ=3 is beaten outright; τ=7 is within 17%; τ=15/30 are 1.9×/1.7× the published values — and the published values are *below persistence on the shipped labels themselves* (0.340 / 0.202), i.e. the paper claims a 55%/34% improvement over the trivial forecast at long horizons that neither a faithful head with a stronger encoder nor any baseline in that paper's own Table 2 (Linear Regression 0.330/0.284) makes plausible. Reported as "not reproduced within tolerance" with the floor stated; no accusation, just the floor.
- **Found — under our controls:** label source changes little (our labels 0.739/0.395/0.277/0.218). The embargoed temporal split is free (0.745/0.418/0.273/0.231). The **ticker-disjoint split raises MSE 9/23/46/36%** and drops R² vs persistence at τ=15 from 0.21 to 0.04 and at τ=30 from −0.15 to −0.38 — the same shape as our own Tables 2–4: what looks like transcript skill on the published protocol is largely company identity. At τ=30 the head is worse than persistence in every condition.
- **Caveats:** 560-call split copy vs the paper's 576; encoder substituted (BGE-M3 for WWM-BERT — favourable to the model); 30 epochs fixed (paper: "until converge"); auxiliary target under our labels is v_post(3). Seed std 0.01–0.04.
- **Next:** remaining 6R entries. SCSS (training-free PEV/STPEV on DEC — the CSV ships the features and rolling test masks) is cheap and next; then the code-availability audit table; KeFVP/DialogueGAT/Sawhney are multi-day ports and get reason codes if they do not run.

## 2026-09-03 (late) — T6R.2 CLOSED: SCSS reproduced to 3 decimals; code-availability audit; KeFVP/DialogueGAT/Sawhney reason-coded

- **Done:** `ecvol reproduce scss` (training-free; DEC ships features + rolling masks): 2023 quarterly-average MSE STPEV 0.240/0.253/0.227/0.246 vs published 0.239/0.253/0.227/0.246, PEV 0.309/0.331/0.264/0.278 vs 0.309/0.330/0.262/0.278 — the paper's Table 4 regenerates from its own CSV; STPEV under ticker-disjoint = reason code. `ecvol audit code`: 17 papers probed live (GitHub API: status, code files, last push, licence, probe date) + curated verdicts → 2 runnable (HTML, SCSS), 6 partial (Qin & Yang data-only, Sawhney 2020, VolTAGE, KeFVP, DialogueGAT, FinTrust), 9 none (Sound of Risk figures-only, DeFVP empty/409, ECHO-GL "can not run", ECC Analyzer, RiskLabs, NumHTML, GNA-Vol 404, AMA-LSTM stub, AT-FinGPT paywalled). Paper: Stage-R paragraph completed, A1 gets the audit table; DECISIONS 2026-09-03 (closing); TASKS T6R.2 → [x].
- **Found:** the two reproductions answer RQ3 from both sides — the transcript model (HTML head) loses 9/23/46/36% MSE when companies are held out, and the best published baseline (STPEV) is literally same-company history. Sawhney/KeFVP/DialogueGAT would add data points, not a new direction; the decision to reason-code them is logged with the revisit condition.
- **Next:** T7.2 (frozen-pipeline post-cutoff evaluation on Earnings25 — our FinCall-trained heads applied without retraining), then T8.1 (reproducibility package) and T8.2 polish. HANDOFF: re-baselining decision + push/CI still with the user.

## 2026-09-03 (later still) — T7.2 DONE: frozen FinCall heads do not degrade on post-cutoff Earnings25; ordering survives

- **Done:** `eval/lookahead.py` + `ecvol evaluate-lookahead` (manifested): the five canonical heads fit once on FinCall temporal train, scored unchanged on FinCall test (in-cutoff, 2020–21) and on all 475 Earnings25 calls (Q4 2025). Sanity: in-cutoff cells equal the committed Table 2/3 values to the third decimal, so the frozen runner is the same estimator as the tables. Month-cluster bootstrap CIs. 2 tests. Paper §6 subsection + `tables/lookahead.tex`; TASKS → [x].
- **Found:** no head degrades post-cutoff; all improve, because the in-cutoff test is the COVID regime. HAR-RV stays on top at every horizon (level-v post 0.56/0.47/0.32/0.08); the text and WavLM heads sit just below it (0.54/0.43/0.20/−0.33 and 0.53/0.40/0.09/−0.43) — i.e. they are past-vol regressors with a content term that does not add; on Δv the frozen text head gets 0.25 at τ=3 vs HAR 0.56. Fusion (3k+-dim ridge) is unstable post-cutoff (Δv τ=3 −7.0, CI −11.9 to +0.26). Nothing beats persistence at τ=30 either side of the cut-off. A lookahead artefact would look like the opposite pattern; we see none.
- **Caveats:** one post-cutoff quarter (475 calls, calm regime); ridge canonicals only; primary (assume-after-hours) targets on both sides — the measured-anchor variant exists for both corpora and could be swapped in with one flag if the user re-baselines.
- **Next:** T8.1 (reproducibility package: clean-machine regeneration record, released archives, release checklist) and T8.2 polish (abstract/intro numbers, limitations). All science tasks on the backlog are now closed or reason-coded.

## 2026-09-03 (close of day) — Paper stubs cleared; T8.1 package drafted; backlog state

- **Done:** rewrote the twelve stale `\pending{}` marks (abstract, intro contributions, data §3.2, methods, experiments, conclusion, A1) with the delivered numbers — the abstract now states the timing, reproduction, bitrate and lookahead results; the conclusion carries a verdict. PDF 26 pp, zero undefined refs. `REPRODUCE.md` (one command per table, 14 commands) and `LICENSE-DATA.md` (per-source licence audit with two open items: EC terms, openSMILE research licence). Gate: 324 tests green.
- **State of the backlog:** every science task is closed or reason-coded — T9.1, T9.2, T9.3, T9.4, T7.1, T7.2, T6R.1, T6R.2 all `[x]`. Open: T8.1 (archive upload + clean-machine run → user), T8.2 (review/venue → user), T8.3 (conditional cloud experiments, not planned). Main is ~75 commits ahead of origin; push + CI batched at the user's request.
- **For the user (HANDOFF):** re-baseline decision (A/B); push + CI; data-host account for the feature archives; EC terms check; advisor review of the draft.

## 2026-09-02 (later) — Clean-clone gate, measured-anchor lookahead, release archives, paper cross-check (12 fixes)

- **Done:** (1) fresh `git clone` + `uv sync` in an empty dir: ruff/format clean, 320 passed + 4 skipped (opensmile/funasr/torch stacks), `ecvol runs verify` 21/21 — the batched CI push should be green; found that default Windows git cannot check the repo out (`Filename too long`, an `ingest/` PDF) → `-c core.longpaths=true` in REPRODUCE.md. (2) `load_eval_frame(..., anchor=)` + `evaluate-lookahead` writes both anchors; `result_table_7.csv` byte-identical to the 2026-09-02 morning run (second live regeneration proof); new `result_table_7_measured.csv` manifested in the same run. (3) `src/ecvol/release.py` + `ecvol release build` + 2 tests; four zips (137.7 MB total) under `data/release/`, `data/manifests/release.json` committed. (4) A subagent cross-checked every number in `paper/sections` and `paper/tables` against the CSVs: all curated tables match cell-for-cell; 12 prose mismatches fixed (DECISIONS 2026-09-02 later, TASKS T8.2); Earnings25 now cited (`jiang2026earnings25`); `tables/lookahead_measured.tex` added; 27 pp, 0 undefined refs, 0 TODO/pending. Gate 327 passed.
- **Found (measured anchor, frozen heads):** HAR-RV moves ≤0.07; the ridge heads with the learned `[v_pre, RV]` block change character — in-cutoff Δv text+pastvol −1.68/−3.08/−3.59/−0.47 → +0.34/+0.28/+0.08/−0.08, WavLM+pastvol +0.27/−1.03/−2.03/−0.56 → +0.37/+0.27/+0.05/+0.05; post-cutoff fusion Δv τ=3 −7.0 → −0.75. Reading: the after-hours rule leaves the reaction session inside the pre-window for the 53% before-open calls, and a learned v_pre coefficient absorbs it; the "past-vol ridge ≠ HAR" limitation is at least partly an anchoring artefact. Level-v ordering unchanged (HAR > content heads everywhere). Exploratory: Δv τ=30 post-cutoff WavLM+pastvol 0.18 (CI 0.14–0.24) vs HAR 0.08 (CI −0.09–0.40) — one quarter, no DM test, not promoted.
- **Found (paper):** the T7.2 write-up's "2020 regime" was wrong — the FinCall temporal test year is 2021 (`result_table_4_peryear.csv`); corrected everywhere. The only DM-significant fusion cell is on ticker-disjoint, not temporal. Three numbers in the paper had no CSV source (call-time mention rate 3.4%, slide-stamp share 32.4%, EC boundary-day counts) — they come from JOURNAL/docs and are left as is.
- **Next (user):** re-baseline decision now has Stage-2–4 evidence (recommendation B unchanged, stronger); push + CI; data-host upload of `data/release/*.zip`; EC terms email (draft in HANDOFF); advisor review.

## 2026-09-09 — T6R.3 sprint opened: recon of the five repositories

- **Done:** user set the sprint policy (DECISIONS 2026-09-09). Six repositories full-cloned and pinned (`data/manifests/repos.json`). Five read-only recon agents, one per paper, produced run recipes; the durable parts are in the T6R.3 subtasks and will land in `docs/faithful_ledger.md` as each thread runs.
- **Found (headline per paper):**
  - *HTML:* the shipped driver (`run_gpu.py`) splits features, main labels and auxiliary labels with three independent unseeded `train_test_split` calls — X and y are decorrelated, so the code as shipped cannot have produced the paper's numbers; dropout is 0.0 in code vs 0.5 in Table 1; the lr warm-up sets `opt.lr` (a no-op); the paper says chronological 7:1:2, the code shuffles. The model classes are ~150 lines of plain PyTorch and run unchanged. Inputs (1024-d WWM-BERT layer-−2 sentence means, 27 Praat features) exist only behind dead Drive links → rebuild. Our own port's auxiliary label is wrong (`future_3` for every τ).
  - *KeFVP:* the audio branch is commented out in `CondInfer.forward`, so the published KeFVP is text+price only; the headline EC run needs the shipped price series plus one 1.2 GB KePt embedding pickle on Drive; MAEC embeddings were never released (regenerate raw BERT-base with their own script); requirements are uninstallable but the model is stock PyTorch; Table 2's MAEC-16 row does not average to its stated mean.
  - *SCSS:* TSMixer runs from DEC alone; TMLP needs OpenAI `text-embedding-3-large` vectors (Drive-only, 3072-d); no LLM baseline exists in the paper; 304 published MSEs are encoded in result filenames, which contain `|` and cannot be checked out on Windows.
  - *DialogueGAT:* Findings of EMNLP **2022** (not 2023); the corpus is the authors' unreleased SeekingAlpha re-scrape with global named speakers; labels need CRSP; DGL has no wheels for torch 2.11 → faithful run impossible; port only.
  - *Sawhney 2020:* EC-only; needs an unshipped call inventory, price series and two unsourced lexicons; TF 2.1 + Keras 2.3.1 + TFA 0.8.3 with delta's v1 graph layers; the code has **no multi-task loss** (single-task models late-fused by an (α,β) grid tuned on the test set) and its text-feature script returns after the first record; the model is ~0.65 M params → PyTorch port is the smaller job. Published numbers recovered second-hand from KeFVP Table 2 (no PDF locally).
- **Sources:** the five repositories at the pinned commits; `legacy/papers/papers with results from our datasets/{KeFVP,DialogueGAT,VolTAGE}.pdf`.
- **Next:** S1/S2 (fast win), K1/K2, H1/H2, K3, S4/H4, then the two ports, then the shared Praat extraction (H3/W3).

## 2026-09-09 (later) — T6R.3 sprint: five threads built; three faithful runs live

- **Done (committed, small steps):** `ecvol reproduce html-faithful | scss-tsmixer | scss-tmlp | kefvp | sawhney | dialoguegat` — six manifested commands, each with tests; `docs/faithful_ledger.md` records every verbatim/patched/substituted item. Jobs run detached; C: filled up mid-sprint (the 31.7 GB HuggingFace cache) → moved to `D:\hf-cache\hub` behind a directory junction, transparent to every library.
- **Results so far:** *SCSS TSMixer* with the authors' code: 76 cells, median |Δ| 1.6e-5, max 4.2e-3 — a clean reproduction. *Sawhney port* on EC: the finance SVR branch alone gives 0.712/0.370/0.218/0.154 against the published ensemble 0.601/0.308/0.181/0.119, and the 2-epoch text BiLSTM never leaves the untrained regime (MSE above persistence) so the (α, β) grid collapses onto finance — consistent with the released code carrying no multi-task loss and tuning the ensemble on the test set. *HTML faithful*, *DialogueGAT port*, *KeFVP MAEC* and *SCSS TMLP* are running.
- **Found (the released code, verbatim, does not run):** KeFVP's `final_series_infer.py` imports `set_seed` from a module that lacks it, its package `__init__` star-imports a file that references two classes the repo never defines, `transformers_model/modules.py` lost a `class GraphChannelAttLayer` header so that class's methods overrode `TransformerBlock`, and the `latent` package it imports was never released; plus placeholder paths, a colon in the log filename, pandas-3 dtype changes. Six patches, none numeric, all in the ledger. HTML's driver decorrelates X from y (see 2026-09-09 morning). SCSS's result filenames contain `|` and cannot exist on Windows.
- **Found (data):** the SCSS OpenAI embeddings and the DEC transcripts are on a public Drive folder (gdown stops at ~50 files per folder; the `Embeddings/openai` subfolder was fetched by its own id). The KeFVP EC embedding file is visible in the browser but refuses every scripted download → HANDOFF.
- **Next:** collect the four running tables, write the 6R-F paper table, then H3/W3 (shared 27-d Praat sentence features from the EC mp3s) and W2 (Mittens).

## 2026-09-09 (evening) — T6R.3 results: four of five papers have numbers; Table 6R-F drafted

- **Done:** results landed for SCSS TMLP (S3), HTML faithful text (H2), Sawhney with audio (W3), DialogueGAT port (D2); Praat features (H3) extracted for all 89,722 EC clips; H4 label fix in our own HTML port; `paper/tables/faithful.tex` + §6 "Reproductions with the authors' own code" (28 pp; six `\pending{}` cells for the runs still going). Every result is a manifested `ecvol reproduce` run; the ledger has the per-paper verbatim/patched/substituted rows.
- **Found:**
  - *HTML (authors' classes, rebuilt inputs):* the published recipe underfits at 10 epochs with raw log-volatility targets — code condition 0.660/0.336/0.253/0.160, paper condition 0.972/0.439/0.406/0.248, VolTAGE split 1.23/1.12/1.06/1.48 (seed spread 0.1–0.3), published 1.175/0.372/0.153/0.133. Only τ=3 on a random split beats the paper; no split beats persistence at τ≥15. Our earlier port (30 epochs, z-scored targets) got closer precisely by departing from the recipe.
  - *SCSS TMLP:* 228 cells, per-window means within ~0.01 of the published, the real ≈ ticker-shuffled finding reproduces.
  - *Sawhney port:* text (2 epochs) and audio (1 epoch) branches untrained (MSE 1.4–2.1 and ~5–6); the finance SVR alone 0.712/0.370/0.218/0.154; the test-tuned 3-way ensemble 0.639/0.298/0.175/0.102 vs published 0.601/0.308/0.181/0.119 with α=0, β≈0.1 — the untrained audio output is a constant that shifts the SVR's intercept, and test-set tuning finds it. Val-tuned: 0.686/0.324/0.187/0.117.
  - *DialogueGAT on FinCall:* 2019 0.694/0.320/0.206/0.134, 2021 0.794/0.344/0.182/0.082 (beats persistence), 2020 0.621/0.350/0.392/0.270 with every horizon stopping at epoch 0 across the COVID boundary — the same regime failure as our Stage-2 heads.
  - *Labels:* the shipped `future_Single_τ` = ln|r| on session +τ and the shipped `future_τ` is the mean-subtracted formula (both verified to 3 decimals against prices).
- **Still running:** KeFVP MAEC-15 (repeat 4 of 10 at τ=3; first repeat 0.414 vs published 0.418 ± 0.012), HTML text+audio, our HTML port rerun with the H4 label. **User:** KeFVP EC embedding download (HANDOFF).
- **Next:** fill the KeFVP and HTML-audio cells, code-audit verdict text for KeFVP ("runs after six import-level repairs"), then the controls sprint (embargoed / ticker-disjoint conditions for each faithful model).

## 2026-09-11 — T6R.3: KeFVP reproduces on MAEC; sprint results complete except the user-gated EC row

- **Done:** `ecvol reproduce kefvp --dataset 15,16` finished (10 repeats × 200 epochs × 8 cells, ~20 h under GPU contention, resumed twice after a capture-encoding crash and a full C: drive): MAEC-15 0.419/0.185/0.123/0.086 vs published 0.418/0.187/0.122/0.087; MAEC-16 0.442/0.288/0.363/0.188 vs 0.445/0.279/0.303/0.177 — seven of eight cells within one published standard deviation, seed spreads matching, with sentence embeddings regenerated by the authors' generator (they never released them). Table 6R-F, ledger, tracker updated; PDF rebuilt. Also today: our HTML port re-run with the corrected auxiliary label (ticker-disjoint penalty now +2/+20/+38/+36%, paper updated), the code-availability audit re-run (HTML reclassified partial: its driver is unimportable and decorrelates X from y → 1 runnable / 7 partial / 9 none), full gate green (355 tests, 27 manifested files).
- **Found:** the one KeFVP cell that misses (MAEC-16 τ=15, 0.363 vs 0.303) belongs to the row of their Table 2 that does not average to its own stated mean. KeFVP's audio branch is commented out in its own model, so what reproduces is a price-series model conditioned on text embeddings.
- **Ops:** C: filled to 0 bytes (user-side directories: Store packages 53 GB, programs 23 GB, CapCut 10 GB); `data/work` moved to `D:\ecvol-data\work_repo` behind a junction; `uv add` of torch_geometric/praat-parselmouth to the gpu group still pending (needs the venv idle + C: space) — HANDOFF.
- **Sprint tally (five papers):** SCSS — TSMixer and TMLP reproduce with the authors' code; KeFVP — MAEC reproduces with the authors' code, EC waits on a browser download; HTML — authors' classes verbatim, text-only recipe underfits on every split, text+audio reproduces only under the shipped code's random split; Sawhney — ported, published ensemble reachable with untrained branches via test-set tuning; DialogueGAT — ported to FinCall, beats persistence 9/12 cells, collapses across 2020.
- **Next:** controls sprint (embargoed / ticker-disjoint for each faithful model) once the user confirms; KeFVP EC after the download.

## 2026-09-16 — T6R.3/S4: SCSS augmented baselines on EC + MAEC reproduce exactly

- **Done:** `ecvol reproduce scss-aug` (commit 551f458, run 20260916T163850Z): the three functions of SCSS's `SS.ipynb` (`build_aug_stpev_mean`, `run_pev_stpev`, `run_EC_MAEC`) exec-loaded verbatim from their cells and run on the shipped EC/MAEC files as the notebook's driver cells do; the published values are parsed from the notebook's stored printed outputs. Plan changed from "re-implement the formulas" to "run the notebook code" once the code turned out to run unpatched — a stronger (a)-class reproduction. Tests: printed-table parser, verbatim loader on a synthetic notebook, window-crossing diagnostic with hand-counted business days. Table 6R-F gains three Aug_STPEV rows; §6 one paragraph; PDF 29 pp, 0 undefined refs. Ledger + tracker updated.
- **Found:** 48/48 cells equal at the notebook's 3-decimal rounding. Aug_STPEV — no text, no training — 0.569/0.293/0.201/0.122 (EC), 0.443/0.214/0.130/0.112 (MAEC-15), 0.470/0.238/0.168/0.113 (MAEC-16), against KeFVP's published 0.610/0.291/0.183/0.114, 0.418/0.187/0.122/0.087, 0.445/0.279/0.303/0.177: within 0.05 everywhere on EC/MAEC-15, below KeFVP at τ≥7 on MAEC-16. Caveats recorded: the augmented history is extra data outside the benchmark's training set; Aug_PEV averages the *test* tickers' per-ticker means (identity-dependent, not a pooled mean); history windows reaching the first test date are negligible (τ=30: 4/2195 EC, 8/3192 MAEC-15, 0 MAEC-16; τ≤15: none).
- **Sources:** `raw/ref/repos/SCSS_tree/SS.ipynb` cells 26–31 and `SameCompanySameSignal/dataset/{EC,MAEC}/*.csv` at the pinned commit (`data/manifests/repos.json`).
- **Next:** T6R.3's agent-doable subtasks are now all done; remaining: K1→K2 (user download), K4 (optional Colab). Then the provenance upgrade of the `backfill` tables (T9.3 note). Controls sprint waits on user scoping; push/CI (now 50 ahead) on the user.

## 2026-09-16 (CI) — CI checkpoint red on two latent platform bugs; fixed

- **Found:** the user's CI run was red — 2 failed / 349 passed. Neither came from today's work: `test_faithful_html::test_loader_execs_only_the_class_block` imports torch (CI is torch-free; since 2026-09-09), and `run_config`'s `weights_file` used `Path(...).name`, which on Linux does not split `D:\x\W.gguf` (since 2026-08-09). Both were invisible locally because the main venv has the gpu group and runs on Windows — the "local gate = faithful CI mirror" claim in LOOP.md was false, and no CI run had happened since before either commit.
- **Done:** `pytest.importorskip("torch")`; `PureWindowsPath(...).name`. Verified in a fresh torch-free venv (`UV_PROJECT_ENVIRONMENT=D:\ecvol-ci-venv`, `uv sync --locked`): 353 passed, 6 skipped; main venv 359 passed. LOOP.md step 5 amended with the torch-free run before CI checkpoints.
- **Next:** user re-runs CI and reports.

- **2026-09-16 (CI, later):** user reports CI green on `5187aeb` (all 56 commits since the previous checkpoint). Agent-doable backlog empty; remaining items are user decisions/actions (HANDOFF).

## 2026-09-16 → 09-17 — T6R.4 controls sprint: the five reproduced models under embargoed + ticker-disjoint splits

- **Scope (user, 2026-09-16):** all five models, cheapest first; KeFVP 3 repeats. Protocol decided in-sprint and logged (DECISIONS 2026-09-16 + two refinements): change only the split; re-run the anchor in the same invocation; where the controlled test set is a subset, score the unchanged model on exactly those calls (`anchor_heldout`); rules applied *inside* each model's own protocol where no committed split exists (SCSS rolling masks, DialogueGAT per-year, KeFVP MAEC split files), authors' code untouched.
- **Done:** `ecvol reproduce html-faithful|sawhney|dialoguegat` gained condition options; new `ecvol reproduce scss-controls` and `kefvp-controls`; shared `control_split` helper; all runs manifested; ledger controls section; paper Table 6R-C (`tables/repro_controls.tex`) + §6 subsection (30 pp, 0 undefined refs). Anchors reproduced T6R.3 bit-for-bit (HTML 24/24, Sawhney 124/124, SCSS) except the PyG DialogueGAT port (≤0.0014).
- **Found — the result:** removing same-company history costs every *text* model — HTML +18–82% (text) / +20–70% (text+audio), falling behind persistence at τ≥15; Sawhney val-tuned ensemble +13–41%, worse than its own finance SVR at τ≥7; SCSS TMLP +12–27%, worse in 16–18 of 19 quarters — while the *price-dominated* models do not move (TSMixer mixed around zero on the same reduced tickers; KeFVP no worse on MAEC-15, slightly better on MAEC-16). Training size does not explain it (EC ticker-disjoint training sets are as large as the anchors'; TMLP and TSMixer lose the same third). The 30-session embargo moves EC/DEC models by ≤13%. DialogueGAT port: inconclusive (one seed, 42–60 held-out calls/year). KeFVP embargoed MAEC rows are confounded (training 535→272, 980→295; MAEC-16 even improves via the re-carved dev set) — ledger only.
- **Found — bugs caught along the way:** (1) the embargo would have deleted chronological validation sets entirely (DialogueGAT, KeFVP) — `chrono_val` re-carves it; caught in a unit test before any run used it. (2) MAEC split files number pre-call windows backwards (`past_k` spans 30−k sessions; variance-verified) — my persistence column was wrong and crashed at τ=30; fixed + test; EC files checked and are forward. (3) Two low-memory kills of KeFVP (other apps hold ~20 GB of 31.6): the kill skipped the split-file restore, leaving MAEC-15 re-split in the build — restored, and `run_dataset` now restores shipped splits first; a per-dataset embedding subset cut peak RAM (free never below 7.3 GB after). (4) C6: writing the new table overwrote the existing `tables/controls.tex` (T3.4 identity table) — restored from git, new table renamed; a `sed` rename then broke `\label` — fixed; three prose over-claims (KeFVP "within spread", embargo "leaves training intact", "a few percent") corrected against the CSVs before commit.
- **Sources:** the pinned repositories (`data/manifests/repos.json`); runs 20260916T190656Z (Sawhney), 20260916T195449Z (SCSS), 20260916T200408Z / 212618Z (HTML), 20260917T003123Z (DialogueGAT), 20260917T053924Z / 133556Z (KeFVP).
- **Next:** CI checkpoint (user push); remaining user items unchanged — K1 KeFVP EC download, T9.2 re-baseline, calendar-day Stages 2–4, T8.1 data host, T8.2 review/venue. Possible follow-ups (not started, would need scoping): more DialogueGAT seeds; KeFVP embargo with training size held fixed.

- **2026-09-20 (CI):** user reports CI green on the T6R.4 push (head `0c52978`).
