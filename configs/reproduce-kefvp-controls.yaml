# Committed command config (T6R.4/C5). `ecvol reproduce kefvp-controls` loads this by default.
# 3 repeats per (condition, tau) are patched in (DECISIONS 2026-09-16).
command: reproduce-kefvp-controls
seeds: [1234]  # the script's own --seed default
