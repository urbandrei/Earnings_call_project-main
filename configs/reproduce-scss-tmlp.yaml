# Committed command config (T6R.3). `ecvol reproduce scss-tmlp` loads this by default.
# The authors' run.py fixes its own seed (2021); this list records that single seed.
command: reproduce-scss-tmlp
seeds: [2021]
